/**
 * MCP-021A — Family F (critical_question) definitions.
 *
 * Populated incrementally per design §3.6; this stub returns [] until
 * the Family F expansion commit lands.
 */

import type { MachineObservationDefinition } from '../nodeLabelTypes';

export const FAMILY_F_DEFINITIONS: ReadonlyArray<MachineObservationDefinition> =
  Object.freeze([]);
