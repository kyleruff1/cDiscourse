/**
 * MCP-021A — Family B (disagreement_axis) definitions.
 *
 * Populated incrementally per design §3.2; this stub returns [] until
 * the Family B expansion commit lands.
 */

import type { MachineObservationDefinition } from '../nodeLabelTypes';

export const FAMILY_B_DEFINITIONS: ReadonlyArray<MachineObservationDefinition> =
  Object.freeze([]);
