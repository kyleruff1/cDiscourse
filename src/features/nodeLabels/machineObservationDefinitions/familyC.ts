/**
 * MCP-021A — Family C (misunderstanding_repair) definitions.
 *
 * Populated incrementally per design §3.3; this stub returns [] until
 * the Family C expansion commit lands.
 */

import type { MachineObservationDefinition } from '../nodeLabelTypes';

export const FAMILY_C_DEFINITIONS: ReadonlyArray<MachineObservationDefinition> =
  Object.freeze([]);
