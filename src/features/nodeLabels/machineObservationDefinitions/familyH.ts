/**
 * MCP-021A — Family H (claim_clarity) definitions.
 *
 * Populated incrementally per design §3.8; this stub returns [] until
 * the Family H expansion commit lands.
 */

import type { MachineObservationDefinition } from '../nodeLabelTypes';

export const FAMILY_H_DEFINITIONS: ReadonlyArray<MachineObservationDefinition> =
  Object.freeze([]);
