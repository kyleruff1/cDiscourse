/**
 * MCP-021A — Family D (evidence_source_chain) definitions.
 *
 * Populated incrementally per design §3.4; this stub returns [] until
 * the Family D expansion commit lands.
 */

import type { MachineObservationDefinition } from '../nodeLabelTypes';

export const FAMILY_D_DEFINITIONS: ReadonlyArray<MachineObservationDefinition> =
  Object.freeze([]);
