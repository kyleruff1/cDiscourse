/**
 * MCP-021A — Family A (parent_relation) definitions.
 *
 * Populated incrementally per design §3.1; this stub returns [] until
 * the Family A expansion commit lands.
 */

import type { MachineObservationDefinition } from '../nodeLabelTypes';

export const FAMILY_A_DEFINITIONS: ReadonlyArray<MachineObservationDefinition> =
  Object.freeze([]);
