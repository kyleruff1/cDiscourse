/**
 * MCP-021A — Family I (thread_topology) definitions.
 *
 * Populated incrementally per design §3.9; this stub returns [] until
 * the Family I expansion commit lands.
 */

import type { MachineObservationDefinition } from '../nodeLabelTypes';

export const FAMILY_I_DEFINITIONS: ReadonlyArray<MachineObservationDefinition> =
  Object.freeze([]);
