/**
 * MCP-021A — Family G (resolution_progress) definitions.
 *
 * Populated incrementally per design §3.7; this stub returns [] until
 * the Family G expansion commit lands.
 */

import type { MachineObservationDefinition } from '../nodeLabelTypes';

export const FAMILY_G_DEFINITIONS: ReadonlyArray<MachineObservationDefinition> =
  Object.freeze([]);
