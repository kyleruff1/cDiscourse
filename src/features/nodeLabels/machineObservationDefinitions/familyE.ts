/**
 * MCP-021A — Family E (argument_scheme) definitions.
 *
 * Populated incrementally per design §3.5; this stub returns [] until
 * the Family E expansion commit lands.
 */

import type { MachineObservationDefinition } from '../nodeLabelTypes';

export const FAMILY_E_DEFINITIONS: ReadonlyArray<MachineObservationDefinition> =
  Object.freeze([]);
