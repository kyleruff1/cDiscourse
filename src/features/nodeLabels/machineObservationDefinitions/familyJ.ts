/**
 * MCP-021A — Family J (sensitive_composer) definitions.
 *
 * Populated incrementally per design §3.10; this stub returns [] until
 * the Family J retroactive backfill commit lands (no new entries; only
 * verbose definitions on the existing 5 sensitive entries).
 */

import type { MachineObservationDefinition } from '../nodeLabelTypes';

export const FAMILY_J_DEFINITIONS: ReadonlyArray<MachineObservationDefinition> =
  Object.freeze([]);
