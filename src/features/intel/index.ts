/**
 * INTEL-001 + INTEL-002 (#900 / #901) — intelligence derivations barrel.
 *
 * Pure-TS derivations that turn already-stored signals into repair-and-friction
 * intelligence: dodge-chains + debt-answer-rate (engagement lane / mediator
 * weighting) and the marker specificity KPI (admin-only advisory). No provider
 * spend, no migration, no per-person axis, no standing coupling.
 */
export * from './dodgeChainModel';
export * from './debtAnswerRateModel';
export * from './roomIntelSignals';
export * from './markerSpecificityModel';
