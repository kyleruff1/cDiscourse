# REF-002 — Review

**Verdict:** Approve
**Reviewer agent run:** 2026-06-12
**Branch:** feat/REF-002-open-issue-model-pure-ts-derivation
**Design:** docs/designs/REF-002.md (commit `93efc7e`)
**Parent contract:** docs/designs/REF-001-DISAGREEMENT-CONTRACT-REFEREE-CARD.md (ratified `fe35812`)
**Diff reviewed:** `93efc7e..e751cfc` (3 implementation commits: `2ded559` model, `c78598a` tests, `e751cfc` status)

## Summary
REF-002 ships REF-001's ratified `DisagreementContract` derivation as one pure-TS model
(`src/features/refereeLoop/openIssueModel.ts` + barrel `index.ts`), with a single
`gameCopy.ts` `moved_on` key added so all 8 `IssueState` codes resolve through
`toPlainLanguageOrSuppress`. The module is total, deterministic, non-mutating,
JSON-serializable, and value-imports only the two permitted pure joins (`buildActPopout`,
`deriveSuggestedMoves`) plus the frozen family-gate mirror — no React/Supabase/fetch/`Date.now`/
provider/banner-selection coupling (verified by both import-reading and the forbidden-imports
suite). Every emitted user-facing string comes from the four frozen REF-001 label maps or from
the surviving `ActPopoutEntry` definitions, all ban-list clean. All five operator ratification
constraints are honored and test-pinned. The one implementer-reported reconciliation (padding
`nextBestMoves` toward 2–3 with the next constructive Act survivors) is faithful to REF-001 (the
named authoritative parent contract), documented in the design's reconciliation note, the test
header, and the status entry, and pinned in `.actGateParity` — not silent drift. No concerns
remain; ready for PR.

## Verification
| Gate | Result (measured in worktree) |
|---|---|
| typecheck (`npx tsc --noEmit`) | **pass** — exit 0 |
| lint (scoped `eslint` on the 10 changed files) | **pass** — exit 0 |
| targeted (`npx jest openIssueModel refereeLoopForbiddenImports`) | **pass** — 5 suites / 256 tests, exit 0 |
| full (`npx jest --silent`) | **pass** — 720 suites / 29876 passed, 1 skipped (29877 total), exit 0 |
| baseline delta | 715 → 720 suites (+5); 29620 → 29876 tests (+256); skip 1 → 1 (unchanged) |
| secret scan | **clean** (only the current-status prose describing the `grep` command; no secret value) |
| doctrine scan | **clean** (verdict tokens appear only inside the ban-list array + doc-comment prohibitions) |
| Migration apply | **N/A** — no `supabase/migrations/**` in diff |

Note: the documented `moveMetadataLedger` META-001 60ms wall-clock flake did **not** trip this
full-suite run (exit 0, all green). The status entry's "may fail under parallel load" hedge is
honest; the captured count (720/29876+1skip) matches the entry exactly.

## Design conformance
- [x] All design file-changes are present (model, barrel, gameCopy key, 5 test files + fixture, status entry)
- [x] No undocumented file-changes (name-only diff is exactly the 10 expected files)
- [x] Data model matches design (`DisagreementContract`/`OpenIssue`, all sub-shapes, `BuildOpenIssueInput` field-for-field)
- [x] API contracts match design (`buildOpenIssue`, the 5 `derive*` helpers, `openIssueToRefereeCard` + `buildRefereeCardViewModel` alias)

## Ratification constraints (each a named verdict input)
1. **Derived-only / pure** — ✓ Imports: type-only domain unions (lines 44–63); value imports (66–71)
   are `OPEN_EVIDENCE_DEBT_STATUSES`, `ALL_MACHINE_OBSERVATION_FAMILIES`,
   `lookupMachineObservationDefinitionByCompoundKey`, `HUB_NON_PRODUCTION_FAMILIES`, `buildActPopout`/
   `flattenActPopout`, `deriveSuggestedMoves` — all pure. No React/Supabase/fetch/`Date.now`/provider.
   Pinned by `refereeLoopForbiddenImports.test.ts` (value-vs-type import discriminator; bans
   fetch/XHR/`Date.now`/secrets/`console.log`).
2. **`replies` fallback** — ✓ `specificOrNull` (openIssueModel.ts:697) collapses any tier that only
   maps to `replies`, so a specific lower-tier relation always wins (test:240–245). A bare `replies`
   never creates `reply_owed` and never clears a debt (burden step 3, openIssueModel.ts:754–766;
   test:305–312). All relation labels route through the frozen `RELATION_LABEL` map — no raw internal copy.
3. **`fact` never an axis** — ✓ `PointLifecycleAxis 'fact'`→evidence (LIFECYCLE_AXIS_MAP:382), Family B
   `disputes_fact`→evidence (DISPUTES_AXIS_MAP:358), `ActiveDisagreementKind 'fact'`→framing
   (ACTIVE_DISAGREEMENT_AXIS:402). Pinned at test `openIssueModel.test.ts:152–160`. The 8-value
   `DisagreementAxis` union contains no `fact`.
4. **Lifecycle map = all 19 live states** — ✓ `LIFECYCLE_STATE_MAP` (openIssueModel.ts:489–509) keys
   match the live `PointLifecycleState` union (pointLifecycleModel.ts:80–98) exactly: zero missing,
   zero extra. `Record<PointLifecycleState, IssueState>` makes tsc enforce totality; test:330–360 also
   asserts `ROWS` has length 19 and 19 distinct keys.
5. **Family-J exclusion via the derived mirror** — ✓ `OPEN_ISSUE_PRODUCTION_FAMILIES` =
   `ALL_MACHINE_OBSERVATION_FAMILIES.filter(!HUB_NON_PRODUCTION_FAMILIES.includes)` (openIssueModel.ts:556),
   mirroring `argumentDetailModel` (`HUB_NON_PRODUCTION_FAMILIES = ['sensitive_composer']`). No hardcoded
   roster. `.sensitiveSurface.test.ts:61` even sets a J mark to `priority:1` (would win the pick if the
   gate failed) and asserts it is dropped.

## Doctrine self-check (all ✓)
- [x] No truth/winner/loser language in user-facing strings — every emitted string scanned vs the 16 tokens (`.banlist`)
- [x] Score never blocks posting — model is consultative; emits no block/reject/route/delay; not in any submit path
- [x] No service-role in client code — `grep SERVICE_ROLE|ANTHROPIC_API_KEY src/features/refereeLoop/` empty
- [x] No direct insert into public.arguments — N/A (pure model, no DB)
- [x] No AI calls in production app paths — no provider import; consumes already-persisted Observations
- [x] Plain language only — all labels from frozen maps; rail states via `toPlainLanguageOrSuppress`; `looksLikeInternalCode` false for every label
- [x] Epic-specific (Rules UX / point-standing) — `OpenIssueDebt`/evidence-debt semantics consumed read-only; engagement/heat/popularity/standing never an input (per design Edge cases + skill §3)
- [x] §10a Observations vs Allegations — `refereeObservations` (machine) and `userAllegations` (user) are disjoint fields, never merged (`.sensitiveSurface.test.ts:129–140`); sensitive marks → composer/moderator visibility

## Gate honesty (checklist b)
- [x] `nextBestMoves ⊆ buildActPopout` survivors in all paths — property test across roles × parents × stages (`.actGateParity:43–63`), incl. padding (`PADDING_POOL` only adds Act survivors) and the recovery route
- [x] Nothing re-enables a gate-removed action — gate-removed suggestion → `branch_tangent` recovery (`isRecoveryRoute:true`), or dropped when branch_tangent itself is gated (observer); never the invalid action (test:106–116, 562–579)
- [x] Model not in any submit path — `grep refereeLoop|openIssueModel` outside the new files returns none; REF-003 consumes it later

## Test coverage
- [x] Every public function has unit tests (`buildOpenIssue`, `deriveOpenIssueAxis/Relation/Burden/State`, `deriveNextBestMoves`, `openIssueToRefereeCard`)
- [x] User-facing strings have ban-list assertion (16 tokens + snake_case, every frozen map + every view-model field across a spread of issues)
- [x] Design § "Edge cases" covered — null room/target → null; observer/own_bubble → `[]`; gate-removed → recovery; terminal burden=none reads state line; J-in-input dropped; verbatim quote/truncation
- [x] Fixtures use REAL seams (real `buildActPopout`/`deriveSuggestedMoves`/registry lookup), not mocks — the subset invariant is tested against the actual engine/role gate
- [x] No `.skip`/`.only`/`xit`/`xdescribe`; test count nondecreasing (+256); no guard loosened (no edits to any existing test/guard in the diff)

## Boundary (checklist e)
Diff touches exactly: `src/features/refereeLoop/{openIssueModel,index}.ts`, `src/features/arguments/gameCopy.ts`
(+ the one `moved_on` key + comment), `__tests__/openIssueModel.{test,banlist,actGateParity,sensitiveSurface}.ts`,
`__tests__/refereeLoopForbiddenImports.test.ts`, `__tests__/fixtures/openIssueFixtures.ts`,
`docs/core/current-status.md`. No `supabase/**`, no `mcp-server/**`, no `scripts/**`, no `.env*`, no migration,
no Edge. **Clean.**

## Blockers
None.

## Suggestions (non-blocking)
1. `OPEN_ISSUE_PRODUCTION_FAMILIES` value-imports `HUB_NON_PRODUCTION_FAMILIES` from
   `argumentDetailModel.ts` — import-safe today (the hub is React/Supabase-free at `c46c8e0`), and the
   design names `classifierHealthModel.FROZEN_NON_PRODUCTION_FAMILIES` as a fallback mirror if a future
   transitive import makes the hub heavy. No action now; worth a glance if REF-003/REF-004 pull the hub
   into a render path.
2. The forbidden-imports value-vs-type import parser is hand-rolled and tuned to this file's import style
   (one statement per `import`/`import type` line). It works and is correct here; if REF-003+ adopt inline
   `import { x, type Y }` mixing, extend the parser accordingly.

## Notes for REF-003's consumer contract
- Entry point is `openIssueToRefereeCard(issue)` (alias `buildRefereeCardViewModel`); it consumes the
  already-seeded contract and **never** calls `selectBanner` — REF-003 must keep the single banner surface.
- `nextBestMoves` is **0–3**: empty for observer/own_bubble, padded to ≤3 for participants. REF-003 must
  render an empty zone-3 gracefully (the view-model already omits the "Best next moves" clause when empty).
- `MoveSuggestion.label`/`accessibilityLabel` come verbatim from surviving `ActPopoutEntry` definitions
  (already plain + ban-list clean) — render as-is, do not re-map.
- `refereeObservations` is **≤ 1** (card-level summary); Inspect's grouped provenance is REF-003/
  `argumentDetailModel`'s job, explicitly out of scope here.
- Rail `IssueState` rendering must route through `toPlainLanguageOrSuppress` (unknown → suppress, never echo raw).
- `DisagreementContract.id` is deterministic but **never shown raw**.
- Family J is already gated out at the model; REF-003 must not re-introduce J marks onto the public card.

## Operator next steps
- Push the branch: `git push -u origin feat/REF-002-open-issue-model-pure-ts-derivation`
- Open PR: `gh pr create --title "REF-002: Open Issue model (pure TS derivation)" --body-file docs/reviews/REF-002.md`
- Deploy steps: **none** — pure `src` change; no migration, no `db push`, no `functions deploy`, no Edge, no env var. Merge is not a deploy.
- Post-merge worktree cleanup: see `.claude/agents/roadmap-reviewer.md` § "Post-merge worktree cleanup (operator step)".
