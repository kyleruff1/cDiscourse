# MCP-017 — Review

**Verdict:** Approve
**Reviewer agent run:** 2026-05-22
**Branch:** feat/MCP-017-anthropic-live-provider-pilot-for-the-se
**Design:** docs/designs/MCP-017.md
**Issue:** https://github.com/kyleruff1/cDiscourse/issues/221 (#221)

## Summary

MCP-017 un-stubs MCP-016's `anthropic` provider slot with a real, working live
Anthropic provider for the `semantic-referee` Edge Function. The implementation
adopted an interrupted earlier run's 11 partial files, audited them as faithful
to the design, and completed the card — 6 new test suites, the
`semanticNoLiveCall.test.ts` → `semanticNoLiveCallInRouter.test.ts` retarget,
the operator runbook, the current-status update. The build is doctrine-clean:
the live provider is opt-in (`?? 'mock'` default unchanged and its regression
guard strengthened), the live `fetch` and the single `Deno.env.get('ANTHROPIC_API_KEY')`
read are isolated to `anthropicProvider.ts`, every Anthropic response is
re-validated through `SemanticRefereePacketSchema` + a Deno content-safety
scanner, all six provider failure modes return a typed `unavailable` result
that never throws to the client, and the seed prompt asks for structural yes/no
answers only. Files-touched scope matches design §4 exactly; `process-language-draft/`
is zero-change. No concerns remain.

## Verification

- typecheck: pass
- lint: pass
- test: 8521 → 8657 tests / 303 → 309 suites (+136 tests / +6 suites)
- skills:validate: pass (both bot skills OK)
- secret scan: clean — every `ANTHROPIC_API_KEY` hit is the documented key name
  in design/runbook/test prose or the single legitimate `Deno.env.get()` read;
  no `sk-ant-` / `Bearer ` / `Authorization:` / JWT / `sb_secret_` literal
- doctrine scan: clean — every `winner/loser/liar` hit is a ban-list constant, a
  scanner-probe fixture, or a "do NOT" prohibition in the seed prompt

## Design conformance

- [x] All design file-changes are present (24 files; new + modified lists match §4 and Appendix A)
- [x] No undocumented file-changes (every changed file is in design §4 New/Modified)
- [x] Data model matches design (no type contract change beyond the sanctioned `ClassifyMoveDisabledReason` widening — Defect resolution 3)
- [x] API contracts match design (`runAnthropicClassifier(request): Promise<ProviderResult>`; six `ProviderUnavailableReason` values; async registry)

## Doctrine self-check (all ✓)

- [x] No truth/winner/loser language in user-facing strings — seed prompt + 23 question strings scanned by `semanticAnthropicSeedPromptBanList.test.ts`; no packet field for a verdict
- [x] Score never blocks posting — packet has no `block` field; `.strict()` schema rejects a smuggled one; `frictionSuggestion` advisory by contract
- [x] No service-role in client code — `SERVICE_ROLE` appears only in a negative-guard test assertion; provider builds no service-role client
- [x] No direct insert into public.arguments — none; `public.arguments` appears only in design prose stating the absence
- [x] No AI calls in production app paths — live `fetch` is in `anthropicProvider.ts` (Edge Function tree); `semanticAnthropicSourceScan.test.ts` proves the host literal + key read are each in exactly one file and absent from `src/`/`app/`
- [x] Plain language only — `ClassifyMoveDisabledReason` codes are boundary-transport only; no raw code surfaced in UI strings by this card
- [x] Epic-specific doctrine (supabase-edge-contract) — disabled-by-default check runs before any provider/key; every error path returns `{ enabled: false, reason }` HTTP 200; no migration, no RLS change, no `config.toml` change (`verify_jwt = true` unchanged); the boundary never throws

## Test coverage

- [x] New public functions have unit tests — `anthropicClassifierCore.ts` / `seedPrompt.ts` / `contentSafetyScan.ts` (the zod-free models) each have happy-path + failure-case tests; `anthropicProvider.ts` (zod-coupled, not Jest-importable) covered by source-scan + orchestration-shape suites, the same standard the sibling provider is held to
- [x] User-facing strings have ban-list assertion — `semanticAnthropicSeedPromptBanList.test.ts` (prompt) + `semanticAnthropicContentScan.test.ts` (packet content)
- [x] Edge cases from design have tests — all six failure modes (`key_missing`, `api_error`, `rate_limited`, `network_error`, `parse_failure`, `validation_failed`) each return a typed `unavailable` outcome
- [x] N/A — no UI card (Edge Function provider only); no accessibility surface

## The 10 critical checks

1. ✓ Secret scan — 0 hits for `ANTHROPIC_API_KEY` / `sk-ant-` / `Bearer ` / `Authorization:` as a leaked value; 0 `ANTHROPIC_API_KEY` in `src/`/`app/` executable code
2. ✓ Default-provider regression — `providerRoutingCore.ts` keeps `?? 'mock'` (line 92); the `semanticProviderRegistry.test.ts` "routes to mock when the flag is on and the provider is unset (doctrine default)" test survives verbatim and is strengthened (now also asserts `anthropicSpy` not called)
3. ✓ Source-scan — `semanticAnthropicSourceScan.test.ts` asserts `Deno.env.get('ANTHROPIC_API_KEY')` and the `api.anthropic.com` host each appear in exactly `['anthropicProvider.ts']`
4. ✓ Seed-prompt ban-list — `semanticAnthropicSeedPromptBanList.test.ts` scans all 23 questions + the system prompt for verdict/winner/loser/truth/popularity vocabulary by word-segment; passes
5. ✓ Log-redaction — `sanitizeRawPayload` retains exactly `{ model, stop_reason, usage }` (`anthropicClassifierCore.ts` line 142); `semanticAnthropicCore.test.ts` asserts every other key dropped; source-scan asserts no console call names the key/header/body
6. ✓ Validation gate — all six failure modes covered; each returns a typed `unavailable` `ProviderResult`; `runAnthropicClassifier` never throws (try/catch around `fetch` and `.json()`)
7. ✓ Files-touched scope — 24 files, all within design §4; `supabase/functions/process-language-draft/` is zero-change (confirmed by `git diff --stat`); `semantic-referee/index.ts`, `src/lib/edgeFunctions.ts`, and the type twins are sanctioned changes
8. ✓ `supabase/config.toml` — not in the diff; `[functions.semantic-referee]` `verify_jwt = true` unchanged
9. ✓ `semanticNoLiveCall.test.ts` retarget — renamed to `semanticNoLiveCallInRouter.test.ts`, re-scoped to the still-pure files; `anthropicProvider.ts` / `providerRouting.ts` / `providers.ts` correctly excluded; `providerRoutingCore.ts` excluded from the no-async assertion (its switch is legitimately async); every still-valid MCP-016 assertion kept; net 0 deleted tests
10. ✓ Doctrine vocabulary — no verdict/truth vocabulary in user-facing copy or packet content fields; 0 `SERVICE_ROLE` usage; no direct insert into `public.arguments`

## Acceptance-criteria audit (#221 §13)

All 9 ticked `- [x]` boxes correspond to real, tested deliverables — **no false ticks**:

1. ✓ `anthropicProvider.ts` exists, exports `runAnthropicClassifier`
2. ✓ `seedPrompt.ts` exists, exports `buildClassifierPrompt` / `CLASSIFIER_QUESTION_TEXT` / `SEED_PROMPT_VERSION`
3. ✓ `providers.ts` routes `anthropic` → `runAnthropicClassifier` via `DEFAULT_PROVIDER_DEPS` + the extracted `providerRoutingCore.ts`
4. ✓ `?? 'mock'` default unchanged; the default-fallback test passes
5. ✓ All §4 test files exist and pass (refined into the 6 design-§8 suites; every test intent covered)
6. ✓ `docs/deployment/mcp-017-live-provider-runbook.md` exists and covers the §9 outline
7. ✓ `typecheck && lint && test && skills:validate` all exit 0 (independently re-run)
8. ✓ `docs/current-status.md` updated with the new test count (8657 / 309)
9. ✓ No live call by the implementer — every test mocks / source-scans

## Blockers

None.

## Suggestions (non-blocking)

1. `semanticEdgeAuthAnthropic.test.ts` is honestly a source-shape scan, not a
   live `supabase functions serve` run — consistent with how MCP-016 handled
   `semanticEdgeAuth.test.ts` and explicitly sanctioned by design §8 ("the
   source-scan + core tests are the CI-time coverage"). The operator may
   optionally run the intercept-`fetch` integration pass locally per the
   runbook before flipping the flag. No change required.

## Operator next steps

- Push the branch: `git push -u origin feat/MCP-017-anthropic-live-provider-pilot-for-the-se`
- Open PR: `gh pr create --title "MCP-017: Anthropic live-provider pilot for the semantic-referee Edge Function" --body-file docs/reviews/MCP-017.md`
- Deploy (after merge — full procedure in `docs/deployment/mcp-017-live-provider-runbook.md`):
  - `npx supabase functions deploy semantic-referee --linked` — ships the live-provider code (still mock-behaving)
  - `npx supabase secrets list` — confirm `ANTHROPIC_API_KEY` present (reused from `process-language-draft`)
  - `npx supabase secrets set SEMANTIC_REFEREE_MODEL=claude-haiku-4-5` — optional model pin
  - `npx supabase secrets set SEMANTIC_REFEREE_PROVIDER=anthropic` — flip to live
  - Rollback is one line: `npx supabase secrets set SEMANTIC_REFEREE_PROVIDER=mock`
