# UX-001.6 Review — Cross-device QA and Visual Polish

**Card:** UX-001.6
**Branch:** feat/UX-001.6-cross-device-qa-and-visual-polish
**Reviewer:** roadmap-reviewer subagent
**Date:** 2026-05-25
**Verdict:** PASS
**Brief authorship:** orchestrator-draft operator-approved at f79da7c
**Design doc:** docs/designs/UX-001.6.md (commit f57e240)
**Issue:** https://github.com/kyleruff1/cDiscourse/issues/294

## Summary

UX-001.6 ships exactly what the brief and the operator-approved design
specified: cross-device QA verification of every UX-001.{1-5} product
contract across the four hard-blocker viewports plus two extension
viewports, expressed as five new pure-test files. The implementer
applied zero acute fixes from the §7 budget because the §1 audit
correctly found zero structural drifts and the parameterized matrix
verification confirmed it empirically. The read-only API boundary
holds: `git diff main..HEAD` shows zero bytes changed across all 52
enumerated UX-001.{1-5} source files, zero changes in
`supabase/functions/submit-argument/`, `supabase/migrations/`,
`package.json`, and `package-lock.json`. Doctrine is clean across all
five scans (verdict / internal-code / secrets / AI-import /
Observations-Allegations source-field constraint). The Phase 6 framing
section is empirically filled with the post-implementation surface
verdicts (18/18 surfaces clean, zero acute fixes, zero regressions,
EvidenceAnnotationChip refactor remains folded into UX-001.7).
Typecheck, lint, and the full 485-suite test run pass at exit 0;
15,661 tests / 485 suites passing matches the implementer's claim
exactly. The card is ready for the operator to push.

## Verdict matrix

| Item | Result | Notes |
|---|---|---|
| A. Doctrine constraints (verdict + internal-code) | PASS | Verdict-token matches in the diff are ALL inside the new `uxOneOneSixDoctrine.test.ts` declaring the ban-list itself (legitimate doctrine-test pattern); zero matches in user-facing copy. Internal-code scan over `src/` is clean. |
| B. Read-only API boundary | PASS | `git diff main..HEAD -- <UX-001.{1-5} source files>` returns zero bytes across all 52 enumerated paths (UX-001.1 brand shell, UX-001.2 Timeline + room chrome, UX-001.3 composer, UX-001.4 menu chassis, UX-001.5 nodeAnnotations + RefereeBannerView). `git diff main..HEAD -- src/` returns zero bytes total. No unenumerated modification. |
| C. UX-001.2 offset acceptance | PASS | `npx jest __tests__/uxOneOneTwoOffsetAcceptance.test.ts` → 11/11 passing, exit 0. The hard caps 128 / 168 / 200 / 200 hold. |
| D. UX-001.3 composer regression | PASS | `git diff main..HEAD -- src/features/arguments/composer/ src/features/arguments/ArgumentComposer.tsx src/features/arguments/ArgumentComposerDock.tsx` = 0 lines. |
| E. UX-001.4 menu chassis preservation | PASS | `git diff main..HEAD -- actPopoutModel.ts Popout.tsx PopoutEntry.tsx PopoutGroup.tsx` = 0 lines. |
| F. UX-001.4 duplicate-rail dispositions intact | PASS | `npx jest __tests__/duplicateRailRemovalDisposition.test.ts` → 37/37 passing, exit 0. The 12 migrate-to-Act, 5 preserve-as-shortcut, 4 retain-with-rationale dispositions still hold. |
| G. UX-001.5 primitive set preservation | PASS | `git diff main..HEAD -- src/features/nodeAnnotations/` = 0 lines. All 12 primitives + descriptor + tokens + aria/focus infrastructure + index byte-equal. |
| H. UX-001.5 semanticFlagsChips additive integration | PASS | `git diff main..HEAD -- inspectPopoutModel.ts inspectContentBuilder.ts InspectPopout.tsx` = 0 lines. The additive `semanticFlagsChips?` sibling is byte-equal. |
| I. UX-001.5 RefereeBannerView bounded modification intact | PASS | `git diff main..HEAD -- src/features/refereeBanners/` = 0 lines. The viewport matrix Surface 17 explicitly verifies the `observationChips` prop is composer-only via a board-level mount-site scan. |
| J. OPS regression | PASS | `npx jest __tests__/spawnCardBranchName.test.ts __tests__/deprecateAvatarMigration.test.ts` → 31/31 passing, exit 0. |
| K. Cross-device QA matrix coverage | PASS | `__tests__/uxOneOneSixViewportMatrix.test.ts` covers the 4 hard-blocker viewports (390×844 iOS, 1024×1366 iOS, 1366×768 web, 1920×1080 web) + 2 extension viewports (412×892 Android, 768×1024 iOS). Every cell encodes `{label, platformOs, windowWidth, windowHeight, band, expandedCeilingFraction, expectsKeyBadges, isHardBlocker}`. Surface count = 18, matrix = 6 × 18 = 108 cells. Sanity-checks (exactly 4 hard-blocker + 2 extension; all 3 bands covered) included. |
| L. Touch target compliance | PASS | `__tests__/uxOneOneSixTouchTargets.test.ts` enumerates 12 interactive files + 3 composing files (inheritance via PopoutEntry). Per-file × per-viewport assertions: presence of `<Pressable>`, `hitSlop` OR ≥44 `minHeight`/`POPOUT_ENTRY_MIN_HEIGHT`/`COMPOSER_STRIP_HEIGHT_BY_BAND`, `accessibilityRole`, `accessibilityLabel`. Specific assertions: AnnotationChip + AnnotationOverflowChip hitSlop=12 all sides; PopoutEntry POPOUT_ENTRY_MIN_HEIGHT=44 + PADDED_HIT_SLOP=8; CollapsedComposerStrip per-band minHeight 56/64/72; ComposerContextStrip expand-trigger hitSlop=14; Popout dismiss hitSlop=14; OneBox cancel hitSlop ≥10. |
| M. Keyboard hints platform-conditional | PASS | Surface 13 in the viewport matrix explicitly encodes `{platformOs, windowWidth}` per cell. Six cell-specific assertions cover the critical 1024-width platform-divergence case (iOS 1024 = touch; web 1024 = browser_keyboard). Additional cross-grid assertions verify 1366/1920 web → browser_keyboard; 390/412/768 native → touch. The pattern matches `accessibility-targets` §Keyboard hints platform-conditional pattern. |
| N. Color independence | PASS | `__tests__/uxOneOneSixColorIndependence.test.tsx` covers 3 representative surfaces: AnnotationChipStrip (aria labels carry meaning, no color-words leak), ArgumentTimelineMap node state (borderWidth/transform/opacity distinguishers asserted in source), AnnotationFocusRing (borderWidth=2-when-focused, opacity-1-when-focused, null-when-neither). Per-viewport × per-kind grayscale-safety assertions ban red/green/amber/orange/yellow/blue/crimson from aria labels. |
| O. Acute fix scope | PASS | Implementer's claim of 0 acute fixes confirmed by `git diff main..HEAD -- src/ 2>&1 | wc -l` = 0. The §7 budget (designer-proposed 6 of 5-10 ceiling) was reserved but not drawn. |
| P. Phase 6 framing for UX-001.7 | PASS | `docs/core/current-status.md` lines 1083+ contain the empirical Phase 6 framing section with: (1) all 18 surfaces enumerated as passing cleanly; (2) zero surfaces with acute fixes applied (with rationale); (3) zero pre-existing regressions documented; (4) EvidenceAnnotationChip disposition recorded ("remains folded into UX-001.7"); (5) verified-clean doctrine scans; (6) cross-platform parity findings on A/I/G key badge platform-conditional pattern; (7) test count delta (12,724 → 15,661 / 480 → 485). |
| Q. Test discipline | PASS | `npm test 2>&1 | tail -30 ; echo "EXIT: $?"` → `Test Suites: 485 passed, 485 total / Tests: 15661 passed, 15661 total / EXIT: 0`. Implementer's claim of 15,661 tests / 485 suites matches reviewer's recount exactly. No `.only` / `.skip` in new test files (file inspection confirms). |
| R. Typecheck + lint | PASS | `npm run typecheck ; echo "TYPECHECK EXIT: $?"` → exit 0; `npm run lint ; echo "LINT EXIT: $?"` → exit 0. |
| S. No new dependencies | PASS | `git diff main..HEAD -- package.json package-lock.json | wc -l` = 0. |

## Findings

None. All 19 verdict-matrix items PASS without blocking issues or
non-blocking suggestions worth recording.

The doctrine ban-list scan helper file (`uxOneOneSixDoctrine.test.ts`)
includes a self-aware second-order assertion: the two files that
LEGITIMATELY declare the ban-list itself (`designTokens.ts`,
`argumentGameSurfaceModel.ts`) are exempted from the verdict scan but
separately verified to expose the literals ONLY inside `FORBIDDEN_*`
/ `BANNED_*` arrays or comment blocks. This is a clean pattern.

The viewport matrix's Surface 13 (A/I/G key badge per-cell visibility)
correctly addresses the 1024-width platform-divergence subtlety the
audit identified: iOS at 1024 returns `touch` (platform gate); web at
1024 returns `browser_keyboard` (threshold gate). Per-cell encoding
prevents the false-positive failure mode the design's §11 risk section
named.

The test-count delta (12,724 → 15,661 = +2,937) exceeds the designer's
±200 estimate window (~1,250 ±200 = 1,050-1,450). The framing section
correctly traces the divergence to the doctrine suite's per-file ×
per-token × per-scan-class parameterization (52 source files × ~20
tokens × 4 scan classes ≈ 1,800 generated tests in `uxOneOneSixDoctrine`
alone, of the +2,937 delta). Per `test-discipline` §Gate timeout
handling, the contract is exit 0 + positive delta — not a specific
number — so the divergence is informational, not blocking.

## Recommendation

**PASS.** The card is ready for the operator to push to remote and
open the PR.

## Test suite snapshot

```
Test Suites: 485 passed, 485 total
Tests:       15661 passed, 15661 total
Snapshots:   0 total
Time:        14.319 s
Ran all test suites.
EXIT: 0
```

Typecheck: exit 0. Lint: exit 0.

## Operator next steps

- Push the branch: `git push -u origin feat/UX-001.6-cross-device-qa-and-visual-polish`
- Open PR: `gh pr create --title "UX-001.6: Cross-device QA and visual polish (UX-001 Phase 6)" --body-file docs/reviews/UX-001.6-review.md`
- Deploy steps: **None.** No migrations, no Edge Functions, no environment variables, no operator-side configuration. Pure test additions + design doc + framing refinement.
- Post-merge worktree cleanup (commands in `.claude/agents/roadmap-reviewer.md` § "Post-merge worktree cleanup (operator step)").
