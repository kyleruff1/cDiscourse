# QOL-041.1 — Review verdict

**Verdict:** approve
**Reviewer agent run:** 2026-05-23
**Branch:** fix/QOL-041.1-concession-acceptances-policy-ambiguity
**Base:** main HEAD = a41dd3c (QOL-041 squash)
**Branch HEAD:** 59a87ed
**Issue:** https://github.com/kyleruff1/cDiscourse/issues/256

## Skill attestation

- cdiscourse-doctrine: invoked ✅
- test-discipline: invoked ✅

## Summary

Fix-forward migration that resolves a SQLSTATE 42702 ambiguous-column-reference deploy blocker in the QOL-041 migration. Original migration not touched (per doctrine §8). New migration `20260523000001_qol_041_1_fix_concession_acceptances_policies.sql` DROPs and re-CREATEs the three affected INSERT policies (`ci_insert_author`, `ca_insert_receiver`, `mr_insert_reactor`) with each previously-unqualified outer `debate_id` reference qualified by its target table. Policy authorization semantics preserved verbatim — only column-reference disambiguation changes. All four verification gates exit 0; 9757-test baseline preserved.

## Verification

- typecheck: pass (exit 0)
- lint: pass (exit 0)
- test: 9757 tests / 372 suites passing (baseline unchanged)
- secret scan: clean
- doctrine scan: clean

## QOL-041.1 reviewer-addendum checks

- [x] **A1 New migration applies cleanly after original.** ✅ Local apply impossible — Docker Engine not running on this Windows machine (`npx supabase status` returns "open //./pipe/docker_engine: The system cannot find the file specified"). Verified by careful read instead, per the addendum's documented fallback:
  - (a) Every unqualified `debate_id` reference identified by the orchestrator's scan (original lines 92, 98, 198, 209, 286) is qualified in the new migration (new lines 58, 64, 91, 102, 127) — see Grep evidence below.
  - (b) Every DROP uses `DROP POLICY IF EXISTS … ON public.<table>` (lines 49, 80, 118), so the migration is idempotent and works whether the policy exists or not — this is essential since the original migration's transaction rolled back at policy #2, meaning policies #1 (`ci_insert_author`) and #3 (`mr_insert_reactor`) were never created remotely.
  - (c) CREATE POLICY statements reuse the original policy names verbatim (`ci_insert_author`, `ca_insert_receiver`, `mr_insert_reactor`), so all downstream references resolve.

- [x] **A2 Original migration NOT edited.** ✅ `git diff main..HEAD -- supabase/migrations/20260522000012_qol_041_concession_acceptance.sql` returns empty. `git diff main..HEAD --stat` shows only two files modified: `docs/core/current-status.md` (+15) and the new `supabase/migrations/20260523000001_qol_041_1_fix_concession_acceptances_policies.sql` (+129). Doctrine §8 ("Never edit an applied migration — write a new one") respected.

- [x] **A3 Fix is comprehensive across ALL affected policies.** ✅ Independent end-to-end re-scan of the original migration via Grep for every occurrence of `debate_id` yields exactly 13 hits:
  - Column definitions (no ambiguity possible): lines 49, 143, 243.
  - Index DDL (no ambiguity possible): lines 72-73, 178-179, 268.
  - WITH-CHECK subquery RHS where ambiguity matters: lines 92, 98, 198, 209, 286 — **exactly the 5 references the orchestrator flagged**.
  - No SELECT or UPDATE policy uses an unqualified `debate_id` in a subquery (verified by reading lines 105-130, 218-227, 292-316 — the SELECT/UPDATE policies use only `argument_id` joins or `author_id`/`reactor_id`/admin checks). Other `debate_id` occurrences in the new migration's qualified-rewrite are at lines 58, 64, 91, 102, 127 — 5 qualifications for 5 ambiguous sources, 1:1.
  - Conclusion: no additional ambiguous reference was missed.

## Standard checks

- [x] **Doctrine §1/§4 — no verdict / person tokens introduced.** ✅ `git diff main..HEAD | grep -iE '\b(winner|loser|liar|true|false|correct|dishonest|bad faith|manipulative|extremist|propagandist)\b'` returns empty. The new migration's prose is purely technical (root-cause description, qualified-column annotations).

- [x] **Doctrine §3 — Rules engine purity.** ✅ `git diff main..HEAD -- src/lib/constitution/engine.ts` is empty.

- [x] **Doctrine §6 — security.** ✅
  - No service-role usage in client (migration-only diff; `git diff main..HEAD -- 'src/**/*.ts' 'app/**/*.ts' | grep -iE 'SERVICE_ROLE|ANTHROPIC_API_KEY'` empty).
  - No `.env*` changes (`git diff main..HEAD -- '.env*'` empty).
  - Secret scan clean — no API key shapes, JWT shapes, Bearer tokens, or Authorization headers introduced.
  - Policy semantics preserved bit-for-bit. See "Policy semantic equivalence" below.

- [x] **Test discipline — no test changes required, 9757 baseline preserved.** ✅ `npm run test` reports `Test Suites: 372 passed, 372 total / Tests: 9757 passed, 9757 total`. The test files `__tests__/qol041-doctrine.test.ts` and `__tests__/submit-argument-concessions.test.ts` reference `concession_acceptances` (the table) by string; they do NOT pin the policy names themselves, so policy-name preservation is more than sufficient. (The migration also preserves the names verbatim — see A1c.)

- [x] **Migration sanity.** ✅
  - Uses `DROP POLICY IF EXISTS` (lines 49, 80, 118) — idempotent.
  - Creates EXACTLY 3 policies, one per affected target (`ci_insert_author` on `public.concession_items`, `ca_insert_receiver` on `public.concession_acceptances`, `mr_insert_reactor` on `public.move_reactions`).
  - No `ALTER TABLE`, no new column, no schema change, no RLS toggling — original migration already created the tables and enabled RLS.
  - Filename `20260523000001_qol_041_1_fix_concession_acceptances_policies.sql` lands in the next sequential slot after `20260522000012_…`; `ls supabase/migrations/` confirms it sorts last.

- [x] **Policy semantic equivalence.** ✅ For each fixed policy, the new WITH CHECK clause is logically equivalent to the original AUTHOR INTENT (the broken original couldn't apply on remote, so we compare to intent, not to a running policy). Verified by diffing the policy bodies character-by-character outside the `debate_id = …` lines:
  - `ci_insert_author` (orig lines 84-100 vs new lines 50-66): `auth.uid() = author_id` AND argument-belongs-to-debate-AND-author-matches AND conceded-to-belongs-to-debate. Identical authz: only the bare `debate_id` on lines 92 and 98 became `concession_items.debate_id` on lines 58 and 64.
  - `ca_insert_receiver` (orig lines 188-212 vs new lines 81-105): `auth.uid() = receiver_id` AND receiving-argument-in-same-debate-and-authored-by-receiver AND concession-item-in-same-debate AND conceded-to-author-is-receiver. Identical authz: bare `debate_id` on lines 198 and 209 became `concession_acceptances.debate_id` on lines 91 and 102.
  - `mr_insert_reactor` (orig lines 278-288 vs new lines 119-129): `auth.uid() = reactor_id` AND argument-in-same-debate. Identical authz: bare `debate_id` on line 286 became `move_reactions.debate_id` on line 127.
  - In all three cases the qualification picks up the target row of the INSERT (the only sensible disambiguation), which matches the author's original intent. No authz tightening or loosening.

## Findings

1. **(Informational, non-blocking)** The new migration intentionally omits any re-creation of SELECT / UPDATE policies. This is correct — the original migration's transaction failed at statement 18 (the `ca_insert_receiver` policy on line 188), which is AFTER `ci_select_read_access` (line 104) and `ci_update_soft_delete` (line 119), so on a partial-apply remote those two would already exist. But the original was wrapped in an implicit transaction (no `commit`/`rollback` markers in this file but `db push` wraps each migration), so the failure rolled back the entire migration. Either way, the re-run of the unedited original (after this fix-forward lands) will recreate every table-creating and policy-creating statement via the `create table if not exists` / `drop policy if exists` idempotents already present — confirmed by reading the original migration's defensive DDL. The two migrations apply cleanly in order.

2. **(Informational, non-blocking)** The `docs/core/current-status.md` addendum (+15 lines, +237..+251) is a clear, accurate operator-facing note that names the root cause, the doctrine rule applied, and the operator follow-up (`npx supabase db push --linked`). No prose changes elsewhere in the file.

3. **(Informational, non-blocking)** The original migration is byte-unchanged — the implementer did NOT take the easier-but-wrong path of editing the in-place file. This is the correct disposition per CLAUDE.md doctrine §8.

## Verdict

**approve**

All three reviewer-addendum checks (A1, A2, A3) and all six standard checks pass. The fix is minimal, doctrine-respecting, and addresses exactly the 5 ambiguous references identified by both the orchestrator and an independent re-scan. The operator's next step is `git push -u origin fix/QOL-041.1-concession-acceptances-policy-ambiguity` followed by PR open and merge; on merge the Supabase GitHub integration will auto-apply both migrations in order (per the supabase-merge-autodeploy memory).
