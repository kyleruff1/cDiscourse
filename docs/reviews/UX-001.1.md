# UX-001.1 — Review

**Verdict:** Approve
**Reviewer agent run:** 2026-05-24
**Branch:** `feat/UX-001.1-brand-and-app-shell-correction-ux-001-phas`
**Design:** `docs/designs/UX-001.1.md` (designer commit `f30fcda`)
**Issue:** https://github.com/kyleruff1/cDiscourse/issues/284
**Epic role:** UX-001 epic opener (Phase 1 of 7); §19 Phase 1 framing
is the load-bearing handoff to UX-001.2's designer.

## Summary

UX-001.1 ships the breakpoint + shell foundation for the seven-card
UX/UI Consolidation Wave. The implementer extends `BRAND` with a
3-band breakpoint contract (phone 0-599 / tablet 600-1279 / wide 1280+),
band-aware logo + header heights (44/80/96 logos; 64/96/120 headers),
and a minimum typography baseline — all additive on top of the
shipped BRAND-001 Stage 1+2 and BRAND-002 foundations. The legacy
`isWide` boolean, `logoHeightPxWide=110`, `headerHeightPxWide=152`,
and `headerWideBreakpointPx=720` constants are preserved as
back-compat aliases. The `useHeaderBreakpoint` hook gains `Band`,
`resolveBand`, and a `band: Band` field while honouring every Stage 1
+ Stage 2 invariant. `AppHeader` + `AppHeaderTagline` are modified
in place per the Q6 verdict; all 6 legacy testIDs + the public prop
surface preserved verbatim. The §19 Phase 1 framing section in
`docs/core/current-status.md` carries the seven binding patterns
UX-001.2's designer will consume as primary input. Test count grew
10734 → 10826 (+92 across 3 new suites). All verifications pass; no
out-of-scope file touched.

## Verification

| Check | Result |
|---|---|
| typecheck | **pass** (`tsc --noEmit` exit 0) |
| lint | **pass** (`eslint . --max-warnings 0` exit 0) |
| test | **pass** — 10734 → 10826 tests / 427 → 430 suites (12.6 s) |
| secret scan | **clean** (only hits are doctrine-grep examples in design doc + prior-PR descriptions in current-status.md; no actual secret-shape literal in source) |
| doctrine ban-list scan | **clean** (single match: the word "truth" inside a JSDoc comment explicitly negating verdict/popularity/truth vocabulary — a doctrine guarantee, not a violation) |
| public.arguments direct insert scan | **clean** (zero matches) |
| service-role / Anthropic-key / console.log in `src/` diff | **clean** (zero matches) |
| package.json / package-lock.json | **unchanged** (no new dependency, per Q3 single-PNG + Q6 in-place verdict) |
| Existing BRAND suite (`appHeader.test.ts`, `appHeaderTagline.test.tsx`, `useHeaderBreakpoint.test.ts`, `darkSurfaceTokens.test.ts`) | **160/160 pass** |
| New UX-001.1 suites (`uxOneOneBreakpointBands.test.ts`, `uxOneOneTypographyBaseline.test.ts`, `uxOneOneAppHeaderDensity.test.ts`) | **92/92 pass** |
| OPS regression (`spawnCardBranchName.test.ts`, `deprecateAvatarMigration.test.ts`) | **31/31 pass** |
| Branch rename (OPS-002 step) | **CLEAN** — 5th end-to-end success since OPS-004; no stale-worktree recovery needed |

## Design conformance

- [x] All design file-changes present (5 modified + 3 new test suites + design doc — matches §4 file-change table)
- [x] No undocumented file-changes (diff scope: `src/lib/designTokens.ts`, `src/hooks/useHeaderBreakpoint.ts`, `src/components/AppHeader.tsx`, `src/components/AppHeaderTagline.tsx`, `__tests__/useHeaderBreakpoint.test.ts` (7-line edit), 3 new `__tests__/uxOneOne*.test.ts`, `docs/core/current-status.md`, `docs/designs/UX-001.1.md`)
- [x] Data model matches design (`BRAND.breakpoints`, `BRAND.logoHeightByBand`, `BRAND.headerHeightByBand`, `BRAND.typography` — all 4 fields land at exactly the §4 specified shapes; legacy aliases preserved byte-identical)
- [x] API contracts match design (`AppHeader` props unchanged; `AppHeaderTagline` props unchanged; `HeaderBreakpoint` interface extended additively with new `band` field; new `Band` type + `resolveBand` helper exported)

## Q1-Q8 designer-question coverage

| Q | Verdict | Implementation | Status |
|---|---|---|---|
| **Q1** Breakpoints | 3-band phone/tablet/wide; isWide alias | `useHeaderBreakpoint.ts` exports `Band` + `resolveBand`; isWide = `band !== 'phone'`; SSR width=0 → wide | **PASS** |
| **Q2** Surface hierarchy | Map mental model; no new token | JSDoc block in `designTokens.ts` documents primary/secondary/tertiary → `BRAND.surface.app.bg` / `BRAND.surface.appElevated.bg` / `SURFACE_TOKENS.overlay`; `SURFACE_TOKENS` 14 keys + `CONTROL` 3 keys unchanged | **PASS** |
| **Q3** Responsive logo | 44/80/96 per band; single PNG via `resizeMode="contain"` | `BRAND.logoHeightByBand = {phone:44, tablet:80, wide:96}`; `AppHeader.tsx` renders Image with `style={{ height: logoHeightPx }}` + `resizeMode="contain"`; single asset `civic-discourse-logo.png` | **PASS** |
| **Q4** Header density | Right-slot capacity 1/3/4 | Documented in Phase 1 framing pattern 5; per-band `getHomePressableMinWidth` returns 120/200/240; comments enumerate hit-target arithmetic | **PASS** |
| **Q5** Header height | 64/96/120; tightens 152 → 120 wide | `BRAND.headerHeightByBand = {phone:64, tablet:96, wide:120}`; legacy `headerHeightPxWide=152` preserved as alias only | **PASS** |
| **Q6** In-place modification | Modify AppHeader/AppHeaderTagline; preserve testIDs + props | All 6 testIDs preserved (`app-header`, `app-header-home`, `app-header-logo-image`, `app-header-logo-fallback`, `app-header-right-slot`, `app-header-divider`); `APP_HEADER_HEIGHT === BRAND.headerHeightPx` still 64; props (`onHomePress`, `rightSlot`, `logoSource`) unchanged | **PASS** |
| **Q7** BRAND extension | Extend `designTokens.ts`; NO new `uxTokens.ts` | All 4 new fields added inside existing `BRAND` object; no new tokens file; `TOKENS` aggregate picks them up automatically | **PASS** |
| **Q8** Typography baseline | 3 sub-objects (wordmarkFallback, tagline, header) | `BRAND.typography` has exactly the 3 sub-objects from §12; `AppHeader` reads `wordmarkFallback[band]`; `AppHeaderTagline` reads `tagline.inline/.stacked`; `header` reserved for UX-001.4 | **PASS** |

## Doctrine self-check (universal + epic)

- [x] **No truth/winner/loser language in user-facing strings** — no copy added; tagline + wordmark fallback preserved verbatim; ban-list scan only matches the word "truth" inside a JSDoc comment explicitly negating verdict vocabulary
- [x] **Score never blocks posting** — N/A (no scoring or submit gate touched)
- [x] **No service-role in client code** — zero `SUPABASE_SERVICE_ROLE_KEY` references in source diff
- [x] **No direct insert into public.arguments** — N/A (no DB touched)
- [x] **No AI calls in production app paths** — no Anthropic/xAI/X API surface added or imported
- [x] **Plain language only (no raw internal codes in UI strings)** — only structural English token keys (phone/tablet/wide, inline/stacked); no internal codes leak
- [x] **expo-rn-patterns** — no new dep (`package.json` byte-identical); RN primitives only (`View`, `Text`, `Image`, `Pressable`, `useWindowDimensions`, `Platform.select`); `useWindowDimensions` used (not CSS media queries — works on native)
- [x] **accessibility-targets** — hitSlop preserved at `{top:8, bottom:8, left:8, right:8}`; per-band `minWidth` 120/200/240 all exceed 44dp horizontal; composed `accessibilityLabel = "CivilDiscourse, Just get to the bottom of it"` preserved; `accessibilityRole="header"` on root, `accessibilityRole="button"` on home pressable, `accessibilityRole="text"` on tagline; `allowFontScaling` preserved; WCAG AA contrast pinned by BRAND-001/002 tests untouched
- [x] **timeline-grammar (no leak into shell)** — `AppHeader` / `AppHeaderTagline` / `useHeaderBreakpoint` / `designTokens.ts` (BRAND object) do NOT import any timeline-grammar token; divider remains 1px hairline (no shape encoding)
- [x] **TL-003 no-route invariant** — `AppHeader.tsx` imports no `@react-navigation/*` or `expo-router`; state-only `onHomePress` callback path preserved

## Test coverage

- [x] **+92 new tests across 3 new suites** (design said +70; the overshoot is fine — all extra tests are genuine extensions of the Q1-Q8 contract, not noise)
  - `__tests__/uxOneOneBreakpointBands.test.ts` (39 tests) — Q1+Q3+Q5: band shape, contiguity, `resolveBand` at 14 canonical widths, isWide back-compat, height token values, monotonic growth, padding budget
  - `__tests__/uxOneOneTypographyBaseline.test.ts` (25 tests) — Q8+Q2: typography shape, numeric invariants, source-scan that AppHeader/AppHeaderTagline now read tokens (not inline literals), doctrine ban-list, Q2 mental-model SURFACE_TOKENS + CONTROL pins
  - `__tests__/uxOneOneAppHeaderDensity.test.ts` (28 tests) — Q4+Q6+Q1: testID preservation, public-surface preservation, band-aware source references, per-band minWidth 120/200/240 literals, no animation/router/icon-lib imports, hitSlop ≥ 8, BRAND tokens for color, no heavy shadow, `Band` type + `resolveBand` helper exports
- [x] **Existing 160 BRAND tests pass byte-identically** with the documented exception of the 7-line edit to `__tests__/useHeaderBreakpoint.test.ts` (5 logical assertions at widths 720/1024/719/0). The implementer note appended to the design doc captures the rationale: the design's §14 "byte-identical preservation" claim conflicted with §4's binding directive to populate `logoHeightPx`/`headerHeightPx` from the new `*ByBand` maps; the implementer updated the 7 lines to read `BRAND.headerHeightByBand.tablet|wide` / `logoHeightByBand.tablet|wide` instead of the legacy `*Wide` aliases. The legacy token assertions (`logoHeightPxWide=110`, `headerHeightPxWide=152`, `headerWideBreakpointPx=720`) remain pinned at the token level. Intentional semantic shift; design narrative anticipated it; commit message + design implementer note document it.
- [x] **Edge cases from design §13** all covered (SSR width=0, negative width, ≤320dp phone, ultrawide 3440+, boundary widths 599/600/1279/1280, isWide back-compat at 720, asset missing → wordmark fallback)
- [x] **Accessibility assertions present** — `app-header-*` testIDs + accessibilityRole assertions remain pinned; hitSlop arithmetic enforced in new density suite; ban-list applied in new typography suite

## Read-only boundary verification

- [x] **NO timeline files touched** — `git diff --stat -- src/features/arguments/` returns empty
- [x] **NO composer files touched** — `git diff --stat -- src/features/composer/` returns empty
- [x] **NO Act/Inspect/Go popouts touched** — `git diff --stat -- app/` returns empty
- [x] **NO metadata layer touched** — `git diff --stat -- src/features/metadata/` returns empty
- [x] **NO migration / Edge Function** — `git diff --stat -- supabase/` returns empty
- [x] **NO other design or review docs modified** — `git diff --stat -- docs/designs/ docs/reviews/` only shows `docs/designs/UX-001.1.md` itself (1467 lines added)
- [x] **NO new dependency** — `package.json` + `package-lock.json` byte-identical

## Phase 1 framing section verification (load-bearing for UX-001.2)

`docs/core/current-status.md` lines 24-142 contain the binding handoff
section "UX-001.1 — Phase 1 framing for UX-001.2 (binding handoff)".
All seven required patterns are present with concrete values + file
references:

| # | Pattern | Verdict |
|---|---|---|
| 1 | Breakpoint contract (3 bands with pixel boundaries + rationale) | **PASS** — table at lines 31-37 with phone 0-599, tablet 600-1279, wide 1280+; rationale paragraph cites Material Design 3 + iOS HIG + laptop-class minimum |
| 2 | Header heights per band (64/96/120) | **PASS** — table at lines 47-51 with explicit pixel values + token references; explicit "no buried board" framing + UX-001.2's vertical-offset consumption instructions |
| 3 | Logo heights per band (44/80/96) | **PASS** — table at lines 62-66 with token references; single-asset note + legacy alias preservation note |
| 4 | Surface hierarchy mental model (primary/secondary/tertiary → app/appElevated/overlay) | **PASS** — table at lines 76-80 mapping all three layers to concrete tokens; explicit "NO new surface token" callout |
| 5 | Header density contract (right-slot capacity 1/3/4) | **PASS** — table at lines 90-94 with per-band capacity; UX-001.2 vs UX-001.4 ownership boundary stated |
| 6 | Typography baseline (3 sub-objects) | **PASS** — full enumeration of `wordmarkFallback`, `tagline`, `header` sub-objects at lines 100-119 with every fontSize/lineHeight/fontWeight/letterSpacing value spelled out; UX-001.7 deferral noted |
| 7 | Read-only file references for UX-001.2 to consume | **PASS** — 8-row table at lines 121-132 enumerating every readable file + modification rights per pattern; explicit UX-001.2-owns paragraph at lines 134-137 |

The section closes (line 139-142) with the explicit unblock claim
("UX-001.2's designer should not need to re-decide breakpoints, header
heights, or surface hierarchy") and a back-reference to `§19` of the
design doc for the verbatim contract. PR-004-analogue completeness:
ready for UX-001.2 designer launch.

## Operator next steps

1. Push the branch: `git push -u origin feat/UX-001.1-brand-and-app-shell-correction-ux-001-phas`
2. Open PR: `gh pr create --title "UX-001.1: Brand and app shell correction (UX-001 Phase 1)" --body-file docs/reviews/UX-001.1.md`
3. After merge, file the UX-001.2 launch prompt using `docs/core/current-status.md` lines 24-142 (the Phase 1 framing section) as the primary input. UX-001.2's designer reads it as a binding contract.
4. Smoke-check at three viewports per design §"Operator steps":
   - Phone portrait (≤390dp): 64dp header, 44px logo, stacked tagline
   - Tablet portrait (~768dp): 96dp header, 80px logo, inline tagline
   - Web/desktop (≥1280dp): 120dp header, 96px logo, inline tagline
5. Resize browser across 599→600 and 1279→1280 boundaries to confirm clean band swap.

**No migration, no Edge Function deploy, no new env var, no Supabase
write, no dependency install required.** Pure UI shell card.

## Blockers

None.

## Suggestions (non-blocking)

1. **Test count overshoot vs design estimate.** Design §4 estimated +70 tests; implementer delivered +92. Overshoot is genuine added coverage (boundary tests + monotonic-growth invariants + hit-target arithmetic), not noise — but a future card's designer may want to budget +30% for "Q-question coverage tends to generate more tests once committed to code than the design narrative anticipates". Operator-judgement, no action required.
2. **Legacy alias deprecation timing.** UX-001.1 preserves `BRAND.headerWideBreakpointPx=720` / `logoHeightPxWide=110` / `headerHeightPxWide=152` as back-compat aliases. The implementer note at the bottom of `docs/designs/UX-001.1.md` records the 7-line semantic shift in `useHeaderBreakpoint.test.ts` — UX-001.7 should consider whether the legacy aliases still earn their keep once the new band-aware contract has shipped through all 7 epic cards. Not blocking; UX-001.7's design will decide.
