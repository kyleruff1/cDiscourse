# PR-004 — Review

**Verdict:** Approve
**Reviewer agent run:** 2026-05-24
**Branch:** feat/PR-004-contact-information-update
**Design:** docs/designs/PR-004.md

## Summary
PR-004 ships the Profile-epic closer cleanly. The deprecation half reverses
PR-003's avatar pipeline byte-precisely (storage SELECT policy drop + 4
profile columns drop + narrowed UPDATE drop + byte-equal restored UPDATE
policy from migration 02) while honouring the 2026-05-24 PR-003 lesson (no
`COMMENT ON POLICY ... ON storage.*`, no `delete from storage.buckets`).
The contact-info half adds the first use of `supabase.auth.updateUser` in
the codebase via a tight client wrapper with full plain-language error
mapping and a verification-pending UI state. PR-003's Q1/Q6 patterns are
preserved with `contact-*` testID prefix; Q2/Q3/Q4/Q5 are correctly
superseded. 5 PR-003 test files (~195 tests) deleted as expected, replaced
by 5 new suites with 100% of the design's Q1-Q5 coverage and the OPS-001
four-class migration source-scan. Boundary respected: zero modifications
to META-*/QOL-*/COMP-*/OPS-* source. OPS-002/003 regression 13/13 still
green.

## Verification
| Check | Result |
|---|---|
| typecheck | pass |
| lint | pass |
| test | 10839 → **10731 / 427** (5 added + 5 removed; -108 net; suites unchanged) |
| secret scan | clean (matches are audit-trail tests + design discussion) |
| doctrine scan (verdict tokens) | clean (no winner/loser/liar/etc.) |
| doctrine scan (SERVICE_ROLE in client) | clean (matches in `edgeFunctions.ts` / `argumentScenarioValidation.ts` are pre-existing, unmodified) |
| direct insert into `public.arguments` | none |
| OPS-002/003 regression (`spawnCardBranchName.test.ts`) | 13/13 pass |
| OPS-001 four-class migration review | textual review — Docker not attempted; migration's own header walks all four classes inline + `deprecateAvatarMigration.test.ts` (15 tests) codifies the review |

## Design conformance
- [x] All design file-changes are present (28 files touched: matches launch-prompt expectation)
- [x] No undocumented file-changes
- [x] Data model matches design (4 columns dropped; profiles row reverts to id + display_name + role + created_at)
- [x] API contracts match design (`contactApi.ts` exports `validateEmail`, `messageForContactError`, `requestEmailChange`, `ContactEmailChangeError`, `PendingEmailChange`, `ContactApiResult` per §4)
- [x] Q1 — Contact fields: only display_name + email (no phone / location / bio / links)
- [x] Q2 — Validation: display_name 1-60 chars + email RFC-shape check; inline `accessibilityLiveRegion="polite"` errors
- [x] Q3 — Propagation: no realtime channel introduced; relies on `useAccountProfile` refetch
- [x] Q4 — Read auth: no new RLS policies (`auth.users.email` is the source of truth; `profiles.display_name` reads via existing policy)
- [x] Q5 — `InitialsAvatar.tsx` exists at `src/features/account/`; `GeneratedAvatar.tsx` is a back-compat re-export shim; PR-001 tests pass through it (42 tests in `userPreferencesModel.test.ts` still green)

## Doctrine self-check (must all be ✓)
- [x] No truth/winner/loser language in user-facing strings (verified by `contactDoctrine.test.ts` ban-list across 5 source files; reviewer-run grep on diff also clean)
- [x] Score never blocks posting (PR-004 touches zero scoring code)
- [x] No service-role in client code (`contactDoctrine.test.ts` source-scan asserts; reviewer-run grep on `src/**` diff confirms zero matches)
- [x] No direct insert into `public.arguments` (PR-004 touches neither path)
- [x] No AI calls in production app paths (zero `anthropic` / `xai` / `openai` imports per source-scan)
- [x] Plain language only — `messageForContactError` returns a plain string for every code; `contactDoctrine.test.ts` asserts no internal code (snake_case) leaks into any `<Text>` body
- [x] **cdiscourse-doctrine §8 (Supabase conventions):** migration is append-only (new file 17, no edit of 16); RLS not toggled off; restored policy uses existing `is_moderator_or_admin()` helper
- [x] **supabase-edge-contract:** no new Edge Function; existing `upload-avatar` deleted at filesystem; operator deletes deployed function via `npx supabase functions delete upload-avatar --linked`; migration deploy via `npx supabase db push --linked`; storage bucket NOT dropped (storage_admin ownership boundary respected — documented in migration header + `deprecateAvatarMigration.test.ts` forbids `drop bucket` / `delete from storage.buckets` in executable SQL)
- [x] **supabase-edge-contract storage lesson:** zero `COMMENT ON POLICY ... ON storage.*` statements in migration 17 (executable SQL stripped of comments; `deprecateAvatarMigration.test.ts` regex-asserts)
- [x] **expo-rn-patterns:** one dep REMOVED (`expo-image-picker`); zero added; `InitialsAvatar` uses only `<View>` + `<Text>` RN primitives
- [x] **accessibility-targets:** every `Pressable` in `ContactInfoSection.tsx` has `accessibilityRole="button"` + `accessibilityLabel` + `accessibilityState` + `hitSlop` or `minHeight: 44`; inline error `<Text>` uses `accessibilityLiveRegion="polite"`; `InitialsAvatar` preserves `accessibilityRole="image"` + label; 8-color palette WCAG-AA contrast against `#f8fafc` verified by `initialsAvatar.test.ts` luminance guard

## Deprecation reversal verification (PR-004-specific)
- [x] Migration 17 drops `storage.objects` policy `"profile-avatars: anyone can read"` (line 113, `IF EXISTS`)
- [x] Drops 4 profiles columns `avatar_path`, `avatar_thumb_path`, `avatar_updated_at`, `avatar_moderation_status` (lines 121-125, all `IF EXISTS`); CHECK constraint dropped automatically with the column
- [x] Drops narrowed UPDATE policy `"profiles: users update own — narrow"` (line 128, `IF EXISTS`)
- [x] Restores byte-equal prior UPDATE policy from migration 02 lines 69-73 (verified by `deprecateAvatarMigration.test.ts` "byte-equal text" assertion comparing USING/WITH CHECK clauses)
- [x] Restore is idempotent: `DROP POLICY IF EXISTS "profiles: users update own; mods update any"` precedes `CREATE POLICY` (line 137 before line 139)
- [x] Bucket itself is NOT dropped (no `delete from storage.buckets`, no `drop bucket` — confirmed by `deprecateAvatarMigration.test.ts`)
- [x] No `DROP TABLE` / `DROP FUNCTION` / `DROP EXTENSION` / `DROP TRIGGER` anywhere
- [x] No service-role escalation, no `SET ROLE`, no `SECURITY DEFINER`

## Clean deletion verification
- [x] `src/features/account/avatarApi.ts` fully deleted
- [x] `src/features/account/AvatarUploadSection.tsx` fully deleted
- [x] `supabase/functions/upload-avatar/index.ts` fully deleted (whole dir gone)
- [x] 5 PR-003 test files deleted: `avatarApi.test.ts`, `avatarDoctrine.test.ts`, `avatarUploadSection.test.tsx`, `avatarUploadValidation.test.ts`, `uploadAvatarEdgeFunction.test.ts`
- [x] `grep avatarApi|uploadAvatar|AvatarUploadSection src/` returns zero matches under `src/features/account` and zero matches under `supabase/functions`; the two `src/lib/edgeFunctions.ts` + `src/features/devFixtures/argumentScenarioValidation.ts` references in the diff are pre-existing `SERVICE_ROLE` discussions unrelated to avatars (neither file modified by PR-004)
- [x] `supabase/config.toml` `[functions.upload-avatar]` block removed (replaced by a 5-line comment explaining the removal — no executable wiring)

## Profile screen modification bounded check
- [x] `AccountScreen.tsx` swap is surgical: `AvatarUploadSection` import + mount removed; `ContactInfoSection` import + mount added in the same slot; User ID / Role / ADMIN? / Sign Out rows preserved
- [x] `accountApi.fetchOwnProfile` SELECT narrowed to `id, display_name, role, created_at` (4 avatar columns + AvatarModerationStatus coercion removed)
- [x] `accountApi.buildProfileUpdatePayload` allowlist preserved: `display_name` only (no email/role/id surface — `contactDoctrine.test.ts` asserts)
- [x] `types.ts` removes `AvatarModerationStatus` type and the four avatar fields from `UserProfile` (asserted by `contactDoctrine.test.ts`)

## InitialsAvatar purely presentational
- [x] No data hook import; no realtime subscription; no side effects (verified by reading `src/features/account/InitialsAvatar.tsx`)
- [x] Accepts `displayName` + `seed` (userId) + optional `size` + optional `highContrast`; renders initials in a colored circle
- [x] Color hash deterministic on `seed ?? displayName ?? '?'` — stable across name changes when userId is passed (design Q5 requirement met or exceeded; fallback chain is a strict improvement)
- [x] Fixed `#f8fafc` (off-white) text color over 8 hand-picked dark backgrounds — luminance guard test (`initialsAvatar.test.ts`) enforces ≥4.5:1 against `#f8fafc`
- [x] Empty/null/whitespace name falls back to `?` (multiple edge-case tests)

## package.json change
- [x] `expo-image-picker` removed (line 81 of prior file gone); zero other dependency changes; `package-lock.json` updated accordingly

## Read-only API boundary verification
- [x] `git diff main..HEAD --stat -- <protected dirs>` returns empty (zero changes in `src/features/auth/`, `src/features/session/`, `src/features/metadata/`, `src/features/concessions/`, `src/features/invites/`, `src/features/notifications/`, `src/features/debates/`, `src/features/evidence/`, all 5 protected Edge Function dirs, `.claude/agents/`, `.claude/scripts/`)

## OPS-002/003 regression
- [x] `npx jest __tests__/spawnCardBranchName.test.ts` → 13/13 passing (EC-1..EC-4 markers and ordering all intact)

## Issue body side-task verification
- [x] `gh issue view 26 --json body` shows a new "## Pivot from PR-003 avatar upload" section prepended before the original Goal + Acceptance criteria
- [x] Goal + Acceptance criteria preserved verbatim below the pivot note
- [x] Effort label visible: "m (revised from l post-pivot)"

## Test coverage
- [x] New public functions have unit tests: `validateEmail` (8+ cases incl. RFC 5321 cap), `messageForContactError` (every code), `requestEmailChange` (every error mapping + every short-circuit)
- [x] User-facing strings have ban-list assertion (`contactDoctrine.test.ts` 13 banned tokens across 5 source files)
- [x] Edge cases from design §12 have tests (CJK, RTL, emoji surrogate pairs, whitespace, null, very-long, single-char, 3+ word names; same-as-current case-insensitive; rate-limit / already-used / network-error mappings)
- [x] Accessibility assertions present (44px + role + label + state + liveRegion in `contactInfoSection.test.tsx`)

## Suggestions (non-blocking)
1. The launch prompt flagged the stale-worktree-claimed-branch incident as
   "worth a known-blockers entry for OPS-004 candidacy." This was NOT
   added to `docs/core/known-blockers.md`. Recommend the operator (or a
   future OPS-00x design pass) capture the `--ignore-other-worktrees`
   recovery pattern there so future implementer/reviewer pairs have a
   documented path. Not a blocker — purely procedural-memory hygiene.
2. The current-status entry for PR-004 sits ABOVE the PR-003 entry rather
   than archiving PR-003. This matches existing repo cadence (each card
   stacks above prior entries), but future cards could collapse closed
   epics into a "Closed" subsection if the running comment list grows
   unwieldy. Not a blocker.

## Operator next steps
- Push the branch: `git push -u origin feat/PR-004-contact-information-update`
- Open PR: `gh pr create --title "PR-004: contact info update + avatar pivot to initials" --body-file docs/reviews/PR-004.md`
- Deploy after merge (per design §17):
  - Phase 1: `npx supabase db push --linked` (applies migration 17)
  - Phase 2: `npx supabase functions delete upload-avatar --linked` (removes deployed Edge Function; filesystem source already gone)
  - Phase 3 (optional): delete the empty `profile-avatars` bucket via the Supabase dashboard (storage_admin ownership — not deletable from a migration)
  - Phase 4: smoke per design §17 (change email → verify pending badge → click verify link → confirm new email displays)
