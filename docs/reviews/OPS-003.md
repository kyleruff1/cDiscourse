# OPS-003 — Review

**Verdict:** Approve
**Reviewer agent run:** 2026-05-24
**Branch:** feat/OPS-003-worktree-cleanup-procedure-hardening-loc
**Design:** docs/designs/OPS-003.md

## Summary

OPS-003 strengthens the OPS-002 "Post-merge worktree cleanup (operator step)"
procedure in `.claude/agents/roadmap-reviewer.md` with four named edge-case
handlers (EC-1 lock force, EC-2 Windows long-path, EC-3 filesystem orphan
sweep, EC-4 periodic branch-ref cleanup) captured from the 2026-05-24 cleanup
session that removed 96 worktrees + 218 branches + 8 filesystem orphans and
recovered 8.93 GB. Each handler is bracketed by a stable
`<!-- OPS-003: EC-N handler -->` HTML comment marker and protected by a new
5-test Group 3 in `__tests__/spawnCardBranchName.test.ts`. The OPS-002
implementer-charter rename step, both spawn-card scripts, and the designer
charter are byte-identical (verified by per-file `git diff` returning empty).
This card is also the first end-to-end validation of OPS-002's rename step —
the worktree branch was created as `worktree-agent-<hash>` and renamed in
Phase-order step 1 to `feat/OPS-003-worktree-cleanup-procedure-hardening-loc`
with no friction. Pure documentation + charter + test change; zero production
code touched.

## Verification

| Check | Result |
|---|---|
| typecheck | pass |
| lint | pass |
| test | 10487 → 10492 tests / 418 → 418 suites (+5 / +0) |
| secret scan | clean (one literal hit at design doc L1210 is the acceptance-criteria scan string being quoted, not a key) |
| doctrine scan | clean (only meta-references to the ban-list itself; no live truth-label usage; `correct` appears once in known-blockers L243 as procedural prose — "OPS-002 was correct for the in-use case" — not a truth label) |
| OPS-002 byte-identity (4 files) | pass (per-file `git diff` returns 0 lines for `roadmap-implementer.md`, `spawn-card.ps1`, `spawn-card.sh`, `roadmap-designer.md`) |
| Production code untouched | pass (no path under `src/`, `app/`, `supabase/`, `scripts/`) |

## Design conformance

- [x] All design file-changes are present (4 files: reviewer charter, known-blockers, test file, current-status; matches design §5 + §6)
- [x] No undocumented file-changes (footprint is exactly the four files named in design §5; the only additional file in the diff is the design doc itself, committed on the designer pass at `7fe41cc`)
- [x] Data model matches design (n/a — no data model)
- [x] API contracts match design (n/a — no API)

## Doctrine self-check

- [x] No truth/winner/loser language in user-facing strings (none added; n/a — no UI)
- [x] Score never blocks posting (n/a — no scoring surface touched)
- [x] No service-role in client code (n/a — no client code touched; verified `git diff main..HEAD --name-only | grep -E '^(src/|app/)'` empty)
- [x] No direct insert into public.arguments (n/a — no DB write)
- [x] No AI calls in production app paths (n/a — no AI surface; pure ops documentation)
- [x] Plain language only (no raw internal codes in UI strings) (n/a — operator-facing charter text only, not user-facing UI)
- [x] Epic-specific doctrine — **cdiscourse-doctrine §6 (secrets policy)** respected: the charter's PowerShell snippets reference no secrets, no env vars, no API keys; the `RESEND_API_KEY` mentioned in known-blockers is in a quoted procedural sentence from a prior stage's lesson, not added by this card. **cdiscourse-doctrine §8 (Supabase conventions)** respected: no migration, no RLS, no `public.arguments` modification. **test-discipline (required coverage)** respected: charter-as-code contract is protected by file-scan tests with stable marker-based assertions (not wording-coupled), matching the OPS-002 pattern.

## OPS-003-specific verifications

**All 4 EC handler markers present in correct sequence** (verified by file scan over `.claude/agents/roadmap-reviewer.md`):
- EC-1 marker at L207 with signal phrase `git worktree remove -f -f` at L222
- EC-2 marker at L261 with signal phrases `\\?\` (L277), `Remove-Item` (L277), `Filename too long` (L269 + L287)
- EC-3 marker at L289 with signal phrases `Compare-Object` (L315) + `filesystem orphan` (L298 + L340)
- EC-4 marker at L342 with signal phrases `git branch --list` (L364), `'feat/*'` + `'worktree-agent-*'` (L360)
- Ordered presence test passes (ec1 < ec2 < ec3 < ec4)

**Lesson-capture entry in known-blockers.md** (verified at L151–L275):
- Title matches design §3 verbatim: `### ✅ Worktree Cleanup Procedure Hardened — Lock Force, Long Paths, Filesystem Orphans, Branch Refs (OPS-003)`
- Format follows OPS-001 + OPS-002 (Incident / Root cause / Resolution / Lesson)
- Cites empirical evidence: 96 worktrees (73 cat-3 + 16 cat-4 + 7 cat-5), 218 branches, 8 filesystem orphans, 8.93 GB recovered (48.56 GB → 57.49 GB free)
- Placed correctly under "RESOLVED — Previously Blocking" immediately after the OPS-002 entry
- `_Last updated:_` line at L3 bumped to `2026-05-24 (Stage 6.4 / OPS-003)` per design §3
- Three-rule lesson block present (force-flag granularity, cross-platform path limits, admin-state vs filesystem-state divergence)
- Closing paragraph correctly extends the OPS-* series narrative to include OPS-003

**Regression test (extended `__tests__/spawnCardBranchName.test.ts`)** (verified by running `npm run test -- spawnCardBranchName`):
- Group 3 "OPS-003 worktree cleanup procedure — charter contract (file scan)" appended after Groups 1+2; 5 new tests total
- Each EC-N test asserts marker presence + signal-phrase presence; ordered-presence test pins the natural execution order (blocking errors before accumulation sweeps)
- All 8 original OPS-002 tests pass byte-identical (verified by the test output: same 8 OPS-002 test names produce same pass results)
- Marker-based scan is correctly decoupled from prose wording — single-token signal phrases (`-f -f`, `\\?\`, `Compare-Object`, `git branch --list`) resist clarity-edit drift while catching deletion or re-ordering
- All 13 tests pass in 0.794s

**No OPS-002 work modified** (verified by per-file `git diff main..HEAD --` returning 0 lines):
- `.claude/agents/roadmap-implementer.md` — empty diff
- `.claude/scripts/spawn-card.ps1` — empty diff
- `.claude/scripts/spawn-card.sh` — empty diff
- `.claude/agents/roadmap-designer.md` — empty diff

**No production code modified** (verified by `git diff main..HEAD --name-only | grep -E '^(src/|app/|supabase/|scripts/)'` returning empty):
- Diff touches only `.claude/agents/`, `__tests__/`, and `docs/` paths
- Five files total: `.claude/agents/roadmap-reviewer.md`, `__tests__/spawnCardBranchName.test.ts`, `docs/core/current-status.md`, `docs/core/known-blockers.md`, `docs/designs/OPS-003.md`

**Internal consistency** — the strengthened cleanup procedure preserves OPS-002's "operator step, not reviewer subagent's" framing (line 196 section heading unchanged + opening paragraph at L197–L203 retains the framing verbatim). The per-card steps 1–4 keep the OPS-002 numbering and intent; only step 2's `--force` → `-f -f` is changed (EC-1 fix), with inline rationale citing the 96/96 failure rate. The existing bulk-cleanup-of-historical-orphans block is preserved and updated to use `-f -f`. No conflict with OPS-001 (migration verification) — OPS-001 is a separate section earlier in the charter and is untouched. The review-doc template bullet at L186 (`Post-merge worktree cleanup (commands in roadmap-reviewer.md § "Post-merge worktree cleanup (operator step)")`) is byte-identical to OPS-002's version.

**OPS-002 rename step end-to-end validation** — this card is the first to exercise the OPS-002 charter step 1 (`git branch -m feat/<code>-<slug>`) end-to-end. The worktree's branch is named `feat/OPS-003-worktree-cleanup-procedure-hardening-loc` (rename complete, `worktree-agent-*` auto-name discarded), the 3 implementer commits + 1 designer commit + this review commit all land on the renamed branch, and no refspec push workaround will be required at PR time. OPS-002's first real-world test passed clean.

## Test coverage

- [x] New public functions have unit tests (n/a — no new public functions; the charter is "code-as-contract" and is covered by the 5 file-scan tests)
- [x] User-facing strings have ban-list assertion (n/a — no user-facing strings; the doctrine ban-list scan over the diff is clean)
- [x] Edge cases from design § "Edge cases" have tests (the four EC handlers ARE the edge-case catalog; each has a dedicated test in Group 3)
- [x] Accessibility assertions present (n/a — no UI card)

## Blockers

None.

## Suggestions (non-blocking)

1. **Cadence heuristic for EC-4** (design §7 already flags this as discretionary). The charter recommends "after every 10–20 cards merge, or whenever `git branch --list <patterns> | wc -l` exceeds 50". The 50-branch threshold is a reasonable midpoint between OPS-002's per-card rhythm and the 218-branch big-bang. Operator may want to tune after the next 10 cards to calibrate against actual accumulation rate; no charter update needed yet.

2. **Future colon-vs-dash slug fix (informally "OPS-004", out of OPS-003 scope per design §6).** The OPS-002 Group 1 Test 3 still pins the cosmetic `feat/OPS-002-ops-002-...` double-prefix slug behaviour for modern colon-separated titles. OPS-003 correctly does not touch this. Whenever the colon-vs-dash fix lands, the pinned-behaviour test will need a one-line update; OPS-003's Group 3 is independent and will not be affected.

3. **Bulk-cleanup pass continues to be operator-judgement-gated** (preserved from OPS-002). The post-OPS-003 bulk block at L387–L412 is informational, not a script. If a future session accumulates another large worktree backlog despite OPS-003 being live, consider promoting the bulk pass to a versioned `.claude/scripts/cleanup-worktrees.ps1` so the procedure is committed-history rather than just charter-text. Not needed for OPS-003; flagged only for future planning.

## Operator next steps

- Push the branch: `git push -u origin feat/OPS-003-worktree-cleanup-procedure-hardening-loc`
- Open PR: `gh pr create --title "OPS-003: Worktree cleanup procedure hardening (lock force, long paths, filesystem orphans, branch refs)" --body-file docs/reviews/OPS-003.md`
- Deploy steps: **none** — pure documentation + charter + test change. After merge, the strengthened cleanup procedure is live for the operator's next post-merge cleanup invocation. The OPS-002 implementer-charter rename step continues to apply to every subsequent implementer subagent (preserved unchanged).
- Post-merge worktree cleanup: commands in `.claude/agents/roadmap-reviewer.md` § "Post-merge worktree cleanup (operator step)". Note: this is the first post-merge cleanup that will be performed using the strengthened OPS-003 procedure — a useful empirical second-pass validation.
