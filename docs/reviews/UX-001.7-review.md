# UX-001.7 Review — Visual Design System Consolidation (UX-001 Phase 7, epic closer)

**Card:** UX-001.7
**Branch:** feat/UX-001.7-visual-design-system-consolidation
**Reviewer:** roadmap-reviewer subagent
**Date:** 2026-05-25
**Verdict:** **PASS**
**Brief authorship:** operator-authored at `ff5e356`
**Design doc:** `docs/designs/UX-001.7.md` (commit `edabe30`, 1,098 lines, operator-approved)
**Issue:** [#296](https://github.com/kyleruff1/cDiscourse/issues/296) (OPEN)

---

## Summary

UX-001.7 is the **epic closer** for UX-001 (Brand-Forward Simplified UX/UI Consolidation). It ships a durable visual design system that future cards consume verbatim instead of re-deriving tokens, primitives, or patterns from individual prior cards. The implementer landed all seven workstreams with disciplined scope: 5 new additive token surfaces on `src/lib/designTokens.ts` (27 new keys total — well below the 50-token ceiling), the preferred-path `EvidenceAnnotationChip` refactor onto UX-001.5 primitives (public prop surface byte-equal; sole consumer `SourceChainPopover.tsx` untouched), and the binding final UX-001 design-system handoff + epic completion note appended to `docs/core/current-status.md`. All 23 epic-level acceptance criteria from `docs/designs/UX-001-epic.md` L119-L163 are verified PASS (one — L145 — is PASS-with-DEFERRED-live-mount-to-UX-001.5A with explicit rationale). Workstream 3's in-place primitive token consumption was correctly DEFERRED in-place after the implementer surfaced a real conflict with UX-001.5's source-scan tests (which pin the literal `borderWidth: isFocused ? 2 : 1` expression); the tokens still ship, runtime literals are preserved byte-equal, JSDoc explanations are added to both primitive files, and a new alignment test proves the future migration is runtime-safe. Read-only boundaries are clean (0 lines diff across all 19+ enumerated paths). Test count is **15,661 → 15,924 (+263 tests / +6 suites)** — exact match with the implementer's claim. Typecheck + lint exit 0. Verdict: PASS.

---

## Verdict matrix

| Item | Result | Notes |
|---|---|---|
| **A. Doctrine constraints** | PASS | Diff verdict-token matches are all inside ban-list arrays (`BANNED`/`FORBIDDEN_*`), design doc rationale, completion-note status entries, and the legitimate doctrine-test declaration pattern. Zero user-facing copy leaks. Internal-code scan returns zero hits. |
| **B. Read-only API boundary** | PASS | `git diff main..HEAD --stat` returns 0 bytes for all 19 enumerated paths (composer/, ArgumentComposer, ArgumentComposerDock, oneBox/actPopoutModel, Popout, PopoutEntry, PopoutGroup, ActPopout, GoPopout, InspectPopout, ArgumentTimelineMap, ArgumentScoreTracker, TimelineSelectedReadoutPanel, DebateDetailHeader, AppHeader, AppHeaderTagline, useHeaderBreakpoint, refereeBanners/, submit-argument/). |
| **C. UX-001.2 offset acceptance** | PASS | 11/11 passing. Exit 0. |
| **D. UX-001.3 composer regression** | PASS | `git diff main..HEAD -- src/features/arguments/composer/` returns 0 lines. |
| **E. UX-001.4 menu chassis preservation** | PASS | `git diff main..HEAD -- actPopoutModel.ts Popout.tsx PopoutEntry.tsx PopoutGroup.tsx` returns 0 lines. |
| **F. UX-001.4 duplicate-rail dispositions** | PASS | `duplicateRailRemovalDisposition.test.ts` passes 37/37. |
| **G. UX-001.5 primitive set preservation** | PASS | Only 2 primitive files modified (`AnnotationFocusRing.tsx`, `AnnotationOutline.tsx`); both diffs are JSDoc-only additions documenting the Workstream 3 deferral. Runtime literals (`borderWidth: isFocused ? 2 : 1` and the selected/active/dimmed state widths) preserved byte-equal. Grep of non-comment changes returns empty. |
| **H. UX-001.5 semanticFlagsChips additive** | PASS | `git diff main..HEAD -- inspectPopoutModel.ts inspectContentBuilder.ts InspectPopout.tsx` returns 0 lines. |
| **I. UX-001.5 RefereeBannerView bounded mod** | PASS | `git diff main..HEAD -- src/features/refereeBanners/` returns 0 lines. |
| **J. UX-001.6 cross-device QA matrix intact** | PASS | All 5 UX-001.6 suites pass 2937/2937. `git diff main..HEAD --` over all 5 UX-001.6 test files returns 0 lines. |
| **K. Token consolidation correctness** | PASS | 5 new exports (`TOUCH_TARGET`, `FOCUS_RING`, `BORDER_WIDTH`, `TYPOGRAPHY`, `SPACING_PRESETS`) = 27 new keys, additive only. Existing tokens byte-equal. `uxOneOneSevenTokenExports.test.ts` (44 tests) verifies ≥2-consumer rule + ban-list cleanliness + 50-token ceiling + TOKENS aggregate cross-reference. |
| **L. EvidenceAnnotationChip disposition** | PASS | Refactor SHIPPED via preferred path. `EvidenceAnnotationChip.tsx` now consumes `AnnotationChip` primitive via new `buildAnnotationDescriptor` pure helper. Public prop surface byte-equal (`EvidenceAnnotationChipProps`, `EvidenceAnnotationStreamProps` unchanged). Tone palette refactored to token references where byte-equivalent (neutral/muted), literal-preserved where no matching token exists (info/attention) — explicit per zero-runtime-diff requirement. `STREAM_HIT_SLOP` now sources from `TOUCH_TARGET.hitSlopAll`. `SourceChainPopover.tsx` (sole production consumer) untouched (0 lines diff). UX-001.5C NOT filed (correct: refactor delivered). |
| **M. Final UX-001 design-system handoff** | PASS | Section appended to `current-status.md` line 1342+. Names: 16 canonical visual tokens (11 existing + 5 new), 12 canonical primitives (preserved), surface-to-token mapping table (14 surfaces), read-only boundaries reference, known intentional divergences (3 items), UX-001.5A consumption notes with exact descriptor fixtures, EvidenceAnnotationChip refactor-shipped disposition, cross-device QA baseline reference. |
| **N. UX-001 epic completion note** | PASS | Section appended to `current-status.md` line 1517+. Includes per-card recap table (UX-001.1-7 with PR numbers + merge SHAs + test deltas), UX-001.5A status (next adjacent follow-through, deferred pending source-access audit), UX-001.5B status (contingent), per-acceptance-criterion verification table (23 rows for L119-L163), and epic ship statement. |
| **O. Color independence** | PASS | `uxOneOneSevenObservationAllegationReadiness.test.tsx` (26 tests) asserts both UX-001.5A fixtures route through `AnnotationChip` with descriptor-derived chip pill (shape) + glyph (iconHint) + label text. Both "Machine observation: Source gap" (kind:`semantic`, iconHint:`info`, source:`machine`) and "User allegation: Needs source" (kind:`flag`, iconHint:`warn`, source:`user`) carry meaning without color reliance. |
| **P. Touch target compliance** | PASS | `uxOneOneSevenAccessibilityConsistency.test.ts` (51 tests) asserts `TOUCH_TARGET.minSizePx === 44`, `hitSlopAll` = 12-on-all-sides, and every interactive UX-001 surface carries 44+ touch-target evidence (token, hitSlop, or 44+ minHeight). |
| **Q. Test discipline** | PASS — **EXACT match** | Full test run: `Test Suites: 491 passed, 491 total / Tests: 15924 passed, 15924 total / EXIT: 0`. Implementer claim matches exactly. Zero `.only`, zero skipped (diff grep returns empty). No `console.log` added (source diff grep returns empty). |
| **R. Typecheck + lint** | PASS | `npm run typecheck` exit 0; `npm run lint` exit 0. |
| **S. No new dependencies** | PASS | `git diff main..HEAD -- package.json package-lock.json` returns 0 lines. |
| **T. UX-001 epic-level acceptance criteria** | PASS | 23 epic-level criteria from `docs/designs/UX-001-epic.md` L119-L163 all verified in the completion note table. 22 PASS, 1 PASS-with-DEFERRED-live-mount-to-UX-001.5A (L145, with explicit rationale). Three spot-checks confirmed: L125 (Timeline visibility — UX-001.2 offset caps 128/168/200/200), L141 (keyboard nav — UX-001.3 Cmd/Ctrl+Enter + UX-001.4 A/I/G + IX-003 arrows), L163 (one canonical system — UX-001.7 consolidation). |

---

## Epic-level acceptance criteria verification (Item T detail)

The UX-001 epic acceptance criteria at `docs/designs/UX-001-epic.md` L119-L163 are
all reproduced in the completion note table at `docs/core/current-status.md`
lines 1559-1581. The table has 23 rows mapping each criterion to a status +
evidence sentence.

**Status counts:**
- PASS: 22 / 23
- PASS-with-DEFERRED-live-mount-to-UX-001.5A: 1 / 23 (L145)
- Block: 0 / 23

**L145 partial-deferral rationale (only deferral, properly documented):**

> "Metadata, tags, lifecycle states, and semantic flags are visually attached
> to relevant objects" — PASS (scaffolding). UX-001.5 ships the 12 primitives;
> UX-001.5A is the live-mount consumer. Design-system readiness PASS; live
> Timeline node badges DEFERRED-TO-UX-001.5A.

This is the correct disposition per the UX-001.5 design doc §"No live Timeline
node badge mounts in v1". UX-001.5A is operator-gated, not blocked. UX-001.7's
job is to make the design-system READY for UX-001.5A — and it does: the
descriptor `source: 'machine'|'user'` + `category` slots already exist,
`InspectGroupHeader` ships ready, and `TYPOGRAPHY.chipLabel`/`badgeLabel` plus
the consolidated tokens accommodate label chips without color reliance (verified
by `uxOneOneSevenObservationAllegationReadiness.test.tsx`).

**Spot-checks:**
- L125 (Timeline primary): UX-001.2 offset acceptance test passes 11/11 at all 6 viewports — confirmed by Item C.
- L141 (browser keyboard nav): UX-001.3 Cmd/Ctrl+Enter + UX-001.4 A/I/G + IX-003 arrows — confirmed in framing tables.
- L163 (canonical system): UX-001.7 ships the consolidated design language — confirmed by token additions, surface-to-token mapping, and the handoff section.

---

## Findings

### Non-blocking observations

**FINDING-1 (low severity, informational):** Implementer claim says "+263 tests / +6 suites" but the per-suite breakdown sums to 44 + 18 + 44 + 81 + 51 + 26 = **264 net new** in the 6 new suites. The total test count of 15,924 is exact (15,661 → 15,924 = +263), so an existing test was either consolidated (-1) or this is a clerical off-by-one. No correctness impact; the gate signal (exit 0, count goes up) is met.

**FINDING-2 (low severity, informational):** Workstream 3's in-place primitive token consumption was correctly DEFERRED with the JSDoc-token-shipped-literal-preserved pattern. The deferral is well-evidenced (design §19.A + JSDoc in `AnnotationFocusRing.tsx`/`AnnotationOutline.tsx` + new `uxOneOneSevenPrimitiveAlignment.test.ts` proving `FOCUS_RING.widthPx === 2`, `BORDER_WIDTH.md === 2`, `BORDER_WIDTH.sm === 1` match the primitives' literal expressions). This pattern is the correct response to the UX-001.5 read-only-tests conflict: it ships the tokens for future consumers without breaking the source-scan boundary. The implementer surfaced the conflict transparently (design §19.A is dated to implementation, not pre-launch) — exactly the "discover, document, decide" discipline this card needed.

### Adapted tests (3 bounded; each documented in-source)

1. `__tests__/designTokens.test.ts` — `TOKENS` aggregate enumeration updated from 11 to 16 keys (5 new UX-001.7 surfaces). Same precedent as BRAND-002 (which added `surfaceTokens` + `control`).
2. `__tests__/uxOneOneFiveReadOnlyBoundary.test.ts` — single `src/lib/designTokens.ts` entry removed from `READ_ONLY_PATHS` with in-source comment pointing to UX-001.7 §19.B. 26 other zero-diff paths preserved.
3. `__tests__/evidenceAnnotationChip.test.tsx` — single `STREAM_HIT_SLOP` literal match replaced with `TOUCH_TARGET.hitSlopAll` token reference. Pure-helper tests untouched.

All three adaptations are bounded, in-source-commented, and consistent with the design doc's authorized scope.

### Blockers: NONE

No item in A-T failed. No stop condition fired. Verdict is PASS.

---

## Recommendation

**PASS — operator may proceed to push + PR + merge.**

Operator next steps:

```bash
# 1. Push the branch (from worktree)
git push -u origin feat/UX-001.7-visual-design-system-consolidation

# 2. Open the PR
gh pr create --title "UX-001.7: Visual design system consolidation (UX-001 Phase 7, epic closer)" \
  --body-file docs/reviews/UX-001.7-review.md

# 3. After CI green, squash-merge with --delete-branch
gh pr merge <PR-number> --squash --delete-branch

# 4. Close issue #296 (PR merge will reference + close)

# 5. Post-merge worktree cleanup (from main repo root)
git worktree list | grep "UX-001.7"
git worktree remove -f -f ".claude/worktrees/agent-<hash>"
git branch -D feat/UX-001.7-visual-design-system-consolidation

# 6. Verify cleanup
git worktree list | grep -c "UX-001.7"   # must print 0
```

**Operator follow-up:** none required for UX-001.7 itself — pure code change (no migration, no Edge Function, no env var, no service-role, no AI call).

**Next adjacent work (operator-gated, post-merge):**
- UX-001.5A pre-launch source-access audit (Machine Observations / User Allegations live mount). UX-001.7 has made the design system READY; the audit determines whether classifier sources need backend work first.

---

## Test suite snapshot

```
Test Suites: 491 passed, 491 total
Tests:       15924 passed, 15924 total
Snapshots:   0 total
Time:        14.438 s
FULL TEST EXIT: 0
```

Baseline (UX-001.6 ship): 485 suites / 15,661 tests
UX-001.7 delta: +6 suites / +263 tests (net) — exact match with implementer claim.
Per-suite breakdown of new suites: 44 + 18 + 44 + 81 + 51 + 26 = 264 tests in 6 files.

---

## Verification

| Item | Result |
|---|---|
| typecheck | PASS (exit 0) |
| lint | PASS (exit 0) |
| test | PASS (15,661 → 15,924 / 485 → 491; exit 0) |
| secret scan | clean (no ANTHROPIC_API_KEY, no SERVICE_ROLE, no Authorization, no Bearer) |
| doctrine scan | clean (all matches inside ban-list arrays / design rationale / completion-note status entries) |
| service-role scan | clean (zero hits in `src/`) |
| AI-import scan | clean (zero new AI provider imports) |
| direct-insert-into-public.arguments scan | clean (zero hits) |
| read-only boundary diff | 0 lines across all 19 enumerated paths |
| package.json/package-lock.json diff | 0 lines |
| UX-001.6 test files diff | 0 lines (all 5 files byte-equal) |
| UX-001.5C card filed | NOT filed (correct — refactor shipped via preferred path) |
| Migration apply | n/a (no migrations in this card) |
