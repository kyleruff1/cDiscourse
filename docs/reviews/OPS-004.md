# OPS-004 — Review

**Verdict:** Approve
**Reviewer agent run:** 2026-05-24
**Branch:** feat/OPS-004-pipeline-operational-hygiene-sweep-drop
**Design:** docs/designs/OPS-004.md

## Summary

OPS-004 ships exactly what its design specifies: four coordinated
strengthening deliverables in the process layer (OPS-001 reviewer template
gets a Class 3 sub-check (e) for `DROP COLUMN` ordering and a Class 4
sub-check (f) for storage schema `COMMENT` ownership; OPS-002 implementer
charter gets a `--ignore-other-worktrees` recovery sequence appended to
the rename step; both spawn-card scripts get a 1-character regex change
to accept colon-separated issue titles). Zero production code touched.
The 5th-end-to-end OPS-002 stale-worktree-claim case arose on this card
during worktree setup and was resolved with the exact sequence OPS-004
codifies — the meta-irony is genuinely validated. Three new contract
tests in `__tests__/spawnCardBranchName.test.ts` Group 4 lock the
extensions in place; both regression-introduction smoke tests confirmed
the contract scans catch reversal of the strengthened text.

## Verification

- typecheck: **pass** (`tsc --noEmit` exit 0)
- lint: **pass** (`eslint . --max-warnings 0` exit 0)
- test: **10731 → 10734 tests / 427 → 427 suites** (+3 in
  `spawnCardBranchName.test.ts` Group 4; matches design §5.3 exactly).
  1 pre-existing failure in `__tests__/deprecateAvatarMigration.test.ts:82`
  is orphan debt from PR-004 hotfix `0756d2f` (the statement-order swap
  invalidated the obsolete inequality the test still pins). Verified
  `git log --oneline -- __tests__/deprecateAvatarMigration.test.ts`
  returns only `7b2e994 PR-004` — OPS-004's diff does not touch the
  file. Out of scope; warrants a follow-up issue against PR-004 cleanup.
- secret scan: **clean** (both `ANTHROPIC_API_KEY` mentions are inside
  the design doc's self-citation of the scan-regex pattern itself, not
  actual secret references)
- doctrine scan: **clean** (ban-list tokens appear only inside the
  design doc's `cdiscourse-doctrine §1` self-check enumeration)

## Design conformance

- [x] All design file-changes are present (7 files touched: the 6 named
      in design §"File changes" plus `docs/core/current-status.md`
      per criterion 15 — design has minor internal inconsistency
      between criteria 13 and 15; implementer correctly resolved by
      following the explicit charter-step-7 instruction in criterion 15)
- [x] No undocumented file-changes (no `src/`, no `app/`, no
      `supabase/migrations/`, no `supabase/functions/`, no
      `.claude/skills/`, no `package.json`, no
      `.claude/agents/roadmap-designer.md`)
- [x] No data model (none in design)
- [x] No API contracts (none in design)

### §1 — OPS-001 reviewer template extension
- [x] Class 3 cell gains marker `(e)` verbatim before `**Resolution:**`
      (cites PR-004 `SQLSTATE 2BP01`, grep instruction, dependent-object
      enumeration matches design §1.2 exactly)
- [x] Class 4 cell gains marker `(f)` verbatim before `**Resolution:**`
      (cites PR-003 `SQLSTATE 42501`, 3 ranked resolution options i/ii/iii
      matches design §1.3 exactly: omit / DO block / not-available)
- [x] No other table edit (classes 1, 2, the table header, and the
      4-row structure are byte-identical pre-OPS-004)

### §2 — OPS-002 charter rename step extension
- [x] Lines 49-53 area replaced with ~36-line strengthened sequence
- [x] Sequence includes all required steps in order:
      `git worktree list | grep` → `git -C <other> status -sb` → 
      `git switch --ignore-other-worktrees` → `git branch -d <auto>`
- [x] STOP-and-surface escape preserved for non-clean holder cases
      (uncommitted work, active session, divergent HEAD)
- [x] Rename heading (line 32) + sanity-check block (lines 33-47) +
      `**Verify clean baseline.**` step heading byte-identical

### §3 — spawn-card regex correction
- [x] `.claude/scripts/spawn-card.ps1` line 74 regex changed `-` → `[-:]`
      (PowerShell `^$Code\s*[-:]\s*`); single-character delta verified
      via `git diff --shortstat`
- [x] `.claude/scripts/spawn-card.sh` line 112 regex changed `-` → `[-:]`
      (sed `^${CODE}[[:space:]]*[-:][[:space:]]*`); single-character delta
- [x] No other edits to either script (line-by-line diff inspected)

### §4 — Consolidated known-blockers entry
- [x] New entry titled "✅ OPS-004 Pipeline Operational Hygiene Sweep
      (2026-05-25)" placed between OPS-002 stale-worktree entry (ends
      line 425) and `## ACTIVE BLOCKERS` heading
- [x] Names all 4 signals (PR-004 DROP COLUMN, PR-003 storage COMMENT,
      OPS-002 stale-worktree, OPS-002 §7 colon-vs-dash)
- [x] Names all 4 deliverables matching §1/§2/§3 of design
- [x] Meta-lesson articulated (textual review necessary but not
      sufficient; some gaps require tooling enforcement)
- [x] `_Last updated:_` bumped to `2026-05-24 (Stage 6.4 / OPS-004)`

### §5 — Regression test updates
- [x] Group 1 Test 3 expectation updated to assert corrected
      non-duplicated slug (`feat/OPS-002-pipeline-operational-hygiene-spawn-card`)
- [x] Test name renamed to "strips the colon separator for
      OPS-004-corrected behaviour (modern titles)"
- [x] Helper at line 33-35 mirrors corrected PS1 regex
      (`'^' + code + '\\s*[-:]\\s*'`)
- [x] New Group 4 added with exactly 3 new tests (Class 3 sub-check
      scan, Class 4 sub-check scan, rename-step recovery scan with
      ordering assertion using string-index comparison)
- [x] All 16 tests pass (13 existing + 3 new)
- [x] Documented design §5.1 → implementer-applied expectation
      discrepancy (`-bra` vs `-card`) recorded in design's appended
      "Implementer note (2026-05-24)" addendum at lines 1111-1147 of
      `docs/designs/OPS-004.md`; the design's arithmetic miscounted the
      40-char truncation. Implementer correctly applied the actually
      computed slug and added a clarifying in-test comment pointing at
      the implementer note. Substantive intent preserved (assert
      non-duplicated form).

### Regression-introduction smoke tests (per launch prompt)

- [x] **Smoke test 1 (spawn-card colon fix):** Reverted PS1 line 74
      regex from `[-:]` back to `-` AND the test helper line 35 from
      `[-:]` back to `-`. Ran `npx jest spawnCardBranchName.test.ts` —
      Group 1 Test 3 (corrected expectation) correctly FAILED:
      "Expected: 'feat/OPS-002-pipeline-operational-hygiene-spawn-card',
      Received: 'feat/OPS-002-ops-002-pipeline-operational-hygiene-spa'".
      Both reverts undone; working tree restored to pre-smoke state.
- [x] **Smoke test 2 (OPS-001 Class 3 sub-check):** Removed the entire
      `(e) Any DROP COLUMN…SQLSTATE 2BP01…` marker text from Class 3.
      Ran the matching Group 4 test — correctly FAILED:
      "expect(reviewerCharter).toContain('every \`DROP COLUMN\` or
      \`DROP TABLE\`')" assertion thrown. Revert undone; full
      `spawnCardBranchName.test.ts` suite back to 16/16 green.

## Doctrine self-check

- [x] No truth/winner/loser language in user-facing strings (no UI
      strings added; the design's mention of the ban-list is its own
      §"Doctrine self-check" enumeration)
- [x] Score never blocks posting (N/A — no score path touched)
- [x] No service-role in client code (no client code touched)
- [x] No direct insert into `public.arguments` (no DB writes anywhere)
- [x] No AI calls in production app paths (no production paths
      touched; reviewer-template extensions are deterministic textual
      scans, charter rename-step extension is deterministic git
      commands, spawn-card regex change is a deterministic regex, the
      3 new tests are pure TS file reads)
- [x] Plain language only (no raw internal codes in UI strings —
      no UI strings)
- [x] Epic-specific doctrine — N/A. OPS-004 sits in the
      Operations/Pipeline-hygiene epic; the only "epic-specific"
      doctrine is the OPS-001 four-class table integrity rule (sub-check
      additions inside existing cells, no row count change, no class
      renumbering). Verified: Classes 1, 2, the table header, and the
      4-row count are all byte-identical pre-OPS-004; sub-checks (e) and
      (f) are appended inside the existing Class 3 and Class 4 cells
      before each `**Resolution:**` sentence per design §1.4–1.5.

## Test coverage

- [x] New "public functions" — N/A (no new functions; the 1-char regex
      change is covered by the modified Group 1 Test 3 expectation)
- [x] User-facing strings — N/A (no UI strings added; the existing
      ban-list test suites continue to pass over the untouched UI
      surface per design §"Test plan")
- [x] Edge cases from design §"Edge cases" — verified: existing
      `**Resolution:**` sentences are the unique anchors used for both
      Class 3 and Class 4 inserts; markers (e) and (f) are at the
      expected next-available positions; spawn-card `[-:]` character
      class preserves dash-separator behaviour (legacy QOL-040.3
      truncation test still passes); the OPS-004 worktree branch name
      uses the pre-fix duplicated-prefix form as documented in §"Edge
      cases" — branch name `feat/OPS-004-pipeline-operational-hygiene-sweep-drop`
      observed on the implementer's worktree
- [x] Accessibility — N/A (no UI changes)

## Suggestions (non-blocking)

1. **PR-004 orphan test debt.** `__tests__/deprecateAvatarMigration.test.ts:82`
   has been broken since PR-004 hotfix `0756d2f` swapped DROP COLUMN /
   DROP POLICY order. The test still pins the obsolete pre-hotfix
   inequality. Worth filing a follow-up PR-005 (or a follow-on OPS-005
   if the hotfix landed without test update) to either: update the
   expectation to the post-hotfix order, or document the explicit
   removal per `test-discipline`'s "unless a card explicitly removes
   tests with a documented reason" carve-out. Out of scope for OPS-004
   but operator should be aware before merging.

2. **Design internal inconsistency (criteria 13 vs 15).** Design §6
   criterion 13 says "diff touches **exactly 6 files**"; criterion 15
   names `docs/core/current-status.md` as a 7th. Implementer correctly
   followed the explicit instruction over the count. Minor wording
   polish for a hypothetical OPS-005 design: state file count as
   "6 implementer-owned files plus the standard charter-step-7
   `current-status.md` update".

3. **Bash twin symmetry uncovered (per design §3.4 / §5.4).** The bash
   `spawn-card.sh` change is verified by diff inspection but not by a
   regression test (the existing helper mirrors only the PS1 regex,
   per the OPS-002 §5.4 documented triple-parity exclusion). A future
   OPS card could file the symmetric assertion if the bash twin
   becomes load-bearing on a CI runner.

## Operator next steps

- Push the branch: `git push -u origin feat/OPS-004-pipeline-operational-hygiene-sweep-drop`
- Open PR: `gh pr create --title "OPS-004: Pipeline operational hygiene sweep — DROP COLUMN ordering + storage COMMENT ownership + stale-worktree recovery + spawn-card colon fix" --body-file docs/reviews/OPS-004.md`
- No deploy steps (pure documentation + charter + script + test card).
- After merge: the corrected spawn-card regex is live for the next
  card spawned with a colon-separated title — sanity-check that the
  printed branch name in the spawn-card prompt body matches the
  expected non-duplicated form before pasting into the next subagent.
- Consider filing a small follow-up to clean up the pre-existing
  `deprecateAvatarMigration.test.ts:82` orphan failure (PR-004 debt;
  not OPS-004's responsibility but visible in CI now).
