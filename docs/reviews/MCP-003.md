# MCP-003 — Review

**Verdict:** Approve
**Reviewer agent run:** 2026-05-20 (re-review)
**Branch:** feat/MCP-003-mcp-003-strategic-semantic-metadata-ledg
**Design:** docs/designs/MCP-003.md
**Issue:** https://github.com/kyleruff1/cDiscourse/issues/156

## Summary

This is a **re-review** following a prior "Changes requested" verdict. MCP-003 is a design-only card (501 lines, one file) specifying the deterministic "referee ledger" — layer 3 of the MCP semantic-referee architecture. The prior review (commit 4d1689d) found the design's logic and doctrine fully sound — all six issue scope items covered, doctrine self-check clean, MCP-001 `SemanticRefereePacket` / `scoreHints` contract referenced faithfully, test plan complete, secret + verdict-token scans clean — and explicitly stated it was **not a Block**. The only findings were four integration-map symbol-name inaccuracies (suggestions 1–4 in that review). Commit 8d99dff ("design(MCP-003): correct integration-map symbol names per review") applied exactly those four fixes: 7 insertions / 7 deletions in `docs/designs/MCP-003.md`, symbol-name corrections only, with no change to the design's logic or doctrine. All four fixes have been re-verified against the real source and are correct. The card is now **Approve**.

## Verification

This is a DESIGN-ONLY card — there is no production code, no tests, no migration, no Edge Function on the branch. `git diff main..HEAD --stat` = `docs/designs/MCP-003.md | 501 +` and `docs/reviews/MCP-003.md | 82 +` (the prior review doc, expected on the branch). The two design commits are 74e6f61 (original) + 8d99dff (the symbol-name fix). The verification battery (typecheck / lint / test) is not applicable to this card; it applies to the future MCP-003-impl card.

- typecheck: n/a (design-only, no code)
- lint: n/a (design-only, no code)
- test: n/a (design-only, no code) — test plan reviewed in §15; six test files defined, all mock/fixture-driven
- secret scan: clean — no `ANTHROPIC_API_KEY` / `XAI_API_KEY` / `X_BEARER_TOKEN` / `SUPABASE_SERVICE_ROLE_KEY` / `sb_secret_` / `sk-ant-` / JWT / Bearer shapes in the design doc
- doctrine scan: clean — every `winner`/`loser`/`liar`/`true`/`false`/etc. occurrence is inside a ban-list definition, an "absent field" assertion, a doctrine prohibition, or an out-of-scope clause; none is a UI string the ledger emits and none labels a person or claim

## Re-review — resolution of the four prior findings

Commit 8d99dff resolves all four prior suggestions. Each is re-verified against the real source:

1. **`gameCopy.ALL_INTERNAL_CODES` (did not exist) → `PLAIN_LANGUAGE_COPY` + `PlainLanguageKey`.** RESOLVED. `ALL_INTERNAL_CODES` now appears **0 times** in the design. The real surface is verified: `PLAIN_LANGUAGE_COPY` is the exported record at `src/features/arguments/gameCopy.ts:150`; `PlainLanguageKey` is `keyof typeof PLAIN_LANGUAGE_COPY` at line 255; `toPlainLanguage` is at line 263. The design's §11, §14, and §16 now correctly state the implementation card extends the `PLAIN_LANGUAGE_COPY` record (and therefore `PlainLanguageKey`).

2. **`reasonsFromEligibility` (file-private) → public `ineligibilityReasons` fields.** RESOLVED. Verified `reasonsFromEligibility` is an un-exported function inside `scoringEngine.ts` (line 57) — it is not in `src/features/pointStanding/index.ts`'s re-export set, so the ledger cannot import it. The public `ineligibilityReasons: string[]` fields exist on `ChallengeGradingResult` (`types.ts:217`) and `RepairGradingResult` (`types.ts:228`). §7.1 now explicitly states "`reasonsFromEligibility` itself is file-private to `scoringEngine.ts` and is not imported"; §9.3's `LedgerResult.ineligibilityReasons` JSDoc now sources the strings from the public grading-result fields.

3. **`AntiAmplificationResult` / `RepairGradingOptions` mis-attributed to `types.ts`.** RESOLVED. Verified `AntiAmplificationResult` is exported from `antiAmplification.ts:45` and `RepairGradingOptions` from `scoringEngine.ts:164` — neither is in `types.ts`. §9.1's types row now reads "all ten verified exported from `types.ts`" and adds the explicit note that `AntiAmplificationResult` / `RepairGradingOptions` "live in `antiAmplification.ts` / `scoringEngine.ts` respectively … not `types.ts`."

4. **`src/features/pointStanding/index.ts` does not re-export `antiAmplification`.** RESOLVED. Verified `index.ts` re-exports exactly `types`, `mixedClassWeights`, `concessionEffects`, `eligibility`, `scoringEngine` — and not `antiAmplification`. §14 now carries a precise note: the implementation card reaches `applyAntiAmplification` / `amplificationContextFromAnnotationFields` / `AntiAmplificationResult` by a deep import from `src/features/pointStanding/antiAmplification`, or adds a one-line `export * from './antiAmplification'` to the economy's `index.ts`.

No logic or doctrine changed between 74e6f61 and 8d99dff — the diff is 7 lines, all symbol-name precision. Nothing else regressed.

## Design conformance

- [x] All design file-changes are present — the card delivers exactly one file (`docs/designs/MCP-003.md`); §11 correctly scopes all code/tests to a future MCP-003-impl card
- [x] No undocumented file-changes — diff is the single design doc plus this review doc
- [x] Data model matches design — §10 correctly states no DB table / no migration; the future model is pure-TS, in-memory like `IssueDebtLedger`
- [x] API contracts match design — §12 names `reconcileMove(input: LedgerMoveInput): LedgerResult` as the single pure entry point; `userReviewRequired: true` literal matches the economy's `ChallengeGradingResult` / `RepairGradingResult`. The integration-map symbol names (§9, §11, §14, §16) are now verified accurate against the real `src/features/pointStanding/` and `src/features/arguments/gameCopy.ts` surfaces.
- [x] All six issue scope items covered — point categories (§4), reconciliation rules (§5), `scoreHints` mapping (§6), anti-exploit gates (§7), doctrine-safe presentation (§8), integration map (§9)

## Doctrine self-check (must all be ✓)

- [x] No truth/winner/loser language in user-facing strings — §8 copy table is all play-quality phrasing ("Clean parent tie.", "Narrow concession noted — the broad point still stands."); §8.2 defines the ban-list; §8.4 forbids any aggregate winner surface; verdict tokens in the doc are all prohibitions or absent-field assertions
- [x] Score never blocks posting — doctrine constraint 3 (§3): `LedgerResult` has no `block` field; the ledger runs AFTER a move exists; §12 + §13 reaffirm
- [x] No service-role in client code — n/a (design-only, no code); §3.11 / §10 require the future model be pure-TS with no Supabase import
- [x] No direct insert into public.arguments — n/a; §13 notes argument bodies are immutable after posting (soft-delete only), the ledger only consumes
- [x] No AI calls in production app paths — §3.5 / §13: the semantic packet is fetched upstream (MCP-009 Edge Function); the ledger model itself is pure-TS and never makes a network call; AI input is `authoritative: false` and treated as a hint
- [x] Plain language only — §3.10 / §8.1: every `feedbackCode` maps through `gameCopy.toPlainLanguage` (now correctly via the `PLAIN_LANGUAGE_COPY` record); an unmapped code is suppressed, not echoed
- [x] Epic-specific doctrine — `point-standing-economy`: §8.3 + §9 + doctrine constraint 8 — concession renders as a repair ("the broad point still stands"), never a defeat; the ledger adopts `gradeRepair`'s `+0.25` broad-lift verbatim; §7 reuses `IssueDebtLedger` + `ScoringEligibility` + per-debt credit flags for "one credit per debt"
- [x] Epic-specific doctrine — `evidence-doctrine`: §4.1 splits categories into an engagement-credit axis and a factual-standing axis; the factual axis routes through `applyAntiAmplification`; §6.1 — `evidencePressure` opens a debt and never subtracts standing; §13 — popularity/heat never an input to any positive delta

All doctrine lines pass. The card is doctrine-clean.

## Test coverage

- [x] New public functions have unit tests — §15 defines `__tests__/refereeLedger.test.ts` (worked examples: continuity tie, evidence-debt open, narrowing credit, evasion penalty), `refereeLedgerAntiExploit.test.ts`, `refereeLedgerConflict.test.ts`, plus `refereeLedgerEconomyParity.test.ts` proving adopted deltas are bit-for-bit equal to `gradeRepair` / `gradeChallenge` output
- [x] User-facing strings have ban-list assertion — §15 + §8.2: `__tests__/refereeLedgerBanList.test.ts` scans every `feedbackCode`, every plain-language string, and `gameCopy.toPlainLanguage` of every new code; also asserts `LedgerResult` has no `block`/`winner`/`truthValue`/`score`-scalar/per-side-aggregate field
- [x] Edge cases from design § "Edge cases" have tests — §13 lists 14 edge cases; the §15 test files map onto these
- [x] Accessibility assertions present — n/a (not a UI card; banner rendering is explicitly MCP-008)
- [x] `refereeLedgerForbiddenImports.test.ts` — guards the pure-TS constraint and the one-way dependency (economy must not import the ledger)

The test plan is appropriate and complete for the future implementation card.

## Blockers (only if Block)

None. No doctrine violation, no secret leak, no failed verification, no design defect. The four prior integration-map findings are all resolved by commit 8d99dff.

## Suggestions (non-blocking)

None outstanding. Suggestions 1–4 from the prior review are resolved. Suggestion 5 (the `scoreHints` integer range `0..3` / `HINT_STEP = 0.08` are assumptions) was already correctly surfaced as a §16 open risk against the seed bank — the implementation card confirms the exact ceiling before fixing `HINT_STEP`. This is handled honestly; no change required.

## Operator next steps

This is a design-only card. The deliverable is the design doc plus this review.

- Push the branch: `git push -u origin feat/MCP-003-mcp-003-strategic-semantic-metadata-ledg`
- Open PR: `gh pr create --title "MCP-003: Strategic semantic metadata ledger and scoring model" --body-file docs/reviews/MCP-003.md`
- Deploy steps: none — pure design document. No `db push`, no `functions deploy`, no migration, no env var, no dependency install (design §19 confirms).
- One open operator **decision** (carried from the issue, restated in design §19): the scoreboard framing word — referee / moderator / scorekeeper / coach. The design uses "referee" as a placeholder; the choice affects only user-facing copy (the §8 `feedbackCode` table and MCP-008's banners), no type/function/test. Resolve before the MCP-003-impl copy table is finalized.
- File the future MCP-003-impl card for the pure-TS `src/features/refereeLedger/` model + the six test files in §15. The integration-map symbol names are now verified accurate, so the implementation card targets real exported symbols.
