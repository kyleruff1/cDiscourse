# REF-006 — Review

**Verdict:** Approve
**Reviewer agent run:** 2026-06-12
**Branch:** feat/REF-006-human-usability-smoke-5-task-first-u
**Design:** docs/designs/REF-006.md
**Issue:** https://github.com/kyleruff1/cDiscourse/issues/589 (#589)
**Automerge posture:** Eligible (docs-only) — this Approve gates the autonomous merge.

## Summary

REF-006 ships exactly what the card promises: two docs-only Markdown artifacts under
`docs/testing-runs/` — a facilitator-run usability protocol and a blank results template — plus a
`current-status.md` entry. The card writes no production code, no tests-as-code, no migration, no
Edge, and spends zero provider budget. Its core risk is *executability accuracy*: every start
screen, button label, and quoted UI string in the protocol must match the live shipped surfaces at
`main` `b515da6`. I spot-verified every load-bearing label against live source and all match. The
doctrine spine holds: tasks are procedural-only, the facilitator never asks who is right, the three
camelCase-glued verdict tokens from the issue's field names are renamed token-free with a
traceability map carried in both files, and `rawCodeSeen` is framed as a failure capture. Gates are
green and unchanged from baseline. No blockers, no changes requested. Two trivial non-blocking
notes below (status line-count approximations; goal-word/button-label overlap in two scripts).

## Verification

| Check | Result |
|---|---|
| typecheck (`npx tsc --noEmit`) | pass (exit 0) |
| lint | n/a (no code/test files touched; Markdown only) |
| test (`npx jest --silent`) | pass — 733 suites / 30042 passed, 1 skipped, 30043 total, exit 0 — **UNCHANGED** from baseline (docs-only diff cannot move the count) |
| secret scan | clean (2 regex hits are the `xai-adversarial` documentation filename, not a key) |
| doctrine / ban scan | clean (16 tokens only inside prohibition/failure-capture framing; zero naked `snake_case`) |
| boundary | clean (only `docs/testing-runs/REF-006-*.md` ×2 + `docs/core/current-status.md`; design doc in the separate GATE-A commit `f2abb2e`) |

Measured artifact sizes: protocol 229 lines; results template 119 lines.

## Design conformance

- [x] All design file-changes are present (2 deliverables + status entry; design doc is the GATE-A commit `f2abb2e`).
- [x] No undocumented file-changes (`git diff main..HEAD --name-only` = exactly the four expected paths).
- [x] Data model matches design (no data model — Markdown tables only, as specified).
- [x] API contracts match design (no API — the 5 tasks + 10 capture fields are transcribed faithfully).

### Executability accuracy (the card's core risk) — every quoted label verified at `b515da6`

| Quoted surface | Live source | Match |
|---|---|---|
| Gallery action `Observe →` / `Open →` (never `Continue →` for a first-timer) | `ConversationGalleryScreen.tsx:636` (`hasUserJoined ? 'Continue →' : open ? 'Observe →' : 'Open →'`) | ✓ |
| Gallery lanes `Needs first rebuttal` · `Jump in now` · `Source trail fights` · `Evidence needed` · `Definition fights` | `gameCopy.ts:1031/1037/1043/1049/1055` (`GALLERY_SECTION_DEFINITIONS`) | ✓ all five verbatim |
| `Referee note: …` | `openIssueModel.ts:1036` (`refereeNoteSentence`) | ✓ |
| `The open task is {burden}.` | `openIssueModel.ts:1020` | ✓ |
| `Point under dispute: "…"` anchor | `RefereeCardView.tsx:140/143` | ✓ |
| `No referee notes yet on this move.` empty state | `RefereeCardView.tsx:60` (`REFEREE_CARD_EMPTY_STATE`) | ✓ |
| Observer rail `Watch · Join For · Join Against · Share` | `ArgumentSideActionRail.tsx:92-101`; `gameCopy.ts:925` (`joinAffShort: 'Join For'`) | ✓ |
| Participant-other rail `Reply · Disagree` | `ArgumentSideActionRail.tsx:109-110` | ✓ |
| Own-bubble collapses to `Open Act ▾` | `ArgumentSideActionRail.tsx:113-119` + `ObserverActionDockLayout.ts:164` (`primary: 'Open Act'`) | ✓ |
| "Act is the canonical home" rail consolidation (two-path starts for Tasks 2/4) | `ArgumentSideActionRail.tsx:90-91` comment | ✓ |
| Act dock `Ask source` | `timelineNodeActionDockModel.ts:470` | ✓ |
| Composer acting-on `Ask source for {type} · "{excerpt}"` | `composerActingOnModel.ts:166` | ✓ |
| Referee Card zone-3 button `Ask for a source` (Task 2 Path A) | `actPopoutModel.ts:214` / `gameCopy.ts:1122`; zone-3 renders `entry.label` via `RefereeCardView.tsx:193` ← `openIssueModel.ts:839` | ✓ |
| Referee Card zone-3 button `Open a side issue` (Task 4 Path A) | `actPopoutModel.ts:307` / `OneBox.tsx:87`; same zone-3 label path | ✓ |
| Request-review label split: chip `Request review` / composer `Request review` / Act popout `Send for review` | `ArgumentBubbleActions.tsx:23` · `RequestReviewComposer.tsx:62` · `actPopoutModel.ts:328` | ✓ all three exactly as quoted |
| Micro-moment banner suppressed on deep-link (Finding 4) | `deepLinkEntryHint.ts:52` (`verbPhrase: ''`) | ✓ |

The protocol correctly cites the **live** gallery lane labels (`Jump in now`, `My active rooms`,
etc.) rather than the stale CLAUDE.md Stage-6.4 narrative labels (`Jump into a live dispute`,
`Easy first move`). That is the right call and the whole point of the card's pre-launch reality
audit. No quoted label fails to exist as quoted.

### Contract coverage

- [x] 5 issue tasks present verbatim (Task 5's `they → you` pronoun is a task-instruction rephrase, not a contract gap).
- [x] 10 capture fields present; 7 kept verbatim, 3 token-free renames applied **with** the traceability map in both files (protocol §"Traceability map" L141-145; template footnote L66-69).
- [x] `rawCodeSeen` framed as a **failure** capture (protocol L128-129, L152-154).
- [x] Per-task `pass`/`stuck` outcome + verbatim-quote slot (template L38-44 et al.).
- [x] Stuck-point ledger (template L75-87).
- [x] "Their words vs our labels" table framed as INPUT offered to META-1D #79 — scope extension *proposed on #79, not assumed* (template L91-94, 119; protocol L201-203).
- [x] Operator dry walk-through 8-line checklist present (protocol L175-193) and named as a pending acceptance step.
- [x] Findings-become-cards rule (protocol L197-203; template closeout L116-119).
- [x] No-quantitative-claims framing (protocol L12; template L18-20).
- [x] Privacy P1/P2/P3, anonymized quotes, no PII (protocol L167-171; template L3).

## Doctrine self-check (all ✓)

- [x] No truth/winner/loser language in user-facing strings — Task lines + facilitator scripts are plain instructions; verdict/`truth`/`verdict` tokens appear only in prohibition or failure-capture framing.
- [x] Score never blocks posting — Task 3 facilitator note states the app **never blocks** the post; the deterministic engine is the sole gate (acceptance-gate invariant respected by construction — no code, no gate added).
- [x] No service-role in client code — no code at all.
- [x] No direct insert into public.arguments — none.
- [x] No AI calls in production app paths — zero provider spend; hand-run Markdown.
- [x] Plain language only — zero naked `snake_case` (Python scan: 0 naked tokens; all underscore tokens backticked); participant-facing wording names no internal vocabulary.
- [x] Epic-specific doctrine (cdiscourse-doctrine §1/§2/§9): the three camelCase-glued tokens (`…Correct…`, `…Truth`) are renamed (`…Intended…`/`…Matched`/`…Standing`); `mixedUpScoreHeatWithStanding` and `refereeCardParaphraseMatched` are clean by construction; heat is treated as a *failure* mode to capture, never endorsed.

### Facilitator neutrality

The facilitator never asks the participant to judge who is right/wrong or "who's winning" — both are
framed only as things the facilitator does **not** do (protocol L36-37, L221-223), with the single
permitted non-answer fixed verbatim. Tasks 1/4/5 scripts describe goals without naming the target
affordance (Task 4 notably rephrases "branch a tangent" to "start a side point that's related but
separate," avoiding the `Open a side issue` / `branch` button name). Tasks 2 and 3 use the goal
words "source or a quote" and "reply," which overlap the shipped `Ask for a source` / `Reply`
labels — but those are the issue's own verbatim task goals, not UI-affordance hints, and the
participant still must discover *where*. This does not lead the witness. Acceptable.

## Test coverage

- [x] New public functions have unit tests — n/a (no code).
- [x] User-facing strings have ban-list assertion — n/a for new code; reviewer ran a manual ban-scan over both committed files (clean).
- [x] Edge cases from design §"Edge cases" have tests — n/a; the protocol's own §"Edge cases" (empty gallery, no-observation card, own-bubble, banner absent, label split, "who's winning?", raw-code-on-screen, offline, n=1) are all carried into the protocol file (L207-229).
- [x] Accessibility assertions present — n/a (not a UI code card).

## Blockers

None.

## Suggestions (non-blocking)

1. **Status-entry line-count approximations.** The `current-status.md` entry says the protocol is
   "~245 lines" (actual 229) and the template "~135 lines" (actual 119). Both are prefixed "~" and
   immaterial; tighten on a future touch if convenient. Not worth a re-spin.
2. **Goal-word / button-label overlap (Tasks 2, 3).** Already analyzed above as acceptable (the
   words are the issue's verbatim task goals, not affordance hints). Recorded only so a future
   facilitator is aware; no change required.

## Operator next steps

1. Push the branch: `git push -u origin feat/REF-006-human-usability-smoke-5-task-first-u`
2. Open PR: `gh pr create --title "REF-006: Human usability smoke — 5-task first-user pass" --body-file docs/reviews/REF-006.md`
3. Deploy steps: **none** — pure docs change. No `db push`, no `functions deploy`, no env var, no service-role, no provider key. Merge is not a deploy.
4. Automerge: Eligible (docs-only); this Approve is the gate. Squash-merge when green.
5. **The run is the work (post-merge, operator-conducted):**
   - **Dry walk-through** — run the protocol's 8-line operator checklist solo on the current build; confirm every task is executable and record the build SHA. The card does not "close" until this passes.
   - **Recruit** 1–3 first-time participants (no docs in hand).
   - **Run** the session with the protocol + a fresh copy of the results template per participant; capture verbatim stuck points, anonymized (P1/P2/P3).
   - **Commit results** under `docs/testing-runs/` (filled template; PII redacted).
   - **File findings** — every stuck point + `suggestedCopyFix` becomes a new GitHub issue against the right epic; propose the vocabulary table as a comment on META-1D #79 (scope extension proposed there, never assumed).
   - **Close** #589.
6. Post-merge worktree cleanup (operator step; commands in `.claude/agents/roadmap-reviewer.md` § "Post-merge worktree cleanup").
