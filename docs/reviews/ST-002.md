# ST-002 — Review

**Verdict:** Approve
**Reviewer agent run:** 2026-05-19
**Branch:** `feat/ST-002-suggested-reply-flags-per-bubble-card`
**Design:** `docs/designs/ST-002.md`
**Issue:** https://github.com/kyleruff1/cDiscourse/issues/13

## Summary

ST-002 lands as designed. A new pure-TS deriver `src/features/arguments/suggestedMovesModel.ts` (773 LOC) produces 0–3 ordered, advisory `SuggestedMove[]` records from upstream gameplay state (lifecycle, manual tags, auto-metadata, source-chain), built deterministically over the 9-code `SuggestedMoveCode` vocabulary specified in the issue. The SC-003 sidecar slot is widened type-only (`SuggestedMoveStub = never` → `SuggestedMoveStub = SuggestedMove`) via a single `import type` + a one-line alias change in `argumentReplySidecarModel.ts`. The SC-003 builder's runtime behaviour is unchanged — `suggestion` still resolves to `null` with `reason === 'st_002_not_yet_implemented'`. Tests: 3475 → 3549 (+74). Suites: 125 → 127 (+2). No new dependencies, no migration, no Edge Function, no `.env*`, no external API. Doctrine clean throughout: no person attribution, no truth/winner/loser tokens, no engagement/amplification reads, no AI call, no service-role.

## Verification

- typecheck: **pass** (`npm run typecheck` exit 0)
- lint: **pass** (`npm run lint` exit 0, `--max-warnings 0`)
- test: 3475 → 3549 tests (+74), 125 → 127 suites (+2). Targeted run of `suggestedMovesModel | argumentReplySidecarSuggestedMovesIntegration | argumentReplySidecarModel` = 132 / 132 pass.
- Pre-existing failing suites (unchanged from baseline, all operator-gated xAI / Anthropic env tests): `xaiAdversarialProvider`, `xaiSeededStancesLive`, `xaiAdversarialPipeline`, `xaiAdversarialSourceHarvest`, `aiDrivenBotCorpus` — 19 failed / 3530 passed total. None are introduced or affected by ST-002.
- secret scan: **clean** (no `ANTHROPIC_API_KEY` / `XAI_API_KEY` / `X_BEARER_TOKEN` / `SUPABASE_SERVICE_ROLE_KEY` / `sk-ant-*` / `Bearer ` / JWT-shape / `Authorization:` introduced)
- doctrine scan: **clean** (no `winner|loser|liar|dishonest|bad faith|manipulative|extremist|propagandist` in source diff; the one hit on `liar` was inside an existing test ban-list assertion — see Notes)

## Design conformance

- [x] All design file-changes are present (new `suggestedMovesModel.ts`; modified `argumentReplySidecarModel.ts` type-only; two new test files; `docs/core/current-status.md` updated).
- [x] No undocumented file-changes (diff is exactly the 6 files listed in the design + a current-status entry).
- [x] Data model matches design — `SuggestedMoveCode` union has the 9 specified variants; `SuggestedMove`, `SuggestionSignal`, `SuggestionDerivationInput`, `SuggestedNextMove` all match the design's published signatures.
- [x] API contracts match design — `deriveSuggestedMoves(input): SuggestedMove[]`, `buildRationale(signals)`, `_forbiddenSuggestionTokens()` all present with the documented total / deterministic / capped / never-throw behaviour.
- [x] SC-003 widening is exactly the design's "Sidecar widening contract": `+import type { SuggestedMove } from './suggestedMovesModel';` and the type alias flip. The JSDoc above `SuggestedMoveStub` was updated to describe the widened state.
- [x] No new presets in `quickActionPresets.ts` (verified — package-lock and presets file unchanged).

## Doctrine self-check (all ✓)

- [x] No truth/winner/loser language in user-facing strings — every rationale flows from RULE-003 helper lines (`LIFECYCLE_UX_MAP.*.helperLine`, `AUTO_METADATA_UX_MAP.*.helperLine`); the rationale ban-list test (`_forbiddenSuggestionTokens()`) exercises every signal × code combination.
- [x] Score never blocks posting — test "no-forced-gate" asserts `[]` is a valid output and there is no must-act sentinel; deriver has no return path that signals blocking.
- [x] No service-role in client code — confirmed by source-scan; deriver imports zero Supabase modules.
- [x] No direct insert into `public.arguments` — pure pure-TS model, no DB.
- [x] No AI calls in production app paths — forbidden-imports test pins absence of `anthropic`, `xai`, `fetch`, `supabase`.
- [x] Plain language only — snake_case scan in test #10 covers rationale + label; no internal codes leak to UI strings.
- [x] Epic-specific doctrine (`expo-rn-patterns` — no new deps; `test-discipline` — +74 new tests; `cdiscourse-doctrine` — standing band is an input but is verifiably ignored, test #15).

## Test coverage

- [x] New public functions have unit tests — `deriveSuggestedMoves`, `buildRationale`, `_forbiddenSuggestionTokens` all covered; 18 describe blocks across `__tests__/suggestedMovesModel.test.ts`.
- [x] User-facing strings have ban-list assertion — rationale ban-list + person-attribution scans (tests #8 and #9) exercise every signal × every code, plus the snake_case scan (test #10).
- [x] Edge cases from design § "Edge cases" have tests — null inputs (cluster summary / metadata / linkage / source-chain), `maxSuggestions === 0`, `source_and_quote`/`primary_present` non-override, archived_or_resolved terminal, standing-band invariance.
- [x] Accessibility assertions — N/A for ST-002 (model layer only; no UI render in this card).

## Checkpoint-by-checkpoint findings

1. **9 SuggestedMoveCode variants + total deriver over ALL_SUGGESTED_MOVE_CODES** — confirmed. Union lists `ask_source`, `ask_quote`, `narrow`, `concede`, `confirm`, `challenge_mechanism`, `challenge_scope`, `branch_tangent`, `synthesize`. `ALL_SUGGESTED_MOVE_CODES` is a frozen `ReadonlyArray` of the same 9 codes. Test #18 pins every reachable code can be produced.
2. **SC-003 widening is type-only** — diff is exactly 13 lines (one import, one alias flip, JSDoc rewrite). No call-site change; no behavior change; integration test #1 verifies SC-003 builder still emits `suggestion: null`. Existing SC-003 test "SuggestedMoveStub type is `never` so ST-002 can widen" still passes because it asserts only `null === null` (it does not pin the literal type).
3. **No new preset in `quickActionPresets.ts`** — confirmed. Diff to `package.json` / `package-lock.json` is empty. The three gap codes route as designed: `challenge_mechanism → 'challenge'`, `challenge_scope → 'narrow'`, `branch_tangent → 'branch'`, each emitting a `preset_gap` diagnostic signal. Tests in describe #12 pin this mapping exactly.
4. **Rationale text doctrine** — `RATIONALE_PHRASES` in the deriver reads VERBATIM from `LIFECYCLE_UX_MAP[*].helperLine` and `AUTO_METADATA_UX_MAP['no_response_after_n_turns'].helperLine`. No freshly-authored sentences. The ban-list test concatenates `_forbiddenLifecycleTokens()` + `_forbiddenMetadataTokens()` + the 15-token `PERSON_ATTRIBUTION_TOKENS` list and scans every rationale fixture; the snake_case regex (`/[a-z]+_[a-z]+/`) covers all rationale + label outputs.
5. **No forced-action gate** — test #13 explicitly asserts (a) `[]` is a valid output for the lifecycle-`open` happy path and (b) no field name in the returned objects matches `/onPress|onAction|dispatch|callback|handler/i`. The composer's reply path is untouched (composer files are not modified — verified via `git diff` file list).
6. **Determinism** — test #14 pins both `deriveSuggestedMoves` output (deep equality across two consecutive calls) and `sourceSignals` kind-order stability per index.
7. **`concede` is intentionally unreachable in v1** — test #18 explicitly comments "v1 doctrine: no lifecycle state or tag emits `concede` outright; the code exists in the vocabulary for ST-003 / PRESET-001 consumers", pins `ALL_SUGGESTED_MOVE_CODES` includes `'concede'`, and verifies `buildRationale([{ kind: 'preset_gap', code: 'concede' }])` accepts the code. The deriver's preset + dock maps cover `concede` (`SUGGESTED_MOVE_PRESET.concede = 'concede'`, `SUGGESTED_MOVE_DOCK_ACTION.concede = 'concede'`). The variant is a deliberate slot, not dead code.
8. **No `console.log`, no `any`, no `.env*`, no Supabase / migration / Edge Function / hosting / external API** — diff scans all clean. Deriver file contains zero `any`, zero `console.*`, zero process/env references.
9. **No new dependency** — `package.json` / `package-lock.json` not in the diff.
10. **SC-003 source-scan from PR #99 preserved** — `argumentReplySidecarModel.ts` still does NOT import from `./timelineNodeActionDockModel`, nor reference `TimelineNodeActionDockActionCode`. The PR #99 source-scan tests (lines 943 + 948 of `argumentReplySidecarModel.test.ts`) still pass.

## Notes (non-blocking)

1. **`buildRationale` empty-signal contract**: returns `''` for an empty array. The design's API contract documents the caller must not pass empty (the deriver guarantees ≥ 1). This is fine; no internal call path passes empty.
2. **`buildRationale([{ kind: 'preset_gap', code: 'concede' }])`** falls back to `RATIONALE_PHRASES.lifecycle_state_match_default` (the `open` helper line). This is doctrine-clean (RULE-003 line) but means the test #18 surface check has a slightly anodyne rationale. Acceptable for v1; a future card that consumes `concede` would route through a real higher-priority signal.
3. **Untracked artifacts**: two unrelated test-side-effect files in `docs/testing-runs/` (`2026-05-19-ai-driven-bot-corpus-dry.md`, `2026-05-19-engagement-epidemiology-synthetic.md`) are present in the worktree but correctly NOT committed to this branch. They are produced by pre-existing dry-mode fixture runs, unchanged by ST-002.
4. **Existing SC-003 test name**: `argumentReplySidecarModel.test.ts:495` is titled "SuggestedMoveStub type is `never` so ST-002 can widen" — the title is now historical (the stub is no longer `never`). The test body still passes because it only checks `null === null`. The implementer could update the title in a follow-up; not required for this card. Design's Risk #3 anticipated this.
5. The diff's one hit on the doctrine ban-list regex (`liar`) is in `__tests__/suggestedMovesModel.test.ts` inside the **smoke check ban-list** for the SC-003 placeholder (`for (const t of ['winner', 'loser', 'correct', 'true', 'false', 'liar'])`) — i.e. an explicit assertion that those tokens are NOT present. Doctrine-clean.

## Operator next steps

- Push the branch:
  ```
  git push -u origin feat/ST-002-suggested-reply-flags-per-bubble-card
  ```
- Open the PR (the design's "Operator steps" section confirms no other deploy/migration is needed):
  ```
  gh pr create --title "ST-002: Suggested reply flags per bubble card" --body-file docs/reviews/ST-002.md
  ```
- No migration to push, no Edge Function to deploy, no secret to set.
