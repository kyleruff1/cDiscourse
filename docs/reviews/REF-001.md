# REF-001 — Review

**Verdict:** Approve
**Reviewer agent run:** 2026-06-12
**Branch:** feat/REF-001-disagreement-contract-referee-card-desig
**Design:** docs/designs/REF-001-DISAGREEMENT-CONTRACT-REFEREE-CARD.md (+ pointer docs/designs/REF-001.md)
**Card:** GATE-A design-only (issue #584). Deliverable is the design doc, not production code.

## Summary

REF-001 ships exactly two design docs (762-line full doc + 13-line pointer stub), one commit `ca64cf0`
on top of main `d4b5232`, provably docs-only (0 source files touched). The design is an exemplary
integration contract: it freezes the `DisagreementContract`/`OpenIssue` TS shape, fills all 12 derivation
rows with live sources + precedence justification, covers all 13 required convergence-map rows without
inventing a parallel taxonomy, gives four field-by-field worked traces (+ a fifth illustrative recovery
case), names the REF-002/REF-003 test files, and states a clean derived-only v1 decision. Doctrine is
respected throughout: the acceptance-gate invariant is quoted verbatim against the correct live engine
path, Family J is re-asserted out of the derivation, the recovery-route guarantee is sound, and no
user-facing copy carries a verdict/person token. Every load-bearing `file:line` claim was re-resolved
against the tree and holds. Two minor count nits (relation enum "8 values" comment vs 9 enumerated;
lifecycle "18 states/18→8 map" vs the live 19 states) are self-correcting at REF-002 implementation and
do not block GATE-A ratification — recorded below as operator notes.

## Verification

- typecheck: pass (exit 0; tree green, no source changed)
- lint: not run — diff is provably docs-only (0 source files); lint cannot be affected by this card
- test: not run — docs-only diff; baseline 1805 tests / 70 suites (CLAUDE.md) unchanged by this card
- secret scan: clean (no key/token/JWT/Bearer/service-role shapes in the diff)
- doctrine scan: clean (every verdict-token hit is inside a prohibition list or the ban-list test spec, never user-facing copy)
- docs-only path gate: pass (only `docs/designs/REF-001-DISAGREEMENT-CONTRACT-REFEREE-CARD.md` + `docs/designs/REF-001.md`)
- Migration apply: N/A — no `supabase/migrations/**` in the diff

### Live-code claim spot-verification (every cited symbol/line re-resolved)

| Cited in design | Verified |
|---|---|
| `buildActPopout` actPopoutModel.ts:685 | ✓ :685 |
| `ActEntryId` / `actEntryToQuickAction` | ✓ :102 / :785 |
| `deriveSuggestedMoves` suggestedMovesModel.ts:336 | ✓ :336 |
| `selectBanner` selectBanner.ts:155 | ✓ :155 |
| `BannerSelectionResult` refereeBanners/types.ts:151 | ✓ :151 |
| `NodeLabelMark` nodeLabelTypes.ts:89 | ✓ :89 |
| `EvidenceDebtKind` values = source/quote/receipt/context/primary_record | ✓ exact match (evidenceDebtModel.ts:48) |
| `EvidenceDebtStatus` / `OPEN_EVIDENCE_DEBT_STATUSES` | ✓ :76 / :112 |
| `PointLifecycleState` pointLifecycleModel.ts:80 | ✓ :80 — **but 19 states, not "18"** (see Suggestion 2) |
| `PointLifecycleAxis` "10 axes" | ✓ :148, exactly 10 |
| `ActiveDisagreementKind` activeDisagreement.ts:47 = framing/context/fact/none | ✓ :47 exact |
| `toPlainLanguage` gameCopy.ts:855 | ✓ :855 |
| `toPlainLanguageOrSuppress` gameCopy.ts:893 | ✓ :893 |
| `HUB_NON_PRODUCTION_FAMILIES` argumentDetailModel.ts:670 | ✓ :670 |
| `confirm` / `branch_tangent` `argumentType: null` (engine-gate-exempt) | ✓ :266 + code comment :135-141 |
| `SourceChainStatus` (BuildOpenIssueInput) | ✓ evidenceModel.ts:64 |
| manual tags = 10, auto-metadata = 16 | ✓ :104 / :148 |
| `lifecycleUxMap.ts`, `machineObservationDefinitions/familyB.ts` | ✓ both exist |
| `src/domain/constitution/engine.ts` (engine path) | ✓ exists |
| `src/features/refereeLoop/` (REF-002 target) | ✓ absent, as expected (not built here) |

## Design conformance

- [x] All design file-changes are present (full doc + pointer stub; nothing else)
- [x] No undocumented file-changes (diff = 2 design docs only)
- [x] Data model matches design (12/12 `DisagreementContract` fields present; sub-types defined)
- [x] API contracts match design (`BuildOpenIssueInput`, `buildOpenIssue`, 5 named derivation helpers, `RefereeCardViewModel`)

### Issue #584 contract coverage (Scope 1–11 + acceptance criteria)

- Scope 1 (the model + pure derivation over all shipped inputs) — ✓ Data model + derivation table; every Scope-1 input source (Constitution types/transitions, RULE-005 channels, LIFE-001, META-001, MCP A–I observations, UX-001.5A allegations) appears as a typed `BuildOpenIssueInput` field with a derivation row.
- Scope 2 (Referee Card 3-zone UI contract, frozen copy) — ✓ Zone spec §; frozen example copy reproduced.
- Scope 3 (Open Issues rail, plain-language states, gameCopy, raw IDs only in Inspect) — ✓ "Open Issues rail/ledger" §; states route through `toPlainLanguageOrSuppress`; unknown codes suppressed.
- Scope 4 (loop verbs Act/Inspect/Go/box/Referee Card) — ✓ "The loop verbs" §.
- Scope 5 (derived-only vs persisted decision) — ✓ "Derived-only v1 decision" §; persistence named as future `REF-002B`.
- Scope 6 (convergence map, all required rows) — ✓ 13 required rows present (Act, Inspect, Go, OneBox chassis, suggestedMovesModel, node labels, referee banners, referee ledger, evidence debt, lifecycle, metadata ledger, active disagreement, tangent routing) + a 14th (#504 chassis). Matches slate §4A inventory; no parallel taxonomy invented.
- Scope 7 (derivation table + precedence) — ✓ 12 rows filled with live source/rule/test; precedence justified for axis, burden, state, relation; axis normalization table + `fact`-split note.
- Scope 8 (zone-1 seeds from `selectBanner`, no duplicate selection logic) — ✓ Zone 1 spec; "`selectBanner` logic is never forked."
- Scope 9 (Inspect contract; raw keys/spans/confidence only in Inspect; confidence as pips) — ✓ "Inspect contract" §.
- Scope 10 (no new classifier family/provider) — ✓ Parallel-taxonomy guard + Out of scope.
- Scope 11 (≥4 worked traces input→issue→copy→buttons) — ✓ 4 traces + 5th illustrative recovery case.
- Acceptance criteria — all 8 checkboxes satisfied (model+derivation covering every Scope-1 input; frozen ban-list-clean copy; rail vocab through gameCopy; explicit answer to the six questions; doctrine self-check vs slate §2; dependency notes onto QOL-030/031/032/033, RULE-005, UX-001.5A, #504; convergence map + §4B + four traces + Inspect contract; integration-contract output).

### Slate parity

- §4 DisagreementContract: 12/12 field parity. The design adds `replies` as a 9th `relationToParent` value (the slate §4B explicitly anticipates a neutral `reply` relation) — a deliberate, documented extension, not a drift.
- §4A convergence seams: all 13 rows mapped to the real files; no second compose system / ledger / classifier / provider path proposed.
- §4B binding derivation table, axis normalization, frozen copy: implemented, not contradicted. Frozen copy reproduced verbatim and extended with "Supports the point" to match the `supports` relation value.
- §10 #504 referee-banner collision: resolved (see below). §11 out-of-scope: mirrored faithfully.

## Doctrine self-check (all ✓)

- [x] No truth/winner/loser language in user-facing strings — frozen relation/burden/state/axis labels + card template + all four trace copies are ban-list clean; verdict tokens appear only as prohibitions and in the ban-list test spec.
- [x] Score never blocks posting — model is consultative, derives only, async, "never sits in the post path"; emits no block.
- [x] No service-role in client code — N/A (no code); design forbids it (Out of scope, doctrine self-check §6/§7).
- [x] No direct insert into public.arguments — N/A; derived-only, no write path designed.
- [x] No AI calls in production app paths — design consumes already-persisted Observations; "never triggers classification from the client."
- [x] Plain language only — rail states map through `gameCopy.toPlainLanguageOrSuppress`; raw `snake_case` confined to code spans / dev-facing derivation tables; unknown codes suppressed not echoed.
- [x] Epic-specific doctrine — cdiscourse-doctrine §10a (Observations vs Allegations): `refereeObservations` (machine) and `userAllegations` (user) are separate fields, never merged; sensitive Observations stay composer-only; **Family J (`sensitive_composer`, `productionEnabled:false`) is never a derivation input** — the model re-asserts the `HUB_NON_PRODUCTION_FAMILIES` gate (verified at argumentDetailModel.ts:670). §3 (popularity/heat not evidence): explicit edge case "heat tries to influence the issue → it does not"; no field reads engagement/views/virality/strength bands.
- [x] Acceptance-gate invariant — quoted **verbatim** against the correct live path `src/domain/constitution/engine.ts` (mirrored at `supabase/functions/_shared/constitution/`); the design correctly uses the live `src/domain/...` path, not the stale `src/lib/constitution/engine.ts`.

## No parallel taxonomy

- [x] Composes existing seams only (Act / Inspect / Go / banners / referee ledger / nodeLabels / evidenceDebt / lifecycle / metadata / activeDisagreement / tangentRouting); each row states "out of scope (stays where it is)."
- [x] No second compose system, no second scoring ledger, no new classifier family, no new provider path, no new action-label vocabulary — every label read from an existing plain-language surface; explicit "Parallel-taxonomy guard (slate §9A)."

## Gate honesty

- [x] `nextBestMoves` = `deriveSuggestedMoves` ∩ surviving `buildActPopout` entries — the 2-step intersection is specified; `.actGateParity` test pins it as a subset.
- [x] Recovery routes never re-enable engine-invalid moves — verified against the live Act entry table: `branch_tangent` and `confirm` carry `argumentType: null` and are engine-gate-exempt (actPopoutModel.ts:266 + comment :135-141); the design correctly notes the recovery route can still be removed by the **role** gate (observer) and is then dropped, not forced. "There is no click→rejected path."
- [x] Derived-only v1 — no persistence smuggled in; the `id` is a deterministic string, not a DB row; persistence split to named future `REF-002B`.
- [x] Nothing placed in the submit path — model is consultative and async by construction.

## #504 boundary

- [x] Banner disposition decided and justified — Referee Card zone 1 **absorbs** the standalone `RefereeBanner` that #504 slice 4 would relocate; #504 does NOT mount a separate `RefereeBannerView` in card detail; `selectBanner` output has exactly one home (zone 1).
- [x] Consistent with the #504 amendment comment — verified against the comment on issue #504: "the referee banner this card's slice 4 relocates … is expected to be unified with / absorbed by the Referee Card zone (REF-001's GATE-A design fixes that disposition)"; "this card stays the chassis + raw-data layer; the REF cards are the synthesis layer on top"; "REF-003's 2–3 next-move buttons are the synthesized subset, distinct from this card's full zone-5 action dock." The design reproduces this boundary exactly and ratifies it.
- [x] No duplication of #504's five zones — "REF-003 claims only the synthesized zone; it changes none of #504's five zones."

## Implementability (REF-002 could build the pure model from this doc alone)

- [x] Full `DisagreementContract` + sub-type TS; `BuildOpenIssueInput` with every input typed; 5 named, individually-tested derivation helper signatures; `RefereeCardViewModel`.
- [x] Precedence rules for axis/burden/state/relation with justification; axis normalization table mapping every source; `SuggestedMoveCode → ActEntryId` join table; 2-step `nextBestMoves` algorithm; four field-by-field traces.
- [x] Test plan names the four REF-002 files (`openIssueModel.test.ts`, `.banlist`, `.actGateParity`, `.sensitiveSurface`) + a forbidden-imports test, and the four REF-003 files (`RefereeCardView.test.tsx`, `refereeCardBanList`, `refereeCardNoRawCodes`, `refereeCardA11y`).
- One deferred item is appropriate and explicit: the full lifecycle-state→`IssueState` map is REF-002's to enumerate (the design gives the precedence rule + representative mappings). See Suggestion 2 — REF-002 must enumerate against the live 19-state union.

## Hygiene

- [x] No secrets / credential shapes in the diff.
- [x] No edit to any file outside the two design docs (diff `--name-only` = exactly the two docs).
- [x] Commit message convention: `design: REF-001 - Disagreement Contract + Referee Card design` (single commit; Co-Authored-By trailer present).

## Blockers

None.

## Suggestions (non-blocking — recordable as GATE-A operator notes; REF-002 can absorb at implementation)

1. **`RelationToParent` count comment is stale.** The type doc-comment says "(8 values)" but the union enumerates 9
   (`replies` added as the neutral relation), and the derivation table correctly says "the 9 relation values." The
   frozen copy set also lists 9 relation labels. The contract is unambiguous (build 9); only the inline comment is
   off-by-one. Correct the comment to "(9 values)" when REF-002 lands the type.
2. **Lifecycle state count cited as 18, but the live `PointLifecycleState` union has 19** (`open, answered, rebutted,
   clarified, sourced, quote_requested, source_requested, narrowed, conceded, confirmed, synthesis_ready,
   moved_on_by_affirmative, moved_on_by_negative, ignored_by_affirmative, ignored_by_negative, ignored_by_both,
   exhausted, branch_recommended, archived_or_resolved`). The design repeatedly calls it "18 states / 18→8 map." The
   full map is deferred to REF-002, so this is a descriptive count error rather than an instruction error — but REF-002
   must enumerate the `IssueState` map against all **19** live states so none (e.g. `archived_or_resolved`) is dropped.
3. **Small-screen placement** is correctly flagged as a REF-003 open question; no action for REF-001.

## Operator next steps

- Push the branch: `git push -u origin feat/REF-001-disagreement-contract-referee-card-desig`
- Open PR: `gh pr create --title "REF-001: Disagreement Contract + Referee Card design" --body-file docs/reviews/REF-001.md`
- **GATE-A ratification (operator gate, separate from merge mechanics):** ratify the design semantics, optionally
  resolving the two count nits (Suggestions 1–2) and the small-screen-placement open question. REF-ADR-001 may be
  ratified in parallel (soft input; does not block).
- Deploy steps (from design): **None** — pure design doc; no `db push`, no `functions deploy`, no env var, no GATE-C.
- Post-merge worktree cleanup: per roadmap-reviewer.md § "Post-merge worktree cleanup (operator step)".

### GATE-A questions for the operator

1. Ratify the **derived-only v1** decision (no `OpenIssue` table; persistence deferred to named `REF-002B`)? Design recommends yes.
2. Ratify promoting **`replies` to a first-class 9th `relationToParent` value** (neutral relation; side ≠ agreement)? Design recommends yes; align the "(8 values)" comment to 9 at REF-002.
3. Ratify the **axis `fact`-split** (LIFE-001 `fact` → `evidence`; `ActiveDisagreementKind` `fact` → `framing`)? Orchestrator default per slate §4B.
4. Accept that the full **lifecycle→`IssueState` map is REF-002's** (design gives precedence + examples only), and direct REF-002 to map all 19 live states?
5. Confirm the **#504 referee-banner absorption** disposition (zone 1 is the banner's sole home; #504 mounts no standalone `RefereeBannerView`) is the binding boundary for both pipelines.
