# QOL-041.2 — Review verdict

**Verdict:** approve
**Branch:** fix/QOL-041.2-in-place-migration-recovery
**Branch HEAD:** a784138
**Base (main):** df0a61d
**Issue:** https://github.com/kyleruff1/cDiscourse/issues/258

## Skill attestation

- cdiscourse-doctrine: invoked ✅
- test-discipline: invoked ✅

## Doctrine exception attestation

- **Exception scope:** EXACTLY 2 files — `supabase/migrations/20260522000012_qol_041_concession_acceptance.sql` modified in place (+40/-5), `supabase/migrations/20260523000001_qol_041_1_fix_concession_acceptances_policies.sql` deleted via `git rm` (-129).
- **Factual basis at design time:** Migration list query at 2026-05-23T22:54Z showed Remote column empty for 20260522000012 (never applied).
- **Re-verified at review time:** ✅ `npx supabase migration list --linked` (executed during this review) confirms `20260522000012` Remote column is still empty. No race condition; the auto-deploy did not fire between 22:54Z and review time. The factual basis for the exception remains valid.

## QOL-041.2 reviewer-addendum checks

- [x] **A1 Doctrine exception is correctly scoped and documented.** ✅
  - Recovery comment block (migration lines 1–29) opens with a banner (`-- ── QOL-041.2 RECOVERY — IN-PLACE EDIT (2026-05-23) ─`) and closes with a matching footer rule (line 29), making the exception block visually unmistakable to future readers.
  - (a) Why permitted: lines 17–22 state the factual basis verbatim, including the `npx supabase migration list --linked` query timestamp (2026-05-23T22:54Z) and the empty Remote column observation.
  - (b) Why standard doctrine still holds: lines 22–24 say "The exception is one-time and scoped narrowly to this recovery; future migration-bearing cards continue to follow the standard never-edit-applied doctrine."
  - (c) Issue + deleted-file references: line 3 cites "issue #258 (QOL-041.2)", lines 13–15 name the deleted fix-forward file `20260523000001_qol_041_1_fix_concession_acceptances_policies.sql`.
  - Commit message (a784138) mirrors all three points and adds "Closes #258. Supersedes #256 (QOL-041.1)."

- [x] **A2 Edited migration produces the same intended schema state.** ✅
  - `git diff a41dd3c..HEAD -- supabase/migrations/20260522000012_qol_041_concession_acceptance.sql` shows EXACTLY two kinds of change:
    1. The 30-line recovery comment block prepended (lines 1–29 + blank line 30).
    2. Five qualifier replacements inside policy WITH-CHECK subqueries: `and X.debate_id = debate_id` → `and X.debate_id = <table>.debate_id`.
  - No table schema change, no column add/drop, no policy rename, no index change, no CHECK change, no RLS toggle, no comment-on-table change.
  - Per-policy authz logic preserved byte-for-byte except qualifier disambiguation:
    - `ci_insert_author` (lines 114–130): still requires `auth.uid() = author_id` AND argument-in-same-debate AND conceded-to-argument-in-same-debate.
    - `ca_insert_receiver` (lines 218–242): still requires `auth.uid() = receiver_id` AND receiving-argument-in-same-debate-and-authored-by-receiver AND concession-item-in-same-debate-and-conceded-to-author-is-receiver.
    - `mr_insert_reactor` (lines 308–318): still requires `auth.uid() = reactor_id` AND argument-in-same-debate.

- [x] **A3 Fix is comprehensive across ALL ambiguous references.** ✅
  - `grep -nE "and [a-z]+\.debate_id = debate_id" 20260522000012_qol_041_concession_acceptance.sql` → **0 matches** (no bare references).
  - `grep -nE "and [a-z]+\.debate_id = (concession_items|concession_acceptances|move_reactions)\.debate_id" …` → **exactly 5 matches** at lines 122, 128, 228, 239, 316.
  - End-to-end scan of all `debate_id` occurrences (14 total): 3 column declarations (79, 173, 273), 3 single-table index references (103, 209, 298 — unambiguous by construction), 5 qualified subquery references (above), 1 comment text (line 5 in the recovery block), and 2 single-table index column references in the create-index DDL (102, 208). No bare ambiguous reference remains anywhere in the file.

- [x] **A4 QOL-041.1 fix-forward migration is correctly removed via git rm.** ✅
  - Working tree check: `test -f supabase/migrations/20260523000001_qol_041_1_fix_concession_acceptances_policies.sql` → file does not exist (output: `DELETED_OK`).
  - `git show a784138 --stat` lists the file with `129 deletions(-)` and the diff entry shows `deleted file mode 100644 / +++ /dev/null`, confirming the deletion is captured in version control as a proper deletion (not a missing-from-tree side-effect).

## Standard checks

- [x] **Doctrine §1/§4 (no verdict tokens).** Secret-scan-style grep over the full diff: `winner|loser|liar|correct|dishonest|bad faith|manipulative|extremist|propagandist|stupid|idiot` → **0 matches**. (`true` / `false` deliberately excluded — they appear only as SQL keywords in `not null` and CHECK clauses, neither of which is doctrine-bearing prose.)
- [x] **Doctrine §3 (engine.ts untouched).** `git diff main..HEAD -- src/lib/constitution/engine.ts` → 0 lines.
- [x] **Doctrine §6 (security: policy authz logic preserved byte-for-byte except qualifier additions).** Each of the 3 affected policies is reviewed line-by-line against PR #255 commit `a41dd3c`. The only delta inside each WITH CHECK block is the qualifier addition; the auth.uid() = author/receiver/reactor predicate is unchanged, the `exists (…)` subqueries are unchanged in shape, the conceded-to-author = receiver invariant in `ca_insert_receiver` is unchanged. The submit-argument and react-to-move Edge Functions are not touched in this diff.
- [x] **Test discipline — no test changes required; 9757 baseline preserved.** Full `npm run test` run: **Test Suites: 372 passed, 372 total / Tests: 9757 passed, 9757 total** — exactly matches the QOL-041 baseline. No tests grep by the 3 policy names (`ci_insert_author` / `ca_insert_receiver` / `mr_insert_reactor`); verbatim preservation of policy names is verified by direct grep in the migration regardless.
- [x] **Migration sanity (post-recovery).**
  - Self-contained: 3 `create table if not exists`, 8 `drop policy if exists` patterns; the migration creates everything QOL-041 needs in a single file.
  - Filename unchanged: `20260522000012_qol_041_concession_acceptance.sql` retained verbatim.
  - Sequential numbering preserved: `ls supabase/migrations/` ends at `20260522000012_…`; the deleted `20260523000001_…` was a follow-up, not a sequential dependency, so removing it leaves no gap.
  - Idempotency preserved: all `DROP POLICY IF EXISTS` and `CREATE TABLE IF NOT EXISTS` patterns intact.

## Verification battery

| Gate | Result |
|---|---|
| `npm run typecheck` | exit 0 (clean) |
| `npm run lint` | exit 0 (clean) |
| `npm run test` | 372 / 372 suites, 9757 / 9757 tests pass |
| Secret scan (ANTHROPIC_API_KEY / XAI / X_BEARER / SERVICE_ROLE / sk-ant- / xai- / Bearer / Authorization / JWT-shape) | clean |
| Verdict-token scan | clean |
| `engine.ts` purity | unchanged |
| `npx supabase migration list --linked` (re-verification of factual condition) | Remote column STILL empty for 20260522000012 — exception still factually valid |

## Findings

No blocking or change-requesting findings. Three small observations, all non-blocking:

1. **(informational, not a defect)** The recovery comment block uses Unicode box-drawing characters (`─`) for the banner rules. Postgres treats the entire `-- …` line as a comment regardless of charset, so this is purely a readability aid. No action.

2. **(informational, not a defect)** The commit message's bullet list of line numbers references the **original** QOL-041 file's lines (92, 98, 198, 209, 286), not the post-edit lines (122, 128, 228, 239, 316). This is correct contextually — the commit is documenting where the bug WAS in the original — but a future reader following the in-tree file directly will find the qualified references at the new line numbers. The migration's own recovery comment block doesn't pin line numbers, so the in-tree file remains self-consistent. No action.

3. **(informational, not a defect)** Docker is unavailable in the implementer's environment, so local-apply verification was correctly SKIPPED. The textual evidence (0 bare references, 5 qualified references, idempotent DROP/IF-EXISTS pattern, byte-equivalent policy bodies vs original) is sufficient for the type of error being fixed (a pure parser-level ambiguity that Postgres surfaces at policy-creation time). The operator's `npx supabase db push --linked` after merge is the authoritative final verification.

## Operator next steps (informational only — reviewer does not push)

After approving locally, the operator can:
1. `git push -u origin fix/QOL-041.2-in-place-migration-recovery`
2. Open PR via `gh pr create --title "QOL-041.2: in-place migration recovery" --body-file docs/reviews/QOL-041.2.md`
3. Merge to main; the Supabase GitHub integration auto-applies the migration (per the auto-deploy memory note). If auto-deploy is dormant for any reason, run `npx supabase db push --linked` manually.
4. After deploy, verify `npx supabase migration list --linked` shows `20260522000012` populated in the Remote column.

## Verdict

**approve**

Every reviewer-addendum check is ✅, every standard check is ✅, every verification-battery gate is green, and the factual basis for the doctrine exception was independently re-verified at review time. The implementer's recovery is comprehensive, minimal, doctrine-conformant, and ready to ship.
