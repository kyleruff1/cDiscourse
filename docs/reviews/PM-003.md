# PM-003 — Review

**Verdict:** Approve
**Reviewer agent run:** 2026-05-20
**Branch:** feat/PM-003-pm-003-research-page-playable-argument-m
**Design:** docs/designs/PM-003.md
**Issue:** https://github.com/kyleruff1/cDiscourse/issues/121

## Summary

PM-003 is a design-only / research-only card. The issue body is explicit: there is no Build
phase and no production code. The branch diff vs `origin/main` is exactly two new files —
`docs/designs/PM-003.md` (132 lines) and `docs/research/playable-argument-modes.md` (915 lines) —
and that is the correct, complete footprint. The research doc is a substantive, well-structured
navigation map for the RULE-004 / RULE-005 / RULE-006 / BR-003 / GAME-002 / GAME-003 / EV-005 /
COMPOSER-002 / SC-005 / VG-004 wave plus PM-004. All 20 numbered sections required by the issue
are present and populated with real content. The §15 child-issue map matches the live GitHub
issue list exactly; the §16 dependency graph is coherent and identifies the two real keystones
(COMPOSER-002, RULE-004). §1 reiterates all 10 doctrine rules verbatim, and the §20 ban-list is a
strict superset of doctrine rule 1's list — the intended relationship. Verdict-token language
("winner / right / wrong") appears only inside clearly-marked research / scope-guard /
operator-decision sections, never as proposed shippable copy. No code, no tests, no migration, no
secrets, no AI call. No concerns remain.

## Verification

- typecheck: N/A — no code files in diff (correct for a research-only card)
- lint: N/A — no code files in diff (correct for a research-only card)
- test: N/A — 0 source files / 0 test files changed; per the issue, no test delta is expected
- secret scan: clean — the only matches are the doctrine rule-6 reiteration naming key *names*
  (`SUPABASE_SERVICE_ROLE_KEY`, `ANTHROPIC_API_KEY`, `XAI_API_KEY`, `X_BEARER_TOKEN`) with no
  values; this is the required doctrine restatement, not a leak
- doctrine scan: clean — verdict tokens confined to §1 / §3 / §4 / §11 / §19 / §20 research and
  operator-decision context; no proposed user-facing copy contains a banned token

## Design conformance

- [x] All design file-changes are present (`docs/designs/PM-003.md`, `docs/research/playable-argument-modes.md`; new `docs/research/` directory created)
- [x] No undocumented file-changes (diff is exactly the 2 design-named files)
- [x] Data model matches design (design states "No new data model"; none present)
- [x] API contracts match design (design states "None"; doc only *describes* child-card contracts, which is correct)

## Issue acceptance-criteria conformance

- [x] Research page exists at `docs/research/playable-argument-modes.md`
- [x] All 20 numbered sections present (§1–§20 confirmed by header scan) and populated with
      substantive content — no placeholders or TODO stubs found
- [x] §15 child-issue map matches the live sibling cards. Verified against
      `gh issue list --repo kyleruff1/cDiscourse`: VG-004 (#113) CLOSED as claimed; COMPOSER-002
      #111, SC-005 #112, RULE-004 #114, RULE-005 #115, RULE-006 #116, BR-003 #117, GAME-002 #118,
      GAME-003 #119, EV-005 #120, PM-003 #121, PM-004 #140 all OPEN as claimed. Upstream
      foundations BR-001 #7, EV-001 #14, EV-002 #15, RULE-001 #32, RULE-002 #33, LIFE-001 #61,
      META-001 #62, SC-004 #63, RULE-003 #65, COMPOSER-001 #84 all CLOSED as claimed. PvP slate
      #141–#147 all OPEN as claimed; RULE-007 #145 title matches the §19 cross-reference.
- [x] §16 dependency graph is coherent — edges are acyclic, "keystone" reasoning (COMPOSER-002,
      RULE-004) is sound, the GAME-003 mutual-entanglement resolution is explicitly addressed
- [x] §19 captures the open operator product questions (6 documented, none answered — recommendations
      carried only as input, exactly as the issue requires)
- [x] Doctrine: §1 reiterates all 10 doctrine rules verbatim; "do not copy" list (§20 ban-list)
      and false-positive-avoidance rules (§3, §4) do not contradict `cdiscourse-doctrine`

## Doctrine self-check

- [x] No truth/winner/loser language in user-facing strings — the doc introduces zero shippable
      copy; every verdict-token occurrence is in §1 (rule reiteration / scope-guard), §3–§4
      (research framing), §11 (EV-005 non-accusation rule — tokens appear as *prohibitions*),
      §19/§20 (operator-decision + ban-list). The §8 permanent-record copy and §7 side-branch
      label candidates are explicitly described as plain-language and ban-list-gated, with the
      final wording deferred to the operator (§19).
- [x] Score never blocks posting — §3, §4, §5, §13 repeatedly state only structural conditions
      block; score/advisories never do. Consistent with doctrine rule 1.
- [x] No service-role in client code — no code in diff; doc only restates the rule
- [x] No direct insert into public.arguments — no code; §7 explicitly routes side-branch creation
      through the existing `submit-argument` path and never hard-removes content (doctrine rule 8)
- [x] No AI calls in production app paths — no code; §10/§18 explicitly state RULE-006 ships zero
      production AI calls and its implementation is a separate operator-gated card
- [x] Plain language only — no raw internal codes proposed as UI strings; §9 names the
      `gameCopy` plain-language map as the constraint child cards inherit
- [x] Epic-specific doctrine — `cdiscourse-doctrine` (universal, the relevant skill for a
      Project-Mgmt research card): all 10 rules reiterated verbatim in §1; the §20 ban-list is a
      documented strict superset of rule 1's token list. No epic-specific surface skill applies —
      PM-003 ships no UI, no DB, no Edge Function, no scoring logic.

## Test coverage

- [x] New public functions have unit tests — N/A; no code shipped. §20 correctly specifies the
      *wave's* test doctrine (per-card pure-model suites, ban-list tests, keyword-no-block
      invariants, benign/should-advise false-positive corpora) so each child card inherits the bar.
- [x] User-facing strings have ban-list assertion — N/A for PM-003 (no strings shipped); §20.2
      names the per-card ban-list test files for the wave.
- [x] Edge cases from design § "Edge cases" have tests — N/A; the design's edge cases are
      documentation-correctness concerns (closed sibling marked closed, PM-004 companion files
      not invented, child-count drift) and each is verifiably handled in the doc text.
- [x] Accessibility assertions present — N/A; PM-003 is not a UI card.

## Blockers

None.

## Suggestions (non-blocking)

1. §15 / §16 / §19 are snapshots dated 2026-05-20. The design doc already flags research drift as
   a risk and the doc's closing line instructs contributors to keep §15/§19 current. No action
   needed now; future sibling-card landings should refresh those sections — a cheap doc edit.
2. The design doc (PM-003.md §"File changes") estimates the deliverable at "~430 lines"; the
   actual file is 915 lines. The larger doc is a positive (more substantive content), but a
   future design-doc author may want to size estimates more loosely for long-form research cards.
   Cosmetic only.
3. §14 references PM-004 companion research files under `docs/project-research/` and
   `docs/roadmap-expansions/` that do not yet exist; the doc correctly states PM-004 creates
   them and PM-003 does not invent them. No change needed — noting it so a reader is not surprised
   by the dangling forward-reference.

## Operator next steps

- Push the branch: `git push -u origin feat/PM-003-pm-003-research-page-playable-argument-m`
- Open PR: `gh pr create --title "PM-003: Research page — playable argument modes, friction, channels, semantic metadata" --body-file docs/reviews/PM-003.md`
- Deploy steps: none — pure documentation change. No `supabase db push`, no function deploy, no
  env var, no migration.
- Product follow-up (not a deploy step): work through the six open questions captured in §19 of
  the research doc (canonical mode origin; invite/observer defaults; off-mainline routing copy;
  the four MVP modes + sensitive-mode disclaimer approval; semantic AI provider direction; the
  forward-looking resolution/who-is-right doctrine question, which RULE-007 #145 will host)
  before the dependent child cards reach implementation.
