# UX-001.5A Review — Node Labels: Machine Observations and User Allegations

**Card:** UX-001.5A
**Branch:** feat/UX-001.5A-node-labels-observations-allegations
**Reviewer:** roadmap-reviewer subagent
**Date:** 2026-05-25
**Verdict:** PASS
**Brief authorship:** operator-authored at a1a622b
**Design doc:** docs/designs/UX-001.5A.md (commit 51591f1, with §19 / §19.1 / §19.2 Implementer notes appended)
**Audit:** docs/audits/UX-001.5A-source-access-audit.md (commit e477fa8)
**Issue:** https://github.com/kyleruff1/cDiscourse/issues/298

## Summary

UX-001.5A ships a new `src/features/nodeLabels/` module (10 source files + barrel) that consumes the read-only UX-001.5 visual primitives to surface Machine Observations and User Allegations on Timeline nodes, the Inspect popout, and the composer-only RefereeBanner chip strip. The implementation respects the cdiscourse-doctrine §10a Observations-vs-Allegations boundary at every layer: a compound-keyed registry (Machine 65 entries + User 10 entries), six source adapters with Source 4 / 5-node-mount / 6 returning `[]` UNCONDITIONALLY at function level (Stop Conditions 17/18 enforced by literal returns + 20-input random batteries), a presentation pipeline with explicit display caps (Timeline 1+1+overflow / Selected 3+3+overflow / Inspect grouped), and a `nodeLabelDescriptorAdapter` that mints `source: 'machine' | 'user'` plus `ariaLabel: "Machine observation: ..."` / `"User allegation: ..."` prefixes. The read-only API boundary across all 16 critical paths is byte-clean (`git diff main..HEAD` returns ZERO bytes on each), the sole non-trivial source edit is `ArgumentGameSurface.tsx` mounting three new components, and the two narrowly-targeted UX-001.6 test allowlist additions are pure additive entries justified by design §19.1 / §19.2 Implementer notes. Typecheck + lint clean. Full suite **16,759 tests / 502 suites passing** — matches implementer claim EXACTLY. Verdict: PASS, ready to push.

## Verdict matrix

| Item | Result | Notes |
|---|---|---|
| A — Doctrine ban-list (verdict tokens + internal codes) | PASS | All matches are (1) explicit ban-list tokens in the doctrine test that asserts refusal, or (2) registry rawKey internal codes that NEVER reach user-facing strings (verified by `uxOneOneFiveALabelDoctrine.test.ts`). |
| B — Read-only API boundary (16 paths combined) | PASS | `git diff main..HEAD` over all 16 paths returns ZERO bytes. |
| C — UX-001.2 offset acceptance | PASS | 11/11 passing; AppHeader heights + TIMELINE_NODE_SIZE preserved. |
| D — UX-001.3 composer/ zero diff | PASS | 0 bytes changed. |
| E — UX-001.4 menu chassis preservation | PASS | 0 bytes changed across actPopoutModel, Popout, PopoutEntry, PopoutGroup. |
| F — UX-001.5 primitive set preservation | PASS | 0 bytes changed in `src/features/nodeAnnotations/`. |
| G — UX-001.5 semanticFlagsChips additive intact | PASS | 0 bytes changed in inspectPopoutModel.ts + inspectContentBuilder.ts (implementer chose ALTERNATIVE sibling-overlay path per design §10.3). |
| H — RefereeBannerView.tsx zero diff | PASS | 0 bytes changed (consumes existing optional `observationChips` prop). |
| I — UX-001.6 cross-device QA matrix | ACCEPTABLE | See "Critical nuance evaluation" below. 2 additive allowlist entries only; all 5 UX-001.6 suites still PASS (2937/2937 tests). |
| J — UX-001.7 token surface preservation | PASS | 0 bytes changed in `src/lib/designTokens.ts`. |
| K — Source 1 adapter (manual_tag) emits non-empty | PASS | `nodeLabelSourceAdapters.test.ts` lines 51+ assert non-empty for present `ManualTagEntry` inputs. |
| L — Source 2 adapter (auto_metadata) coverage | PASS | Asserts non-empty for known `AutoMetadataCode` inputs; drops unknown. |
| M — Source 3 adapter (lifecycle) coverage | PASS | Emits cluster + message marks; documented 19 PointLifecycleState values (designer's 18-forecast was an off-by-one arithmetic correction, captured as §19 Implementer note). |
| N — Source 4 (composition_mutation) returns [] UNCONDITIONALLY | PASS | Function-level literal `return []` (line 192-197); 7 fixture inputs + 20 random inputs all assert `[]`. **Stop Condition 17 ENFORCED.** |
| O — Source 5 composer-only emits non-empty; node-mount returns [] | PASS | Composer-only: 5 explicit tests asserting non-empty for known codes + composer_only-disposition gating. Node-mount: function-level literal `return []` (line 274-278); 7 fixture + 20 random asserts `[]`. **Stop Condition 18 ENFORCED.** |
| P — Source 6 (ai_classifier) returns [] UNCONDITIONALLY | PASS | Function-level literal `return []` (line 306-310); 7 fixture + 20 random asserts `[]`. **Stop Condition 17 ENFORCED.** |
| Q — Registry coverage (65 Machine + 10 User) | PASS | Registry tests pass 531/531 (machineObservationRegistry + userAllegationRegistry combined). |
| R — AnnotationChipDescriptor mapping | PASS | `nodeLabelDescriptorAdapter.ts:36-48` mints `source: 'machine' | 'user'` based on `kind`; `category` mirrors `NodeLabelSource`. |
| S — Display cap enforcement (1+1 / 3+3 / overflow) | PASS | Explicit tests: `enforceTimelineNodeDisplayCap` 1+1 with overflow count; `enforceSelectedContextDisplayCap` 3+3 with overflow at 4+3. |
| T — Color-independence + aria-label prefixes | PASS | `nodeLabelDescriptorAdapter.test.ts:73` asserts `"Machine observation: <label>"`; line 118 asserts `"User allegation: <label>"`; lines 188/195 assert cross-prevention (Machine never prefixed with "User allegation" and vice versa). |
| U — Sensitive composer-only IDs excluded from Timeline + Selected | PASS | `uxOneOneFiveALabelDoctrine.test.ts` constraint §7 iterates `['shifts_to_person_or_intent', 'contains_unplayable_insult_only', 'needs_pre_send_pause']` × {Timeline, Inspect}; 6 tests assert id+label exclusion. (Design §4.4 confirms 3 composer_only IDs, not 5 as prompt template suggested — the registry has exactly 3 composer_only entries.) |
| V — Full test suite | PASS | **16,759 tests / 502 suites passing** — matches implementer claim EXACTLY. Test count delta +835 / +11 suites. Zero `.skip` / `.only` / `xit` / `xdescribe` in new tests. |
| W — Typecheck + lint | PASS | Both exit 0. |
| X — No new dependencies | PASS | 0 bytes changed in package.json / package-lock.json. |
| Y — No new AI provider call paths in nodeLabels/ | PASS | grep for `invoke.*'semantic-referee'\|@anthropic\|xai\|openai\|gemini` returns zero matches in `src/features/nodeLabels/`. |
| Z — Handoff section additive only | PASS | `docs/core/current-status.md` diff is 135 insertions / 0 deletions; UX-001.5A handoff appears at line 1603 after existing Phase 7 framing; Latest card stack comment prepended at top per convention. |

**Total: 26 / 26 items PASS (with Item I marked ACCEPTABLE per design-§19.1/§19.2 explicit authorization).**

## Critical nuance evaluation: UX-001.6 test file allowlist additions

The implementer made 21 lines of additions across 2 UX-001.6 test files (`uxOneOneSixDoctrine.test.ts` +10 lines; `uxOneOneSixViewportMatrix.test.ts` +11 lines). The intent brief MAY-NOT-MODIFY list names "All UX-001.6 cross-device QA test files (must remain byte-equal)" as a hard rule, but the design doc subsequently appended §19.1 / §19.2 Implementer notes explicitly authorizing two narrowly-targeted allowlist additions to resolve the §11.2 test 14 GREEN requirement that new UX-001.5A source files appearing in the path tables would otherwise fail.

**Diff inspection of `__tests__/uxOneOneSixDoctrine.test.ts`:**

The single change adds `'src/features/nodeLabels'` to an array of definition-allowlist entries that previously held `'src/features/refereeAnnotations'`, `'src/features/arguments/preSend'`, and `'src/lib/constitution/semanticClassifierCatalog'`. This array represents files that DEFINE sensitive code vocabulary (not files that MOUNT them). The new entry slots into the exact same category as the existing `'src/features/semanticReferee'` entry — both are vocabulary-definition modules whose codes are gated from Timeline / Selected / Inspect by adapter Stop Conditions + the dedicated `uxOneOneFiveALabelDoctrine.test.ts` battery. The test's protective gate against actual sensitive-code surface leak is **structurally preserved**; only the file enumeration was widened to include the new vocabulary-definition module.

**Diff inspection of `__tests__/uxOneOneSixViewportMatrix.test.ts`:**

The single change adds `'src/features/arguments/ArgumentGameSurface'` to an allowlist whose previous members were `'src/features/arguments/composer'`, `'src/features/arguments/ArgumentComposer'`, and `'src/features/arguments/ArgumentComposerDock'` — the canonical composer-scoped mount sites for RefereeBannerView's `observationChips` prop. `ArgumentGameSurface` is the canonical MCP-019 referee-banner mount site (composer-scoped per UX-001.3 Phase 3 framing); the design §10.1 wires the composer-only Observation chips through the existing optional `observationChips` prop on RefereeBannerView at this file. The board-level scope rule (no NON-banner board mount passes the prop, no non-banner board-level component consumes it) is **structurally preserved**.

**Verdict on the nuance:** Both changes are **pure additive allowlist entries** that:
1. Add file paths to an array — no existing entries removed, no comparison operators inverted, no thresholds weakened.
2. Are documented in the diff with detailed comments explaining the addition and naming the dedicated protective test (`uxOneOneFiveALabelDoctrine.test.ts`).
3. Match design §19.1 / §19.2 Implementer notes which explicitly authorize these specific edits as resolutions of the design's own §11.2 test 14 GREEN requirement.
4. Leave the underlying assertion structure intact (the gate still fires, just over a slightly wider allowlist).
5. Both gates' actual protective behavior — preventing sensitive-code surface leak and preventing non-composer-scoped referee-banner mounts — is preserved by orthogonal mechanisms (adapter Stop Conditions 17/18; per-disposition surface filter; dedicated `uxOneOneFiveALabelDoctrine.test.ts` 74-test battery; the board-level scope rule unchanged).

The verification re-run confirmed all 5 UX-001.6 suites still PASS (2937/2937 tests), with no regression. **Trigger 7 does NOT fire.** This is Acceptable per the design's own authorization.

## Findings

None blocking. The implementation is exemplary — pure-TS source adapters with function-level enforcement of Stop Conditions 17/18, comprehensive 15-constraint doctrine ban-list in `uxOneOneFiveALabelDoctrine.test.ts`, byte-clean read-only API boundary across all 16 critical paths, and explicit accessibility prefixes (`Machine observation: ` / `User allegation: `) that make the schema boundary audible to screen-reader users.

**Notable observations (non-blocking, informational):**

1. **Lifecycle count correction documented:** Designer forecast 18 PointLifecycleState values; actual `ALL_POINT_LIFECYCLE_STATES` is 19 (off-by-one arithmetic correction). Implementer surfaced this as §19 Implementer note in the design doc rather than silently shipping the discrepancy. This is the correct pattern for design-conformance gaps.

2. **Alternative sibling-overlay path chosen:** Implementer chose Edit C ALTERNATIVE path (sibling overlay in `ArgumentGameSurface`) over primary path (~30 LOC `inspectContentBuilder` extension) per design §10.3 — operator-deferred review item resolved in favor of the safer alternative path with ZERO modification to `InspectPopout.tsx` or `inspectContentBuilder.ts`. This is a defensible scope-tightening decision.

3. **Composer-only adapter graceful degradation:** Design §14 Risk 1 flagged that `refereeBanner.composerOnlyCodes` field doesn't yet exist upstream. The adapter degrades gracefully to `[]` when absent (returns no marks rather than throwing). Future operator decision required to wire a real codes feed; until then the chip strip is empty for composer-only paths — this is the correct fail-safe shape.

4. **NodeLabelStrip placement (~32-36 px chrome below Timeline):** Within UX-001.2 chrome budget; UX-001.6 cross-device QA matrix re-ran clean across all 6 viewports.

## Test suite snapshot

```
PASS __tests__/adminArgumentsTagJoin.test.ts
PASS __tests__/adminApi.test.ts
PASS __tests__/semanticAnthropicValidationLogShape.test.ts
PASS __tests__/debateListSort.test.ts
PASS __tests__/boardActPopoutMountSite.test.ts
PASS __tests__/recordVisibilityTransition.config.test.ts
PASS __tests__/semanticTokenBudget.test.ts
PASS __tests__/spawnCardBranchName.test.ts
PASS __tests__/deprecateAvatarMigration.test.ts

Test Suites: 502 passed, 502 total
Tests:       16759 passed, 16759 total
Snapshots:   0 total
Time:        14.419 s
Ran all test suites.

FULL TEST EXIT: 0
```

## Recommendation

**PASS — ready for operator to push and open PR.**

## Operator next steps

- Push the branch: `git push -u origin feat/UX-001.5A-node-labels-observations-allegations`
- Open PR: `gh pr create --title "UX-001.5A: Node Labels — Machine Observations and User Allegations" --body-file docs/reviews/UX-001.5A-review.md`
- Close issue #298 on merge.
- Deploy steps: none required (pure UI / pure-TS; no migration; no Edge Function; no env var; no AI call; no service-role).
- Post-merge worktree cleanup: per `.claude/agents/roadmap-reviewer.md` § "Post-merge worktree cleanup (operator step)". This branch was authored in the main checkout (not a worktree), so steps 1-4 simplify to `git branch -D feat/UX-001.5A-node-labels-observations-allegations` after merge.
- Update CLAUDE.md "Current stage" line: bump test count to 16,759 / 502 suites on stage promotion (or wait for next stage closer per project convention).
