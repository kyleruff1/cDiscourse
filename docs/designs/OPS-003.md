# OPS-003 — Worktree cleanup procedure hardening (lock force, long paths, filesystem orphans, branch refs)

**Status:** Design draft
**Epic:** Operations / Pipeline hygiene (third card in the OPS-* series; sibling to OPS-001, OPS-002)
**Release:** 6.7 (lesson-capture, post-OPS-002 real-world cleanup session)
**Issue:** https://github.com/kyleruff1/cDiscourse/issues/277
**Skill consulted:** `cdiscourse-doctrine` (invoked before reading any code per the agent contract).

---

## §1 — Current state + 4 gaps

### §1.0 — OPS-002 procedure verbatim (the contract being strengthened)

OPS-002 added the section **"Post-merge worktree cleanup (operator step)"** to
`.claude/agents/roadmap-reviewer.md` at lines 196–275. The body in shipping
form is the following block (reproduced verbatim from
`.claude/agents/roadmap-reviewer.md:196–275` at OPS-003 design time):

```markdown
## Post-merge worktree cleanup (operator step)

After the PR merges to `main`, the orchestrator runs the cleanup
procedure below to remove the implementer's worktree and the
auto-generated local branch. This step is the operator's, not the
reviewer subagent's — the reviewer cannot delete its own worktree
mid-session. The reviewer's "Operator next steps" block names the
deploy command; this section adds the cleanup command beneath it.

Run from the main repo root (not from inside a worktree):

```
# 1. Identify the worktree path for this card.
git worktree list | grep "feat/<code>-<slug>"
#   → C:/Users/kyler/cdiscourse/debate-constitution-app/.claude/worktrees/agent-<hash>

# 2. Remove the worktree. --force is required because the worktree
#    was created with isolation="worktree" and is marked locked.
git worktree remove --force ".claude/worktrees/agent-<hash>"

# 3. Delete the local auto-branch (the rename step in the implementer
#    charter renames worktree-agent-<hash> to feat/<code>-<slug>, so
#    after merge the local feat branch is the only one left; if a
#    pre-OPS-002 card was used, both branches may exist).
git branch -D feat/<code>-<slug>          # post-OPS-002 cards
git branch -D worktree-agent-<hash>       # pre-OPS-002 cards (if present)

# 4. Verify cleanup.
git worktree list | grep -c "agent-<hash>"   # must print 0
git branch -a | grep -E "feat/<code>-<slug>|worktree-agent-<hash>"
#   → only the remote-tracking branch remains, and after `gh pr merge
#     --delete-branch` even that is gone.
```

**Cross-platform notes.**

- The commands above are POSIX-shape but run identically in PowerShell
  (`git` accepts the same arguments on Windows). The path separator
  `/` works in PowerShell for `git worktree remove`; backslashes are
  not required.
- On Windows, `grep` is not native PowerShell; substitute
  `Select-String` if running PowerShell-only.
- `git worktree remove --force` will fail if the worktree path is
  the operator's current working directory. The operator must `cd` to
  the main repo root first.
- If `gh pr merge --delete-branch` ran *before* `git worktree
  remove`, the remote-branch delete may have failed silently. Re-run
  `gh api -X DELETE repos/{owner}/{repo}/git/refs/heads/feat/<code>-<slug>`
  after cleanup, or let `git remote prune origin` clear it later.

**Bulk cleanup of historical orphans.**

For the 96 existing orphan worktrees (one-time historical debt), the
operator may run a single PowerShell pass to remove every locked
worktree whose branch has already merged. (Operator-judgement-gated;
not automated by OPS-002.)
```

### §1.1 — The four gaps observed during the 2026-05-24 cleanup session

The 2026-05-24 session executed the OPS-002 procedure against the 96 orphan
worktrees that accumulated before OPS-002 shipped. Cleanup outcome was a
success — 96 worktrees + 8 filesystem orphans + 218 branches removed,
8.93 GB recovered, typecheck + lint + spawn-card regression test all green
post-cleanup. But the execution surfaced four distinct procedural gaps.

| Gap | Frequency observed | Workaround applied | One-sentence cause |
|---|---|---|---|
| **EC-1: `--force` insufficient for agent-locked worktrees** | 96/96 git-registered worktrees (100%) | Replaced `git worktree remove --force <path>` with `git worktree remove -f -f <path>` (double force) | Git distinguishes "locked" from "in-use"; an explicit lock requires `-f -f` to override, as git itself emits in its error message. |
| **EC-2: Windows MAX_PATH (260 chars) blocks deep node_modules removal** | 14/96 worktrees (~15%) | PowerShell `Remove-Item -Path "\\?\<absolute-path>" -Recurse -Force` followed by `git worktree prune` | Nested `node_modules/jest/.../node_modules/...` paths exceed Win32 MAX_PATH (260 chars) and `git worktree remove` uses the standard file API; the `\\?\` UNC prefix bypasses MAX_PATH validation. |
| **EC-3: Filesystem orphans live outside git's admin state** | 8 directories (~1.45 GB: 5 unregistered `agent-*` + 3 zero-byte session dirs) | Same UNC + Remove-Item method as EC-2; no `git worktree prune` needed since git's admin state never knew about them | Some past sessions created worktree directories without registering them via `git worktree add`, or the worktree was pruned but the directory persisted; `git worktree list` iteration misses these entirely. |
| **EC-4: Local branch refs accumulate independently of worktree removal** | 218 local branches matching cleanup patterns | Single bulk pass — `git branch --list <patterns>` collected the 218 branches, then `git branch -D <branch>` deleted each | `git worktree remove` clears worktree admin state but leaves the local branch ref intact; the OPS-002 per-card deletion (one branch per merge) doesn't catch historical accumulation from pre-OPS-002 sessions or auto-naming patterns that don't match the current convention. |

**Designer rationale for treating all four as binding.** EC-1 and EC-2 are
blocking errors that prevent the OPS-002 procedure from completing on the
first invocation against an agent-locked or deep-`node_modules` worktree.
EC-3 and EC-4 are accumulation gaps — the OPS-002 procedure works correctly
on each individual card but does not address state that exists outside its
per-card iteration model. All four were observed empirically in a single
cleanup session; treating any of them as out-of-scope would leave the next
operator to rediscover and re-solve the same problem.

---

## §2 — Strengthened procedure (new content for the reviewer charter section)

This section's content **replaces** the body of the existing
`.claude/agents/roadmap-reviewer.md` section "Post-merge worktree cleanup
(operator step)" at lines 196–275. The section title remains exactly the
same — `## Post-merge worktree cleanup (operator step)` — and the framing
"operator step, not reviewer subagent's" is preserved per OPS-002. Only
the body content changes.

The §2 body integrates the four new handlers cleanly with the existing
OPS-002 content: the per-card steps 1–4 are preserved with the EC-1 fix
applied in step 2; a new **EC-2 (Windows)** subsection is added inside
"Cross-platform notes"; two new top-level sub-sections — **"Filesystem
orphan sweep (EC-3)"** and **"Periodic branch-ref cleanup (EC-4)"** — are
added between the existing per-card block and the existing bulk-cleanup
block; the existing bulk-cleanup block is preserved and updated to use
`-f -f`. The cleanup remains an operator step throughout — the reviewer
subagent never executes any of the commands; it documents them in the
review-doc's "Operator next steps" block (which OPS-002 already wired and
this design does not modify).

The verbatim text the implementer writes into the charter is the block
below (everything between the outer fenced markers). Each EC handler
includes a `<!-- OPS-003: EC-N handler -->` HTML comment marker
immediately above the relevant block so the §4 regression test can scan
for it without coupling to whitespace.

```markdown
## Post-merge worktree cleanup (operator step)

After the PR merges to `main`, the orchestrator runs the cleanup
procedure below to remove the implementer's worktree and the
auto-generated local branch. This step is the operator's, not the
reviewer subagent's — the reviewer cannot delete its own worktree
mid-session. The reviewer's "Operator next steps" block names the
deploy command; this section adds the cleanup command beneath it.

Run from the main repo root (not from inside a worktree):

<!-- OPS-003: EC-1 handler -->

```
# 1. Identify the worktree path for this card.
git worktree list | grep "feat/<code>-<slug>"
#   → C:/Users/kyler/cdiscourse/debate-constitution-app/.claude/worktrees/agent-<hash>

# 2. Remove the worktree. Double force (-f -f) is required because the
#    worktree was created with isolation="worktree" and is marked
#    locked by the Claude agent. Single --force is INSUFFICIENT for an
#    explicit lock — git distinguishes "locked" from "in-use" and
#    emits "use 'remove -f -f' to override or unlock first" when given
#    only single force against an agent-locked worktree. The
#    OPS-002-era single-force command failed on 96/96 worktrees during
#    the 2026-05-24 cleanup session; the double-force form succeeds.
git worktree remove -f -f ".claude/worktrees/agent-<hash>"

# 3. Delete the local auto-branch (the rename step in the implementer
#    charter renames worktree-agent-<hash> to feat/<code>-<slug>, so
#    after merge the local feat branch is the only one left; if a
#    pre-OPS-002 card was used, both branches may exist).
git branch -D feat/<code>-<slug>          # post-OPS-002 cards
git branch -D worktree-agent-<hash>       # pre-OPS-002 cards (if present)

# 4. Verify cleanup.
git worktree list | grep -c "agent-<hash>"   # must print 0
git branch -a | grep -E "feat/<code>-<slug>|worktree-agent-<hash>"
#   → only the remote-tracking branch remains, and after `gh pr merge
#     --delete-branch` even that is gone.
```

**Cross-platform notes.**

- The commands above are POSIX-shape but run identically in PowerShell
  (`git` accepts the same arguments on Windows). The path separator
  `/` works in PowerShell for `git worktree remove`; backslashes are
  not required.
- On Windows, `grep` is not native PowerShell; substitute
  `Select-String` if running PowerShell-only: `git worktree list |
  Select-String "feat/<code>-<slug>"`. The orchestrator typically
  runs the cleanup from the Bash tool (Git-for-Windows MSYS bash) where
  `grep` works directly.
- `git worktree remove -f -f` will fail if the worktree path is
  the operator's current working directory. The operator must `cd` to
  the main repo root first (the procedure's first sentence
  enforces this).
- If `gh pr merge --delete-branch` ran *before* `git worktree
  remove`, the remote-branch delete may have failed silently because
  a worktree was still on it. Re-run `gh api -X DELETE
  repos/{owner}/{repo}/git/refs/heads/feat/<code>-<slug>` after
  cleanup to finish the remote delete, or simply let
  `git remote prune origin` clear the stale tracking ref on the next
  fetch.

<!-- OPS-003: EC-2 handler -->

**Windows: long-path workaround when `git worktree remove` reports
"Filename too long".**

On Windows, each worktree contains a full copy of `node_modules/`, and
nested dependency paths (e.g.
`node_modules/jest/.../node_modules/...`) can exceed the Win32
MAX_PATH limit (260 chars). When `git worktree remove -f -f` returns
exit 128 with `error: failed to delete '<path>': Filename too long`,
use the UNC long-path prefix from PowerShell:

```powershell
# Use the \\?\ UNC prefix; this tells Win32 to bypass MAX_PATH
# validation (supports paths up to ~32k chars). The path must be
# absolute.
Remove-Item -Path "\\?\C:\Users\kyler\cdiscourse\debate-constitution-app\.claude\worktrees\agent-<hash>" -Recurse -Force

# Then clean up git's administrative state for the now-missing
# directory. This is the equivalent of the second half of
# `git worktree remove` minus the file deletion.
git worktree prune
```

Linux and macOS operators do not encounter MAX_PATH and can ignore
this subsection. The trigger condition is the literal substring
`Filename too long` in the `git worktree remove` error output.

<!-- OPS-003: EC-3 handler -->

**Filesystem orphan sweep (run after the per-card block, and
periodically).**

`git worktree list` enumerates only git-registered worktrees.
Directories that exist under `.claude/worktrees/` but were never
registered (or whose admin state was pruned without the directory
being removed) are invisible to the per-card block above. They
accumulate as filesystem orphans. To detect and remove them:

```powershell
# 1. List filesystem entries under .claude/worktrees/ (excluding the
#    main worktree's own directory if it lives there — by convention
#    the main checkout is at the repo root, not under .claude/).
$fsDirs = Get-ChildItem -Path ".claude/worktrees/" -Directory | Select-Object -ExpandProperty Name

# 2. List git-registered worktree paths.
$gitDirs = git worktree list --porcelain |
  Select-String -Pattern '^worktree ' |
  ForEach-Object { ($_ -split ' ', 2)[1] } |
  Where-Object { $_ -like '*\.claude\worktrees\*' -or $_ -like '*/.claude/worktrees/*' } |
  ForEach-Object { Split-Path $_ -Leaf }

# 3. Compare. Anything in $fsDirs but not in $gitDirs is a filesystem
#    orphan.
$orphans = Compare-Object -ReferenceObject $fsDirs -DifferenceObject $gitDirs |
  Where-Object { $_.SideIndicator -eq '<=' } |
  Select-Object -ExpandProperty InputObject

$orphans   # operator reviews this list
```

For each orphan, the operator decides per-orphan whether to remove
(default: yes, since by definition the orphan has no git association
to preserve). Removal uses the same UNC long-path method as EC-2,
since the orphan typically contains the same deep `node_modules/`
structure:

```powershell
foreach ($orphan in $orphans) {
  $absPath = (Resolve-Path ".claude/worktrees/$orphan").Path
  Remove-Item -Path "\\?\$absPath" -Recurse -Force
}
# No `git worktree prune` needed — git's admin state never knew about
# these directories in the first place.
```

The 2026-05-24 cleanup session found 8 filesystem orphans after all
96 git-registered worktrees were removed (5 unregistered `agent-*`
directories at ~289 MB each + 3 zero-byte session-name directories
totalling ~1.45 GB). This step is idempotent and safe to re-run.

<!-- OPS-003: EC-4 handler -->

**Periodic branch-ref cleanup (run independently of any worktree
removal).**

`git worktree remove` clears worktree admin state but leaves the
local branch ref intact. The per-card branch deletion in step 3 of
the per-card block above handles the current card's branch only;
historical accumulation from pre-OPS-002 sessions (where per-card
deletion was never run) and auto-naming patterns from past
conventions (e.g. `worktree-agent-*`, `review*`, `claude/*`) survive
until explicitly cleared. To bulk-delete these orphan refs:

```powershell
# Patterns observed in the 2026-05-24 cleanup session. The list is
# additive — append new patterns as past conventions surface; do not
# remove existing patterns without confirming no historical branches
# match them.
$patterns = @('feat/*', 'worktree-agent-*', 'review*', 'claude/*', 'ev005-expand')

# Collect candidates. Each branch is local-only (no remote impact
# because squash-merge --delete-branch cleared remotes during merge).
$branches = git branch --list @patterns |
  ForEach-Object { $_.Trim().TrimStart('*').Trim() } |
  Where-Object { $_ -ne '' -and $_ -ne (git branch --show-current) }

$branches.Count   # report count for operator review

# Bulk force-delete. Capital -D (force) is required because some
# branches may have unmerged-into-main commits from abandoned cards;
# upstream-merged-state is already verified by the OPS-002 per-card
# block, and pre-OPS-002 branches are typically abandoned-or-merged
# with no value in preserving the ref.
foreach ($branch in $branches) {
  git branch -D $branch
}
```

This step is idempotent (re-running it after all orphan refs are
deleted simply reports zero branches) and safe (no remote impact;
local refs only). The 2026-05-24 cleanup session deleted 218 orphan
refs in a single pass. Recommended cadence: run after every 10–20
cards merge, or whenever `git branch --list <patterns> | wc -l`
exceeds 50.

**Bulk cleanup of historical orphans (one-time, post-OPS-003).**

For any remaining historical-debt worktrees that pre-date OPS-003
(none are expected after the 2026-05-24 cleanup), the operator may
run a single PowerShell pass combining EC-1's double force, EC-2's
long-path workaround, and EC-3's filesystem sweep:

```powershell
# Dry run first — list every worktree under .claude/worktrees/ except
# the currently-active one.
git worktree list --porcelain |
  Select-String -Pattern '^worktree '

# Then for each non-active path the orchestrator confirms is safe,
# attempt git removal first (catches the admin state); if that fails
# with "Filename too long", fall back to the UNC + Remove-Item
# method from EC-2.
git worktree remove -f -f "<path>"
git branch -D "<branch>"
```

The bulk pass remains **operator-judgement-gated** — the orchestrator
does not automate it because mis-identifying a non-merged worktree as
orphan loses work. OPS-003 mandates the per-card cleanup in steps
1–4 above plus the EC-2/EC-3/EC-4 handlers; the bulk pass is
informational.
```

### §2.5 — Why the section title and framing remain identical

OPS-002's "operator step, not reviewer subagent's" framing is preserved
verbatim. The reviewer subagent still does not execute any cleanup; it
references the section from the review-doc's "Operator next steps" block
(the existing bullet at `.claude/agents/roadmap-reviewer.md:186` is
unchanged — `- Post-merge worktree cleanup (commands in roadmap-reviewer.md
§ "Post-merge worktree cleanup (operator step)")` still points at the
same section title). OPS-003 strengthens the body in-place; OPS-002's
contract surface is untouched.

---

## §3 — Lesson-capture content (verbatim text for `docs/core/known-blockers.md`)

**Location.** Append after the existing OPS-002 entry under the
**"RESOLVED — Previously Blocking"** heading. The OPS-002 entry currently
ends at the closing paragraph that begins "The OPS-001 reviewer-template
lesson and the OPS-002 alignment lesson together form the foundation of
the OPS-* series…". The OPS-003 entry follows immediately. The
implementer also updates the file's `_Last updated:_` line at line 3 to
`2026-05-24 (Stage 6.4 / OPS-003)`.

The block below is the verbatim text the implementer pastes:

---

### ✅ Worktree Cleanup Procedure Hardened — Lock Force, Long Paths, Filesystem Orphans, Branch Refs (OPS-003)

**Incident.** The 2026-05-24 cleanup session — the first real-world
execution of OPS-002's "Post-merge worktree cleanup (operator step)"
procedure against the 96 orphan worktrees that accumulated before
OPS-002 shipped — surfaced four distinct procedural gaps that the
OPS-002 text did not address. Session outcome was a success: 96
git-registered worktrees removed (73 cat-3 merged + 16 cat-4 abandoned
+ 7 cat-5 detached-HEAD), 8 filesystem orphans removed (5 unregistered
`agent-*` directories at ~289 MB each + 3 zero-byte session directories),
218 local branches deleted in one bulk pass, 8.93 GB of disk space
recovered (48.56 GB → 57.49 GB free), and typecheck + lint + spawn-card
regression test all green post-cleanup. But each of the four gaps
required an in-session workaround that OPS-002 did not document; the
next operator running cleanup would have rediscovered and re-solved
each one.

**Root cause.** OPS-002 designed the procedure from the per-card model:
one card ships, the operator removes one worktree and one branch. The
2026-05-24 session was the first invocation against accumulated
historical debt (96 worktrees, 218 branches, 8 filesystem orphans), and
the per-card model did not generalise:

1. **EC-1: `--force` insufficient for agent-locked worktrees.** OPS-002
   step 2 used `git worktree remove --force` (single force). Git
   distinguishes "locked" from "in-use" and emits `use 'remove -f -f'
   to override or unlock first` when given only single force against
   an agent-locked worktree. The Claude `isolation="worktree"` runtime
   marks every worktree as locked, so 96/96 worktrees failed step 2 on
   the first attempt. The fix is mechanical (`--force` → `-f -f`).

2. **EC-2: Windows MAX_PATH (260 chars) blocks deep `node_modules`
   removal.** After EC-1 was fixed, 14/96 worktrees still failed with
   `error: failed to delete '<path>': Filename too long`. Nested
   `node_modules/jest/.../node_modules/...` paths exceed Win32
   MAX_PATH and `git worktree remove` uses the standard file API. The
   fix is the UNC long-path prefix from PowerShell:
   `Remove-Item -Path "\\?\<absolute-path>" -Recurse -Force` followed
   by `git worktree prune`. Linux and macOS operators do not hit this.

3. **EC-3: Filesystem orphans exist outside git's admin state.** After
   all 96 git-registered worktrees were removed and `git worktree
   list` showed only main, `Get-ChildItem .claude/worktrees/` still
   listed 8 directories that git's admin state never knew about
   (either past-session worktrees that were never `git worktree add`-ed
   or worktrees that were pruned while leaving their directory
   behind). The OPS-002 per-card iteration over `git worktree list`
   misses these by construction. The fix is a `Compare-Object` sweep
   between filesystem and git's list, with per-orphan operator
   decision (default: remove, since by definition no git association
   exists to preserve).

4. **EC-4: Local branch refs accumulate independently of worktree
   removal.** After all worktrees were removed, `git branch --list
   'feat/*' 'worktree-agent-*' 'review*' 'claude/*' 'ev005-expand'`
   returned 218 branches with no associated worktree and no associated
   remote (squash-merge `--delete-branch` had cleared remotes during
   each card's merge). OPS-002's per-card step 3 deletes the current
   card's branch only; historical accumulation from pre-OPS-002
   sessions (where per-card deletion was never run) and auto-naming
   patterns from past conventions survive until explicitly cleared.
   The fix is a periodic pattern-based bulk pass (`git branch -D`)
   that is idempotent, safe (local refs only), and runnable
   independently of any worktree removal.

**Resolution (OPS-003, #277).** The
`.claude/agents/roadmap-reviewer.md` section "Post-merge worktree
cleanup (operator step)" is rewritten in place to address all four
gaps. The section title and framing ("operator step, not reviewer
subagent's") are preserved per OPS-002; only the body content
changes. The per-card block uses `-f -f` (EC-1 fix); a new
**Windows long-path workaround** subsection covers EC-2 with the
trigger condition `Filename too long`; two new top-level
subsections **"Filesystem orphan sweep"** and **"Periodic
branch-ref cleanup"** cover EC-3 and EC-4. The existing
bulk-cleanup-of-historical-orphans block is preserved and updated
to use `-f -f`. The
`<!-- OPS-003: EC-N handler -->` HTML comment markers placed above
each new handler block provide the regression test's contract
surface (§4). No production code, no migration, no Edge Function,
no `spawn-card.ps1` / `spawn-card.sh` edit, no
`.claude/agents/roadmap-implementer.md` edit, no
`.claude/agents/roadmap-designer.md` edit. OPS-002's
implementer-charter rename step is preserved unchanged.

**Lesson for future sessions.** Three rules:

- **Rule 1 (force-flag granularity).** When a command uses `--force`
  to override one condition (e.g. "in-use"), the same command may
  require a stronger force-flag combination (e.g. `-f -f`) to override
  a *different* condition (e.g. "locked"). Read the error message
  carefully: git names the override required. The OPS-002 procedure
  was correct for the in-use case but incomplete for the locked case;
  OPS-003 captures both.

- **Rule 2 (cross-platform path limits).** Windows MAX_PATH (260
  chars) is a real production constraint on a procedure that walks a
  deep dependency tree. Procedures that work on Linux/macOS may fail
  on Windows with no warning until the deepest path is touched. When
  documenting a cleanup procedure, include the platform-specific
  long-path workaround as a named subsection with the trigger error
  string. The fix is the `\\?\` UNC prefix on Windows; macOS/Linux
  procedures don't need it.

- **Rule 3 (admin-state vs filesystem-state divergence).** Procedures
  that iterate over a tool's admin state (e.g. `git worktree list`)
  miss state that exists outside that admin state (filesystem
  orphans). When the admin state and filesystem state are *expected*
  to be coupled (the tool creates both in lockstep), any divergence
  is by construction an orphan that the per-iteration procedure
  cannot detect. The fix is a separate `Compare-Object` sweep that
  enumerates both and surfaces the diff. The same pattern applies to
  branch refs: `git worktree remove` clears worktree admin state but
  leaves branch refs intact, and the per-card deletion catches one
  branch at a time — historical accumulation requires a separate
  pattern-based bulk pass.

The OPS-001 reviewer-template lesson, the OPS-002 alignment lesson,
and the OPS-003 cleanup-hardening lesson together form the
foundation of the OPS-* series: OPS cards capture process bugs that
ship product correctly but accumulate operational friction; they fix
the process, not the product. Future OPS cards continue to follow
the same shape (single design doc, verbatim lesson block,
minimal-footprint implementer diff, verification table in the
reviewer doc that confirms the lesson is recorded).

---

## §4 — Regression test / contract assertion

### §4.1 — Three options reconsidered (and a fourth)

The launch prompt enumerated three options:

- **Option A** — Extend `__tests__/spawnCardBranchName.test.ts` with
  charter file-scan tests for the four EC handler markers (optionally
  renaming the file to something more general).
- **Option B** — New test file `__tests__/cleanupProcedure.test.ts`
  with focused coverage of the four handlers.
- **Option C** — Charter contract assertion only via explicit comment
  markers (`<!-- OPS-003: EC-N handler -->`) and a thin file-scan
  test that asserts markers stay present.

A fourth option exists by composition:

- **Option A + C (chosen)** — Use explicit `<!-- OPS-003: EC-N handler -->`
  markers in the charter body **and** extend the existing
  `__tests__/spawnCardBranchName.test.ts` with a small file-scan group
  that asserts all four markers are present and each is followed by
  the canonical signal-phrase that identifies the corresponding
  handler. The markers from §2 are not decorative — they are the
  contract surface that the regression test consumes.

### §4.2 — Recommendation: Option A + C (extend existing test, marker-based scan)

Justification:

| Criterion | A (extend, no markers) | B (new file) | C (markers only, no test) | A + C ✅ |
|---|---|---|---|---|
| Catches a future charter edit that drops EC-1 handler | partial (depends on phrase choice) | yes | no (no test) | yes |
| Catches a future charter edit that drops EC-2/3/4 handler | partial | yes | no | yes |
| Catches a future charter edit that re-orders handlers | no (file-scan is order-agnostic) | could, with care | no | could be added; not in baseline |
| Resistant to whitespace / wording drift | no (substring match couples to text) | depends on test text | n/a | **yes** — markers are stable; signal-phrase is single-word |
| Touches a new test file | no | yes | n/a | no — reuses existing OPS-002 file |
| Test count delta | +4 | +5 to +8 | 0 | **+5** (4 marker tests + 1 ordered-presence test) |
| Reviewer mental model | "spawn-card naming + OPS-003 cleanup contract in one suite" | "two separate test files for OPS-002 + OPS-003" | "trust the markers; no test" | "one file covers OPS-002 + OPS-003" |
| Rename of existing test file | optional | n/a | n/a | **no** — keep filename; the OPS-002 test group is unchanged |

Option A + C wins on resistance-to-drift (the marker is a single token
that does not change with wording) and minimises new infrastructure
(no new file, no new dependency, no new CI job). The four marker
assertions plus one ordered-presence test add 5 tests to the
existing suite; no production code is added.

### §4.3 — Test specification

The implementer **extends** the existing file
`__tests__/spawnCardBranchName.test.ts` with a third `describe` group
**after** the two existing OPS-002 groups. The filename is preserved
(no rename); the test mirror function `computeSpawnCardBranchName` is
unchanged; the two existing groups are unchanged. The new third
group is approximately 60 lines of new test code.

**Group 3 — OPS-003 cleanup-procedure contract (file scan over
`.claude/agents/roadmap-reviewer.md`).**

The new group reads the reviewer charter file once at the top of the
group and asserts five facts about its contents:

```ts
describe('OPS-003 worktree cleanup procedure — charter contract (file scan)', () => {
  const reviewerCharterPath = path.join(
    REPO_ROOT,
    '.claude',
    'agents',
    'roadmap-reviewer.md',
  );

  const charter = fs.readFileSync(reviewerCharterPath, 'utf8');

  it('contains the EC-1 (double-force) handler marker and signal phrase', () => {
    expect(charter).toContain('<!-- OPS-003: EC-1 handler -->');
    // Signal phrase: the EC-1 fix is `-f -f` (double force). The
    // single-token `-f -f` substring is stable across wording changes.
    expect(charter).toContain('git worktree remove -f -f');
  });

  it('contains the EC-2 (Windows long-path) handler marker and signal phrase', () => {
    expect(charter).toContain('<!-- OPS-003: EC-2 handler -->');
    // Signal phrase: the EC-2 fix is the `\\?\` UNC prefix. The
    // literal four-char substring is stable; PowerShell `Remove-Item`
    // is named to clarify the platform context.
    expect(charter).toContain('\\\\?\\');
    expect(charter).toContain('Remove-Item');
    expect(charter).toContain('Filename too long');
  });

  it('contains the EC-3 (filesystem orphan sweep) handler marker and signal phrase', () => {
    expect(charter).toContain('<!-- OPS-003: EC-3 handler -->');
    // Signal phrase: the EC-3 fix is a `Compare-Object` sweep between
    // filesystem listing and git's worktree list.
    expect(charter).toContain('Compare-Object');
    expect(charter).toContain('filesystem orphan');
  });

  it('contains the EC-4 (periodic branch-ref cleanup) handler marker and signal phrase', () => {
    expect(charter).toContain('<!-- OPS-003: EC-4 handler -->');
    // Signal phrase: the EC-4 fix is a pattern-based bulk
    // `git branch -D` pass; the patterns list is the stable signal.
    expect(charter).toContain('git branch --list');
    expect(charter).toContain("'feat/*'");
    expect(charter).toContain("'worktree-agent-*'");
  });

  it('orders all four EC handler markers in EC-1, EC-2, EC-3, EC-4 sequence', () => {
    // String-index comparison guards against a future edit that
    // re-orders the handlers. The intended order is the natural
    // execution order: blocking error (EC-1, EC-2) before
    // accumulation sweep (EC-3, EC-4).
    const ec1 = charter.indexOf('<!-- OPS-003: EC-1 handler -->');
    const ec2 = charter.indexOf('<!-- OPS-003: EC-2 handler -->');
    const ec3 = charter.indexOf('<!-- OPS-003: EC-3 handler -->');
    const ec4 = charter.indexOf('<!-- OPS-003: EC-4 handler -->');

    expect(ec1).toBeGreaterThan(-1);
    expect(ec2).toBeGreaterThan(ec1);
    expect(ec3).toBeGreaterThan(ec2);
    expect(ec4).toBeGreaterThan(ec3);
  });
});
```

Total test count: 5 new tests. The baseline at OPS-003 design time
inherits the OPS-002 post-merge count (10487 tests / 418 suites). After
OPS-003 the count is expected to be **10492 tests / 418 suites** (no
new suite — the third describe block lives in the existing
`spawnCardBranchName.test.ts` file).

### §4.4 — Why marker-based scan beats wording-based scan

The OPS-002 Group 2 tests use literal substring assertions on charter
text (e.g. `**Rename the worktree auto-branch to the named feat branch.**`).
That choice is fine for OPS-002 because the rename step is a single
heading that's unlikely to be reworded. The OPS-003 EC handlers are
larger blocks (code samples, multi-paragraph explanations) and are
much more likely to be edited for clarity over time. Coupling the
test to a multi-sentence substring would force test updates on every
clarity edit, eroding the test's value.

The `<!-- OPS-003: EC-N handler -->` markers are decoupled from the
prose: they are stable contract anchors. Combined with single-token
signal phrases (`-f -f`, `\\?\`, `Compare-Object`, `git branch --list`),
the test catches the structural failure modes (handler deleted, handler
re-ordered, key command removed) without coupling to wording.

### §4.5 — Why this is "lightweight"

- Zero production source under `src/` or `app/` is added.
- Zero new dependency.
- The test reads one file (`.claude/agents/roadmap-reviewer.md`) and
  performs string operations.
- Runs in the existing `npm run test` battery; no new CI job.
- Failure mode is informative: the test name names the gap, the
  expected substring is in the assertion message.
- The OPS-002 test groups are byte-identical; no risk of regressing
  the OPS-002 contract.

---

## §5 — Acceptance criteria

The OPS-003 implementer's diff must satisfy every criterion below. The
reviewer of the OPS-003 implementation PR checks each against the diff.

1. `.claude/agents/roadmap-reviewer.md` "Post-merge worktree cleanup
   (operator step)" section body is rewritten in place per §2 of this
   design verbatim. The section title remains exactly
   `## Post-merge worktree cleanup (operator step)`. The
   per-card step 2 uses `git worktree remove -f -f` (not `--force`).
   All four `<!-- OPS-003: EC-N handler -->` markers (N ∈ {1, 2, 3,
   4}) are present in EC-1, EC-2, EC-3, EC-4 sequence. Each marker
   is immediately above the block that implements its handler.

2. `.claude/agents/roadmap-reviewer.md` has **no other** edits
   outside that section. The "Inputs you'll receive", "Phase order",
   "What the review doc MUST contain", "What you must NOT do",
   "Verdict rules", "When the design is wrong", and "Output" sections
   remain byte-identical. The review-doc template bullet at line 186
   (`- Post-merge worktree cleanup (commands in roadmap-reviewer.md
   § "Post-merge worktree cleanup (operator step)")`) remains
   byte-identical (OPS-002's wiring is preserved).

3. `docs/core/known-blockers.md` has a new section titled exactly
   **"✅ Worktree Cleanup Procedure Hardened — Lock Force, Long
   Paths, Filesystem Orphans, Branch Refs (OPS-003)"** appended under
   the existing "RESOLVED — Previously Blocking" heading, immediately
   after the OPS-002 section. Body matches §3 of this design
   verbatim.

4. `docs/core/known-blockers.md`'s `_Last updated:_` line at line 3 is
   updated to `2026-05-24 (Stage 6.4 / OPS-003)`.

5. `__tests__/spawnCardBranchName.test.ts` gains a new third
   `describe` group titled exactly
   **"OPS-003 worktree cleanup procedure — charter contract (file
   scan)"** appended after the existing two OPS-002 groups. The
   group contains exactly 5 tests matching §4.3 (one per EC marker +
   one ordered-presence test). The OPS-002 groups remain
   byte-identical. The total test count delta is **exactly +5
   tests**; the suite count delta is **exactly +0 suites** (no new
   suite, third group in existing file).

6. `docs/core/current-status.md` is updated with a one-line OPS-003
   entry above the latest-implementer-card comment (per the
   implementer charter step 6 "Update docs" block). The entry names
   the four gaps fixed (EC-1 lock force, EC-2 Windows long-path,
   EC-3 filesystem orphan sweep, EC-4 periodic branch-ref cleanup)
   and cites the 2026-05-24 cleanup-session empirical evidence (96
   worktrees, 218 branches, 8 filesystem orphans, 8.93 GB recovered).

7. The OPS-003 implementer diff touches **zero** files outside the
   four named in criteria 1, 3, 5, 6 (the reviewer charter, the
   known-blockers log, the test file, current-status). No production
   source under `src/` or `app/` is touched, no file under
   `supabase/migrations/` or `supabase/functions/` is touched, no
   `.claude/scripts/` file is touched, no `.claude/skills/` file is
   touched, no agent file other than the reviewer charter is touched
   (the implementer charter and designer charter remain byte-identical
   — OPS-002's rename step is preserved). The `spawn-card.ps1` and
   `spawn-card.sh` scripts remain byte-identical.

8. `npm run typecheck`, `npm run lint`, and `npm run test` all pass
   after the diff is applied. The doctrine ban-list scan over the
   diff (`git diff main..HEAD | grep -iE
   '\b(winner|loser|liar|correct|dishonest|bad faith|manipulative|
   extremist|propagandist|stupid|idiot)\b'`) returns zero hits. The
   secret scan
   (`git diff main..HEAD | grep -iE
   'ANTHROPIC_API_KEY|XAI_API_KEY|X_BEARER_TOKEN|SUPABASE_SERVICE_ROLE_KEY|sb_secret_|sk-ant-|Bearer |Authorization:|eyJ[A-Za-z0-9_-]{20,}'`)
   returns zero hits.

9. The OPS-002 implementer-charter rename step is **preserved
   byte-identically**. `git diff main..HEAD --
   .claude/agents/roadmap-implementer.md` returns empty. This is
   non-negotiable: OPS-003 builds on OPS-002, it does not modify it.

---

## §6 — Non-deliverables (BINDING)

The OPS-003 implementer must NOT touch any of the following. The
reviewer's verdict matrix asserts each via a `git diff` check:

- **`.claude/agents/roadmap-implementer.md`.** OPS-002's Phase-order
  step 1 (the `git branch -m feat/<code>-<slug>` rename) is preserved.
  No OPS-003 edit touches the implementer charter at all.

- **`.claude/scripts/spawn-card.ps1` and `.claude/scripts/spawn-card.sh`.**
  OPS-002 left these byte-identical; OPS-003 does the same. The slug
  computation regex `^$Code\s*-\s*` is unchanged (the cosmetic
  colon-vs-dash issue flagged in OPS-002 §7 is explicitly out of scope
  here; see below).

- **`.claude/agents/roadmap-designer.md`.** No scope here. OPS-003
  does not modify the designer charter.

- **The colon-vs-dash spawn-card regex issue (OPS-002 §7).** OPS-002's
  §7 flagged that the strip regex expects a dash but modern issue
  titles use a colon, producing slugs like
  `feat/OPS-002-ops-002-pipeline-operational-hygiene-spa`. That fix
  is a separate hypothetical card (informally "OPS-004") and is NOT
  addressed by OPS-003. OPS-003's Group 3 file-scan tests do not
  touch the OPS-002 Group 1 slug-parity tests, so the pinned
  colon-double-prefix behaviour for OPS-002 Test 3 is preserved.

- **Any production code path.** No file under `src/`, `app/`,
  `supabase/`, or `scripts/` is edited.

- **The verdict-template structure in
  `.claude/agents/roadmap-reviewer.md`.** OPS-003 rewrites the body of
  one section ("Post-merge worktree cleanup (operator step)") but does
  not change the verdict rules, the review-doc template, the input
  block, or the phase order. The review-doc template's bullet
  pointing at the cleanup section (line 186) is preserved
  byte-identically — OPS-002's wiring stays.

- **`spawnCardBranchName.test.ts` Groups 1 and 2 (the OPS-002 tests).**
  The new Group 3 is appended after them; the existing 8 tests
  remain byte-identical. No test is renamed, deleted, or rewritten.

---

## §7 — Open questions for operator

None of the four edge cases are disputed; all four were observed in
the 2026-05-24 cleanup session and each has a working workaround
documented in the evidence file. No open questions block the
implementer's work.

One discretionary call surfaced during §2 drafting:

- **Recommended cleanup cadence for §2's EC-4 step ("after every
  10–20 cards merge, or whenever `git branch --list <patterns> | wc
  -l` exceeds 50").** The numbers are heuristic, drawn from the
  observation that 218 refs accumulated over the entire pre-OPS-002
  history (~30+ cards). Faster cadence is harmless but adds operator
  noise; slower cadence delays the next bulk pass. The operator may
  override these numbers in practice; the procedure documents the
  recommendation, not a mandate. **No designer action required.**

---

## Doctrine self-check

This design adds documentation, charter text, and extends one test
file. No production code. The relevant doctrine constraints from
`cdiscourse-doctrine`:

- **§1 (no truth labels).** The design uses none of the banned tokens
  (`winner`, `loser`, `correct`, `true`, `false`, `liar`, `dishonest`,
  `bad faith`, `manipulative`, `extremist`, `propagandist`, `stupid`,
  `idiot`) in any added text. The standard reviewer ban-list scan
  over this design file is clean.

- **§2 (heat means activity).** Not applicable — no heat / activity
  surface is touched.

- **§3 (popularity is not evidence).** Not applicable — no scoring
  or evidence surface is touched.

- **§4 (AI moderator limits).** No AI is added to the pipeline. The
  cleanup procedure is deterministic git and PowerShell commands. The
  regression test is pure TS file-scan.

- **§5 (rules engine is sacred).** `src/lib/constitution/engine.ts`
  is untouched. The test file lives under `__tests__/` and does not
  import the engine.

- **§6 (secrets).** No secret value appears in any added text. The
  test file reads no env vars. The cleanup procedure runs no Supabase
  command. The charter text contains no API keys, no Authorization
  headers, no JWT-shape strings.

- **§7 (no AI calls from production app).** Not applicable — no
  production app code is touched.

- **§8 (Supabase conventions).** Not applicable — no migration, no
  RLS, no Edge Function, no `public.arguments` write.

- **§9 (plain language for users).** Not applicable — no user-facing
  copy added. The charter and lesson-capture text are operator-facing
  documentation, not user-facing UI strings.

- **§10 (v1 scope guards).** Not applicable — no product feature
  added. OPS-003 is process hygiene, not product.

The design also respects the launch prompt's explicit non-deliverables
(§6 above): no implementer charter edit, no spawn-card script edit, no
designer charter edit, no colon-vs-dash regex fix, no production code
path.

---

## Operator steps (if any)

None — pure documentation + charter + test change. After the
implementer's PR merges, the strengthened cleanup procedure is live
for the operator's next post-merge cleanup invocation. The OPS-002
implementer-charter rename step continues to apply to every
subsequent implementer subagent (it is preserved unchanged). No
deploy, no env var, no migration apply, no Supabase function deploy.
