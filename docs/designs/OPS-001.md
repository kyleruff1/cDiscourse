# OPS-001 — Reviewer template strengthening for migration-bearing cards

**Status:** Design draft
**Epic:** Operations / Reviewer discipline (first card in a future OPS-* series)
**Release:** 6.6 (lesson-capture, post-QOL-041 chain)
**Issue:** https://github.com/kyleruff1/cDiscourse/issues/260
**Skill consulted:** `cdiscourse-doctrine` (invoked before reading any code per the agent contract).

---

## §1 — Problem statement

The QOL-041 work shipped a SQL bug that no reviewer caught and that only the
operator-gated `npx supabase db push` surfaced. The bug was 5 unqualified
`debate_id` references in 3 INSERT-policy `WITH CHECK` subqueries inside
`supabase/migrations/20260522000012_qol_041_concession_acceptance.sql`. In each
case the outer policy-target table (`concession_items`, `concession_acceptances`,
`move_reactions`) and the subquery-joined `public.arguments` row both have a
`debate_id` column, so the bare RHS reference inside `... and a.debate_id =
debate_id` could resolve to either side. Postgres treats this as
`SQLSTATE 42702 ambiguous column reference` and refuses to create the policy.
Two consecutive reviewers — the original QOL-041 reviewer (`docs/reviews/QOL-041.md`,
re-review approve at commit `7ec8346`) and the QOL-041.1 fix-forward reviewer
(`docs/reviews/QOL-041.1.md`, approve at `5a2192d`) — approved by structural
read of the SQL. Neither ran the migration against a real database; the
fix-forward reviewer explicitly noted Docker was unavailable on Windows.

The QOL-041.1 attempt (PR #257 at `df0a61d`) tried to repair the bug with a
**subsequent** migration that recreated the broken policies with qualified
references. This path was architecturally dead-on-arrival: Postgres applies
migrations strictly in filename order, so the broken `…_qol_041_…` migration
always failed first, the transaction rolled back, and the fix-forward never
got a chance to run. The QOL-041.2 recovery (PR #259 at `6fcfdbf`) edited the
original migration **in place** under a one-time doctrine-scoped exception:
CLAUDE.md §8 forbids editing migrations only after they have been *applied*,
and `npx supabase migration list --linked` at 2026-05-23T22:54Z confirmed
the Remote column for `20260522000012` was still empty (the broken migration
had never reached any database). The exception was factually justified,
narrowly scoped to the recovery PR, and documented inline in the migration
file's recovery-comment banner (lines 1–29 of the post-recovery file).

The gap the lesson exposes is in the **reviewer template**, not in the
implementer's diligence: the template asked reviewers to verify migrations
by reading them, and both reviewers did exactly that. Structural reading is
insufficient for parser-level SQL bugs because Postgres only resolves the
ambiguity at policy-creation time. The empirical signal — `db push` against
a real Postgres — is the only test that catches this class of bug, and the
template did not require it. OPS-001 closes that gap.

---

## §2 — Canonical reviewer template location

**Primary canonical location:** `.claude/agents/roadmap-reviewer.md`.

**Justification.** The repo has three concentric layers of reviewer guidance,
and the new policy fits exactly one of them:

| File | Scope | Why it is NOT the primary location |
|---|---|---|
| `.claude/agents/roadmap-reviewer.md` | The executable subagent definition every roadmap review reads — the implementer pipeline drives reviewers from this file (see `docs/core/agent-workflow.md` table at line 38). | (this IS the primary location) |
| `docs/core/agent-charters.md` | Documentation-only charters for **specialized** roles (timeline-ui-agent, evidence-rules-agent, etc.). The doc explicitly states (lines 17–26) that the three pipeline agents in `.claude/agents/` are the executable layer and that "new specialized `.claude/agents/*.md` files are NOT created." The migration-verify policy applies to the **generic** review pipeline, not to a specialized lane. |
| `.claude/skills/cdiscourse-doctrine/SKILL.md` | Universal product + safety doctrine consumed by designer / implementer / reviewer alike. A reviewer-specific verification step would dilute its scope; doctrine answers "what may exist," the reviewer template answers "how to verify the diff." |
| `.claude/skills/test-discipline/SKILL.md` | Defines what counts as "passing tests" and required coverage. The migration-verify step is closer to a verification gate than a test-coverage rule; placing it here would also miss the no-Docker fallback path, which is reviewer-workflow not test-coverage. |
| `CLAUDE.md` § "Supabase Conventions" | Repo-wide convention list (RLS on, sequential migrations, never edit applied). A one-line cross-reference belongs here (see §5), but the policy text itself is too long for the convention list and belongs in the reviewer template. |

**Secondary location (cross-references only):** `CLAUDE.md` § "Supabase
Conventions" and `docs/core/agent-charters.md`. Both get short
cross-references pointing back to `.claude/agents/roadmap-reviewer.md` so
that anyone landing in either file follows the trail to the canonical
policy. Both files already exist; no new file is created.

The new policy text lives as a new section inserted into the existing
`.claude/agents/roadmap-reviewer.md` after the current "Phase order" §8
("Migration / Edge Function check") block — that block already exists and
already partially covers migrations, so the new section logically nests at
that point. The implementer chooses between (a) extending the existing
"Migration / Edge Function check" bullet into a full subsection or
(b) inserting a sibling subsection titled exactly **"Migration-bearing card
verification (mandatory)"** immediately after it. Option (b) is preferred
because it makes the new gate visually unmistakable to future reviewers,
matches the acceptance-criteria wording in the GitHub issue, and avoids
re-flowing the existing "Phase order" prose.

---

## §3 — Policy text (verbatim)

The implementer pastes the block below verbatim into
`.claude/agents/roadmap-reviewer.md` as a new top-level subsection inserted
after the current "Phase order" step 8 ("Migration / Edge Function check").
The exact heading is the bolded title; the body text follows verbatim,
including the four-class table.

---

### Migration-bearing card verification (mandatory)

**Trigger.** Any card whose implementer diff includes one or more new or
modified files under `supabase/migrations/`. Detect with:

```
git diff main..HEAD --name-only -- 'supabase/migrations/**'
```

A non-empty result triggers this section. Reviewing the SQL by reading it
alone is not sufficient — Postgres surfaces several entire classes of bug
only at apply time. The reviewer either runs the migration locally or
performs a heightened textual review against the four issue classes below.

**Required action when Docker IS available.**

1. Confirm Docker Desktop is running (`docker info` exits 0).
2. Run `npx supabase db reset --linked=false` and capture stdout + stderr.
3. If the reset completes and every migration applies cleanly, this check
   passes. Record the captured stdout tail (the "Finished applying" line is
   enough) in the review doc under "Verification."
4. If any migration fails to apply, the verdict is **Block** regardless of
   any other check passing. Quote the failing statement, the SQLSTATE, and
   the migration filename in the review's "Blockers" section. Do not
   approve the card on the basis of a reviewer-side workaround; the
   implementer must fix the migration and the branch is re-reviewed.

**Required action when Docker IS NOT available.**

1. Document the limitation in the review doc with a specific reason —
   `docker info` not found, daemon not running, platform incompatibility
   on a CI container, etc. State the reason in one sentence under
   "Verification" so a later reader knows the apply step was skipped on
   factual grounds, not by oversight.
2. Perform a **heightened textual review** of every new or modified SQL
   file in `supabase/migrations/`. The heightened review targets the four
   issue classes in the table below. For each class, scan the diff for the
   listed syntactic markers and resolve every match before approving.

**The four heightened-review issue classes.**

| # | Class | Pattern | Syntactic markers |
|---|---|---|---|
| 1 | **Ambiguous column references in subqueries** (QOL-041 motivating example) | The outer policy-target table and the subquery-joined table share a column name; the reference inside the subquery's WHERE clause is unqualified, so Postgres cannot tell at policy-create time whether the reference resolves to the outer policy-target row or to the joined row. Surfaces as `SQLSTATE 42702 ambiguous column reference` at apply time. | (a) Bare column name on the RHS of any equality inside a policy WITH CHECK or USING subquery whose LHS is qualified — e.g. `... and a.debate_id = debate_id` where `debate_id` is also a column on the outer policy-target table. (b) Any `exists (select 1 from public.X ... where ... = <bare_name>)` where `<bare_name>` is a column shared by both `public.X` and the policy-target table. (c) Mixed qualifier discipline within a single policy: if any reference in the WITH CHECK block is qualified, every other reference to a same-named column should also be qualified. Asymmetric qualification is a red flag. **Resolution:** every same-name reference must be table-or-alias-qualified. The QOL-041 worked example was `and a.debate_id = debate_id` (ambiguous) → `and a.debate_id = concession_items.debate_id` (qualified), repeated five times across three policies. |
| 2 | **Column type mismatches** | A foreign-key declaration, a CHECK constraint, or a join condition compares columns of different types. Postgres may either reject the migration outright or accept it with surprising implicit-cast semantics. | (a) `references public.<table>(<col>)` where the local column's declared type differs from the referenced column's type as defined in an earlier migration. Resolve by reading the referenced migration. (b) CHECK constraints comparing columns of different types — particularly `text` vs `uuid`, `text` vs `timestamptz`, `int` vs `bigint`. (c) Composite-key foreign references where the local column ordering or types do not match the referenced unique constraint. **Resolution:** declare the local column with the same type as the referenced column, or convert in a follow-up migration with an explicit cast and a documented reason. |
| 3 | **Implicit ordering dependencies** | A `CREATE INDEX`, `CREATE POLICY`, `CREATE TRIGGER`, or `ALTER TABLE` statement references an object that has not yet been created earlier in the same migration. Postgres applies statements within a migration in source order, so the dependency must appear earlier in the file. | (a) `CREATE INDEX ... ON public.<table>` appearing in the file before `CREATE TABLE [IF NOT EXISTS] public.<table>`. (b) `CREATE POLICY ... ON public.<table>` appearing before `ALTER TABLE public.<table> ENABLE ROW LEVEL SECURITY`. RLS must be enabled before any policy is created on the table. (c) `CREATE TRIGGER ... EXECUTE FUNCTION public.<fn>()` appearing before `CREATE [OR REPLACE] FUNCTION public.<fn>()`. (d) A `drop policy if exists` followed by a `create policy` with the same name — confirm the create block names the same target table as the drop. **Resolution:** reorder the statements so every referenced object exists before it is referenced. |
| 4 | **Function / trigger / extension dependencies** | The migration references extensions, schemas, functions, or roles that the migration does not explicitly create, grant, or assume from a prior migration. | (a) `gen_random_uuid()` without confirming `pgcrypto` (or `uuid-ossp`) is installed by an earlier migration. (b) `auth.uid()` references without confirming the Supabase `auth` schema is present (Supabase projects have it by default; self-hosted variants may not). (c) `GRANT ... TO service_role` / `anon` / `authenticated` without confirming the role exists in the target database. (d) RLS policies whose `auth.uid()` semantics assume an authenticated session — if a policy must also permit service-role bypass, that must be explicit, never assumed. (e) Trigger functions calling `public.is_admin(auth.uid())` or similar helpers — confirm the helper was created by an earlier applied migration. **Resolution:** name the prior migration that provides the dependency in a SQL comment above the dependent statement, or add the `CREATE EXTENSION IF NOT EXISTS` / `CREATE OR REPLACE FUNCTION` block to this migration. |

**Recording the result in the review doc.**

The review doc's "Verification" table gains a new row:

```
| Migration apply | local-apply pass / heightened-review pass / Block |
```

When the Docker path was taken, the cell says `local-apply pass (db reset
output: <one-line summary>)`. When the heightened-review path was taken, the
cell says `heightened-review pass — Docker not available (<reason>); classes
1–4 scanned with zero unresolved markers`. When the verdict is Block, the
cell says `Block — <SQLSTATE or marker class>; see Blockers §`.

---

## §4 — Lesson-capture text (verbatim)

**Location.** `docs/core/known-blockers.md` is the canonical
institutional-memory file for this kind of lesson. It already collects
multi-incident operator-facing notes (Anthropic key rotation, Docker
unavailability, the migration 0006 RLS recursion fix). The QOL-041 incident
chain is the same shape: a recurring constraint that future sessions need to
remember, not a one-off bug. The implementer appends the block below to
`docs/core/known-blockers.md` as a new section under the existing
**"RESOLVED — Previously Blocking"** heading (the incident is closed) and
updates the file's `_Last updated:_` line to the OPS-001 date.

The block below is pasted verbatim:

---

### ✅ QOL-041 Migration Deploy Chain — Reviewer Template Strengthened (OPS-001)

**Incident.** The QOL-041 work (#210, PR #255 at `a41dd3c`) shipped with a
SQL bug: 5 unqualified `debate_id` references in 3 INSERT-policy
`WITH CHECK` subqueries triggered `SQLSTATE 42702 ambiguous column
reference` on every `npx supabase db push` attempt. The reviewer of PR #255
approved the migration by structural read of the SQL ("well-formed per
design §5") without running the migration against a real Postgres. The
QOL-041.1 fix-forward attempt (#256, PR #257 at `df0a61d`) added a
subsequent migration to recreate the broken policies with qualified
references, but Postgres applies migrations in strict filename order — the
broken original always failed first, its transaction rolled back, and the
fix-forward migration never executed. The QOL-041.1 reviewer also approved
by read because Docker was unavailable on Windows.

**Recovery (QOL-041.2, #258, PR #259 at `6fcfdbf`).** The fix landed under
a doctrine-scoped exception to CLAUDE.md §8 ("Never edit an applied
migration"). The exception was permitted because the migration had never
been *applied* to any database — `npx supabase migration list --linked` at
2026-05-23T22:54Z confirmed the Remote column for `20260522000012` was
empty. The recovery edited the original migration in place to qualify the
5 ambiguous references and deleted the redundant fix-forward file. The
exception is one-time, narrowly scoped, factually justified, and documented
inline as a recovery-comment banner at lines 1–29 of the post-recovery
migration file. Future migration-bearing cards continue to follow the
standard never-edit-applied doctrine.

**Template update (OPS-001, #260).** The reviewer template at
`.claude/agents/roadmap-reviewer.md` now contains a mandatory subsection
titled "Migration-bearing card verification (mandatory)" that triggers on
any diff under `supabase/migrations/`. When Docker is available, reviewers
must run `npx supabase db reset --linked=false` and block on apply failure.
When Docker is not available, reviewers must document the limitation and
perform a heightened textual review against four named issue classes —
ambiguous column references in subqueries (QOL-041 motivating example),
column type mismatches, implicit ordering dependencies, and function /
trigger / extension dependencies. The full policy text and the four-class
table live in the reviewer template; the cross-references in this file,
in `CLAUDE.md` § "Supabase Conventions", and in `docs/core/agent-charters.md`
all point back to that canonical location.

---

## §5 — Cross-references

The implementer adds the following short cross-references verbatim. Each
points back to `.claude/agents/roadmap-reviewer.md` so the canonical text
has exactly one source of truth.

### 5.1 — `CLAUDE.md` § "Supabase Conventions" (existing section at line 157)

Append one bullet to the end of the existing list (after the
`arguments` rows soft-delete bullet at line 164). Exact text:

```
- Migration-bearing PRs receive heightened verification at review time. See
  `.claude/agents/roadmap-reviewer.md` § "Migration-bearing card verification
  (mandatory)" — reviewers either run `npx supabase db reset --linked=false`
  (when Docker is available) or perform a heightened textual review against
  four named issue classes. The QOL-041 deploy chain is the motivating
  incident; see `docs/core/known-blockers.md` § "QOL-041 Migration Deploy
  Chain — Reviewer Template Strengthened (OPS-001)".
```

### 5.2 — `docs/core/agent-charters.md` (existing "Common preflight" block at line 40)

Append a short paragraph to the existing "Common preflight" section,
immediately after the "Read first:" block (line 60). Exact text:

```
**Migration-bearing cards have an extra reviewer gate.** When the
implementer diff touches `supabase/migrations/`, the reviewer follows
`.claude/agents/roadmap-reviewer.md` § "Migration-bearing card verification
(mandatory)" — local migration apply when Docker is available, heightened
textual review against four named issue classes when it is not. This gate
sits on top of every charter in this file; no specialized charter overrides
or weakens it.
```

### 5.3 — `docs/core/agent-workflow.md` (existing agents table at line 38)

Append one sentence to the "roadmap-reviewer" row's "Output" cell (the
table cell currently reading `docs/reviews/<code>.md with verdict`). Exact
replacement text for that cell:

```
`docs/reviews/<code>.md` with verdict; migration-bearing diffs additionally
verified per `.claude/agents/roadmap-reviewer.md` § "Migration-bearing card
verification (mandatory)"
```

The implementer makes this a minimal edit — only the cell content changes,
not the surrounding table structure.

### 5.4 — No change to `docs/core/implementation-plan.md`, `docs/core/session-handoff.md`, or any skill file

These files do not currently discuss reviewer workflow at the level OPS-001
modifies; adding cross-references there would create noise without value.
The four files above (the reviewer template itself, `CLAUDE.md`, the
charters doc, and the workflow doc) are the complete cross-reference
surface.

---

## §6 — Acceptance criteria

The implementer's diff must satisfy every criterion below. The reviewer of
the OPS-001 implementation PR will check each one against the diff.

1. `.claude/agents/roadmap-reviewer.md` contains a new top-level subsection
   titled exactly **"Migration-bearing card verification (mandatory)"**
   inserted after the existing "Phase order" step 8 ("Migration / Edge
   Function check") block.
2. The new subsection's body matches §3 of this design verbatim, including
   the trigger detection command, the Docker-available block, the
   Docker-unavailable block, and the four-class table with all four classes
   present in their numbered order.
3. The four-class table in the new subsection names "Ambiguous column
   references in subqueries" as class 1 and explicitly cites the QOL-041
   five-reference example as the worked instance in that class's
   "Syntactic markers" cell.
4. The new subsection ends with the "Recording the result in the review
   doc" block that names the new `| Migration apply | … |` table row exactly
   as written in §3.
5. `docs/core/known-blockers.md` contains a new section titled exactly
   **"✅ QOL-041 Migration Deploy Chain — Reviewer Template Strengthened
   (OPS-001)"** under the existing "RESOLVED — Previously Blocking"
   heading, with body matching §4 of this design verbatim.
6. `docs/core/known-blockers.md`'s `_Last updated:_` line at the top of
   the file is updated to the OPS-001 commit date.
7. `CLAUDE.md` § "Supabase Conventions" has one new bullet appended
   matching §5.1 of this design verbatim.
8. `docs/core/agent-charters.md` § "Common preflight" has one new
   paragraph appended matching §5.2 of this design verbatim.
9. `docs/core/agent-workflow.md`'s agents table row for `roadmap-reviewer`
   has its Output cell replaced with the text in §5.3 of this design
   verbatim.
10. The OPS-001 implementer diff touches **zero** files outside the five
    named in criteria 1, 5, 7, 8, 9. No production source under `src/` or
    `app/` is touched, no migration file under `supabase/migrations/` is
    touched, no skill file under `.claude/skills/` is touched, no script
    under `scripts/` or `.claude/scripts/` is touched, no test file under
    `__tests__/` is touched, no agent file other than the reviewer is
    touched (`.claude/agents/roadmap-designer.md` and
    `.claude/agents/roadmap-implementer.md` remain byte-identical).

---

## Doctrine self-check

This design adds documentation and policy text only. The relevant doctrine
constraints from `cdiscourse-doctrine`:

- **§1 (no truth labels).** The design uses none of the banned tokens
  (`winner`, `loser`, `correct`, `true`, `false`, `liar`, `dishonest`,
  `bad faith`, `manipulative`, `extremist`, `propagandist`, `stupid`,
  `idiot`) in any added text. The standard reviewer ban-list scan over
  this file is clean.
- **§4 (AI moderator limits).** No AI is added to the review flow. The
  reviewer remains a human-driven (or operator-spawned) Claude Code
  session; the new gate is a deterministic local-apply or textual scan.
- **§6 (secrets).** No secret value appears in any added text. The policy
  references `npx supabase db reset --linked=false` — the `--linked=false`
  flag is explicit so reviewers never reset a hosted database during
  review.
- **§7 (no AI calls from production app).** Not applicable — no production
  app code is touched.
- **§8 (Supabase conventions).** The policy reinforces the existing
  never-edit-applied rule by adding a verification step that catches the
  bugs that would otherwise force the in-place-edit doctrine exception.
  It does not change the rule.
- **§10 (v1 scope guards).** Not applicable — no product feature added.

The design also respects the OPS-001 issue's stated non-negotiables: no
production code modified, no migration files touched, no agent charter or
skill renamed or restructured beyond the documented additions, verdict
ban-list respected.

## Operator steps (if any)

None — pure documentation change. After the implementer's PR merges, the
new policy is live for every subsequent migration-bearing review. No
deploy, no env var, no migration apply, no Supabase function deploy. The
next roadmap card that touches `supabase/migrations/` is the first card
the new gate runs against.
